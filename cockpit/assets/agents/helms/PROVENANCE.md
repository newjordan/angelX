# Knight helm poses

Five original sheets generated by Astra through the built-in imagegen tool on
2026-09-15. Only Astra performed this art generation. The four profile variants
use the Turbo sheet as the layout/pose reference. Exact prompts are in
`prompts.json`; original paths, dimensions and hashes are in `manifest.json`.
All originals are preserved unmodified.

Each sheet has eight poses in a four-column, two-row normalized grid. The
ordinary cockpit crops those cells at runtime and retains bounded 192px frames.
Thinking, acting, long-context attention, approval wait and settled poses follow
live state. Full motion permits infrequent idle glances; Reduced/Off hold the
idle pose. A settled portrait marks a finished turn, not benchmark acceptance.

These portraits belong to the agent pane, outside the Dotmax world. World views
never display these source sheets or any other source PNG as a native plate.
The source manifest records the original dimensions and hashes.
