# angel0-cockpit environment knobs

`ANGEL_WATCH_YUKON_BENCHMARK` and `ANGEL_WATCH_YUKON_SUBMISSION` opt a turn
into authenticated Yukon status observation for that exact pair of full lowercase
UUIDs. Both are required; neither has a default. Fetches run off the turn thread,
at most once per 15 seconds, using normal Yukon CLI credential configuration.
Refresh failures preserve the last observation with a stale notice. Acceptance,
promotion, and a frontier win remain separate; tool prose cannot adopt a slot.

Every `ANGEL_*` variable the cockpit reads, grouped by subsystem. Defaults are
the built-in values; **`0` usually means "disabled/unbounded"** for the numeric
guards. Nothing here is required — the cockpit runs with none set.

## RL campaigns in a loop

- `rl_campaign` is a native tool from the first active loop iteration, including
  Deli and MoA loops, at every thinking level and schema profile. It does not
  depend on a stall, tool search, or an activation limit.
- The model chooses `run`, `status`, `results`, or `stop`. A run uses the loop's
  current route and defaults to its task and acceptance command; it can also
  measure a research subproblem with an explicit task and real verifier.
- Campaigns run asynchronously in isolated copies. The main loop can continue
  useful work. `/rl`, `/rl status`, and `/rl stop` operate the same controller.
- Results and candidate artifacts survive fresh iterations and process restarts.
  Resuming the same loop recalls its latest results without relaunching them.
  Apply a useful candidate to the main workspace and verify it before submitting.
- An independent audit can approve and install a learned prompt policy. Without
  one, the campaign remains measured exploration. Neither path updates model
  weights or proves that a separate trainer consumed the results.
- Pausing, stopping, completing, or leaving a loop cancels its owned campaign.
  There is no default campaign thinking, hop, token, or time cutoff. Explicit
  loop token/time allowances also cover campaign work; token accounting is
  estimated, as in the parent loop.
- Deli deliberates before its first action pass, then forwards native tools and
  their results. It does not repeat all deliberation rounds after each tool call.
- The model can also select a Deli excursion with
  `consult_model({"method":"deli","club":"self","prompt":"...","rounds":6})`.
  Its synthesis returns to the current turn, which can continue with ordinary
  tools. `rounds` is optional and defaults to `ANGEL_DELI_ROUNDS`; this does not
  change the loop mode. Cancellation reaches deliberation and synthesis.
- Active loops advertise `consult_model`, `spawn`, and `continual_harness`
  alongside `rl_campaign`, including compact schema profiles. Deli, MoA, and
  supplemental note retention therefore do not depend on a discovery shortlist.

Example model call: `rl_campaign({"action":"run","rounds":1,"group":3,"samples":2})`.
Use `results` for retained evidence, and submit when there is a verified winner.

> A test reports new `ANGEL_*` literals that lack an entry here.
> `ANGEL_DOC_STRICT=1` turns that report into a failing release/CI gate; without
> strict mode, documentation drift remains a visible non-blocking warning.

## Provider registration

| Variable | Default | Purpose |
| --- | --- | --- |
| `ANGEL_API_CLUBS` | unset (allow configured providers) | Optional comma-separated API-provider allowlist: `glm`, `kimi`, `deepseek`, `qwen`, `longcat`, `cerebras`, `openrouter`, `grok`, `openai`. Unset, `*`, or `all` permits configured providers; empty or `none` disables API routes. Credentials and required model pins still apply. OAuth and local routes are separate. The launcher removes disabled provider keys from its process only when the user supplies this restriction. |

## Agent loop (`harness.rs`)

The optional research-runner client uses `ANGEL_EVALUATOR_SOCKET` (unset by
default): a host-owned private Unix socket for `angel-evaluate full` and
`angel-evaluate status REQUEST_ID`. The runner injects it with
`--worker-evaluator`; it does not grant arbitrary host command execution or
change the cockpit's verification policy.

### Research and sealed-worker controls

`ANGEL_CODING_TRAINING_AUTHORITY_DIR` (unset) enables publication of native
coding-evaluator decisions to the operator-owned authority directory. Recovery
rejects an authority path that overlaps candidate-writable working or scratch
trees, including symlink aliases; an invalid capture configuration disables
capture while preserving ordinary recovery.

| Variable | Default | Purpose |
| --- | --- | --- |
| `ANGEL_TASK_PACE` | `auto` | `rapid` or `deep` pins task cadence; auto resolves from intent and the wall/hop envelope. `--task-pace` takes precedence by setting this value. |
| `ANGEL_TASK_PACE_RESOLVED` | unset | Startup-owned resolved cadence shared by the turn policy and runtime receipt. Do not independently override it in worker descendants. |
| `ANGEL_TASK_PACE_SOURCE` | unset | Startup-owned attribution for the cadence decision: explicit, prompt, budget or default. |
| `ANGEL_TASK_EDITABLE_PATHS_JSON` | unset | JSON relative-path allowlist for sealed task edits and candidate snapshots; for example `["src/ordering"]`. Invalid scope is rejected. |
| `ANGEL_TASK_SHELL_NO_DETACH` | `0` | In an active sealed task, reject shell detachment patterns so a tool receipt owns the process lifetime. |
| `ANGEL_TASK_SHELL_PROTECT_GIT` | `0` | In an active sealed task, admit Git inspection but reject repository-control mutation through shell and Git tools. |
| `ANGEL_SOTA_MOA_STRICT_ROUTES` | `0` | Disable cross-seat/provider fallbacks where the configured strict routing contract applies. |
| `ANGEL_SOTA_MOA_REQUIRE_FULL_WIDTH` | `0` | Require the configured full formation width instead of silently accepting a partial-width run. |
| `ANGEL_SOTA_MOA_TOOL_PREFLIGHT` | explicit formation request | Enable the tool-capable formation preflight. Automatic amplification does not enable it by default. |
| `ANGEL_SOTA_MOA_TOOL_PREFLIGHT_REQUIRED` | `0` | Treat failed preflight as a blocking formation failure rather than optional advice. |
| `ANGEL_SOTA_MOA_PREFLIGHT_ONLY` | `0` | Return the tool preflight result without continuing the later synthesis pass. |
| `ANGEL_DEEPSEEK_REASONING_EFFORT` | provider default | DeepSeek effort override. Applicable competition formation setup seeds `high` only when unset. |
| `ANGEL_DSFLASH_REASONING_PROFILE` | unset in code; repository local profile `deepseek-v4-vllm` | Explicit calibrated local vLLM capability profile, bound to the pinned served model ID and local endpoint. Offers accepted `none/minimal/low/medium/high/xhigh/max` controls using `reasoning_effort`; `none` disables thinking. Does not rename the model, infer cloud context/output limits, or enable official-API private-reasoning replay. Unknown profiles, missing/changed model pins and conflicting dialects fail explicitly. Revalidate when the serving setup changes. Generic HTTP namespaces use the same `ANGEL_<CLUB>_REASONING_PROFILE` form. |
| `ANGEL_PXPIPE_TIMEOUT_MS` | `5000` | Pixel-pipe helper request deadline in milliseconds, clamped to 50–30000. |

### Test fixture names, not operator settings

The documentation scanner also encounters these test-only names. They are not
ordinary cockpit configuration recommendations:

- `ANGEL_DELEGATED_LINEAGE_FIXTURE` is an optional output JSON path for the
  owned native delegate-lineage test. It also writes a `.fixture.json` companion
  identifying the retained test workspace and rollout store. It has no
  production reader and makes no model or submission request.
- `ANGEL_ACP_LIVE_ONE`, `ANGEL_ACP_LIVE_TWO`, `ANGEL_GLM_LIVE_OK`,
  `ANGEL_HARNESS_GLM_OK`, `ANGEL_HARNESS_GROK_OK`, and `ANGEL_HARNESS_TOOL_OK`
  are exact response/output sentinel text, not environment controls.
- `ANGEL_FAKE_GROK_SPAWNS` is a test process-spawn log path;
  `ANGEL_FAKE_GROK_CRASH_ONCE` and `ANGEL_FAKE_GROK_HANG` inject failures into
  the fake Grok process. They are unset outside the fixtures.
- `ANGEL_BUDGET_LEARN_PROBE_MAX_TOKENS`,
  `ANGEL_MAX_TOKENS_CACHE_PROBE_MAX_TOKENS`,
  `ANGEL_PROMPT_CACHE_PIN_CACHE_PROBE_PROMPT_CACHE`, and
  `ANGEL_STREAM_USAGE_PIN_CACHE_PROBE_STREAM_USAGE` exercise synthetic
  per-club token/cache settings. They are unset outside the fixtures.
- `ANGEL_LSP_POSTCHECK` appears as a disabled compatibility fixture setting;
  the active production source has no reader for this spelling.

### Core agent-loop controls

| Variable | Default | Purpose |
| ----------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `ANGEL_YOLO`                                                      | `0` (off)                                        | Explicit operator-owned unrestricted-execution profile (also `angel0 --yolo` or live `/yolo on` / `/yolo full`). While on, Angel bypasses action/swarm/self-edit approvals, every Landlock posture including read-only reviewers, shared subprocess/HTTP-tool/spawn-formation deadlines, PreToolUse hook denials, sealed child-environment credential scrubbing, generic code-mode call/effect limits, HTTP mutation/private-address policy, and background-descendant cleanup. Shell/process networking and filesystem writes are unrestricted. Behavioral reliability remains independent: max hops, turn deadlines, spin/error/read-churn/first-write/mutation/post-green/completion/verification gates, and autonomous-loop iteration/deadline/token/stall budgets still apply. Each gate retains its documented explicit `ANGEL_*` off control. Operator Esc/^C and `/stop` still work; provider/context/process limits remain physical constraints. Full overrides Smart when both are set. |
| `ANGEL_YOLO_SMART`                                                | `0` (off)                                        | Smart / powerful-coding profile (live `/yolos on` or `/yolo smart`). Skips interactive action-capsule modals, auto-approves workspace `ActionBatch`/`SelfTest` scopes and gate-green self-edit merges, and allows effectful `code_mode` without `ANGEL_CODE_MODE_EFFECTS`. **Keeps** Landlock sandbox, tool/HTTP/formation timeouts, PreToolUse hooks, secret scrubbing, and hop/progress/verify/completion gates. Remote phone/peer/host and loop SOTA escalations still prompt. Prefer this over full YOLO for trusted local coding. Cleared when `/yolo on` enables Full. |
| `ANGEL_MAX_HOPS`                                                  | `0` (unbounded) | Runaway guard: maximum tool hops in one interactive, headless, or sub-agent turn. This is not a token/output cap and does not end a multi-turn campaign; work continues through later settled turns. `0` explicitly selects an unbounded individual turn. YOLO does not alter it. |
| — | telemetry only | Metered SOTA input has no million-token stop or rotation guard. Each whole-million boundary crossed during one turn emits one in-place gold-coin receipt and the turn continues; the receipt never enters model history or transcript scrollback. Provider counters are preferred, with the already-computed local request size as fallback. |
| `ANGEL_WORKSPACE` | launcher/TUI: launch cwd; direct binary `--task`: `~/.angel0/workspace` | Workspace root for tools and session binding. `bin/angel0` uses a leading directory or `--workspace DIR` after convenience flags, then this variable (including trusted env files), then original launch cwd. Relative selection resolves against original launch cwd; missing or misplaced TUI selectors fail. Machine commands retain their own options and prompt boundaries; direct `angel --task --workspace DIR` overrides its environment. In the TUI, `/cd <path>` (alias `/workspace`) changes the bound project. |
| `ANGEL_TURN_DEADLINE_SECS`                                        | `0` (off) | Wall-clock budget for one turn. The absolute deadline is propagated into blocking provider and tool cancellation, then recorded as a structured deadline stop at the owned hop boundary. Off by default so autonomous / handoff / loop work is never killed by an invented clock. |
| `ANGEL_COMPETITION_TURN_DEADLINE_SECS`                            | unset (off) | Optional wall-clock for competition turns only when this env is **explicitly set**. Never defaults on — overnight loops must not die at an arbitrary wall. |
| `ANGEL_NO_MUTATION_NUDGE` / `ANGEL_NO_MUTATION_STOP`               | competition nudge: `4` · **stop: always off**    | Competition hops without a workspace mutation: **nudge only** by default. Hard-stop is **never** enabled unless the operator sets `ANGEL_NO_MUTATION_STOP>0` — autonomous/loop work must not be killed.                                                                                                                                                                                                                                                                                            |
| `ANGEL_SUBMIT_STARVE_NUDGE` / `ANGEL_SUBMIT_STARVE_STOP`          | competition nudge: `3` · **stop: always off**    | After a mutation on a competition turn, hops without submit/score: **nudge only** by default. Hard-stop requires explicit `ANGEL_SUBMIT_STARVE_STOP>0`.                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_COMPETITION_MODE`                                          | off                                              | Force competition action cadence even without leaderboard/handoff cues in the prompt.                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_ADAPTIVE_REASONING`                                        | `0` (off)                                        | Adaptive 3-state reasoning ladder. When enabled (`1` / `true`), uses high baseline reasoning effort on hop 0 (turn intake and task planning), dynamically downgrades to `low` (or `none` for binary models) during clean tool execution hops to eliminate thinking latency, and immediately escalates back to `high` on errors, tool failures, anti-spin nudges, or friction. |
| `ANGEL_GLM_FLASH_IDLE_EFFORT`                                     | `low` | Idle `reasoning_effort` for GLM-5.3-Flash when no effort is pinned: `low` (default; measured 2026-09-06: 28 s median vs 64 s on `auto`, same solve rate), `auto` (omit the field, provider decides), `high`, `max`. One rung is held for the whole turn — z.ai keys its prefix cache on the value. | `high` \| `max` pin a rung. Was `high` until 2026-09-05: every hop thought at full depth and the harness arena ran 2× slower than opencode on the same model with no solve-rate gain. |
| `ANGEL_GLM_MAX_TOKENS` | unset | Output-token cap for the registered GLM provider, independent of the model display label. The old model-label spelling `ANGEL_GLM_5_3_FLASH_MAX_TOKENS` is ignored for this provider; use the registered GLM spelling. | positive integer |
| `ANGEL_GLM_THINKING_DEFAULT`                                      | off (disabled)                                   | When unset/off, GLM dialect sends `thinking: disabled` unless effort is explicitly raised (`ANGEL_GLM_REASONING_EFFORT` or THINK chip). Set `enabled` only if you want Z.ai open-ended thinking without an effort pin.                                                                                                                                                                                                                                                                            |
| `ANGEL_GLM_THINKING_BURN` | `0` (off) | Opt-in cumulative reasoning-token budget for locked-thinking GLM seats. A positive value lowers later hops to the lowest effort after that many reasoning tokens without an answer. By default the selected effort stays intact throughout sustained work. Adaptive reasoning is also opt-in. |
| `ANGEL_PROVIDER_RETRIES`                                          | `unset (unbounded)` | Retry count per hop when the in-hand provider fails transiently before visible content, or a stream ends without a terminal event (stall, keep-alive-only stream, severed read, or a partial tool fragment). Unset means unbounded: an implicit count must not end a productive turn, so a recoverable outage retries the same hop with cancel-aware backoff and one notice per attempt until the provider answers or the operator cancels (L01 unattended policy). An explicit integer keeps the bounded policy; `0` means one attempt and no retry. A retry retracts speculative text already streamed and discards partial tool calls, so a completed tool is never replayed or executed twice, and each later hop gets a fresh allowance. A permanent failure outranks the recoverable-stream class: an error that carries both a rejected credential (or exhausted long-window quota) and stall context is not retried at all — authentication, invalid request/model, billing, and exhausted long-window quota fail after one attempt with an actionable message instead of looping. A retry is a fresh provider request and may be billed again: output and reasoning already streamed on the failed attempt are not free, and the progress ledger and rollout manifest count every attempt and mark it recoverable or permanent accordingly. A tool-bearing agent-graph node's retry belongs to its turn, so the graph does not start a second turn after that budget is spent; only a toolless node, which has no turn recovery, keeps the graph's own cancel-aware retry. The trajectory ledger and task envelope record `hop_stream_cuts[] {hop, stream_cut}`. Other errors after visible text are not replayed. An empty reply additionally injects one transient re-prompt. |
| `ANGEL_PROVIDER_RETRY_BACKOFF_MS`                                 | `500`                                            | Base delay before each provider retry (multiplied by the attempt number, capped at 2s, cancel-aware). `0` retries immediately.                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_CONTEXT_OVERFLOW_RECOVERIES`                               | `1`                                              | When a provider rejects an oversized request (413/context-length error), compact and rebuild it once instead of retrying identical bytes. Capped at `2`; `0` disables.                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_TEACHER_WATCH`                                             | `1` (on)                                         | Long-session recovery latch for local/student models. Classifies context overflow, empty replies, transport death, unavailable models, and timeouts; rolls context deterministically and continues when the fault is recoverable. Paid SOTA routes retain their ordinary provider recovery path. `0` disables the latch.                                                                                                                                                                           |
| `ANGEL_TEACHER_WATCH_ASK`                                         | `1` (on)                                         | Let teacher-watch ask one reachable cheap/local teacher for a bounded one-line recovery note. The teacher is optional and has a six-second timeout; recovery remains offline-first when none is reachable. `0` skips the teacher call.                                                                                                                                                                                                                                                           |
| `ANGEL_TEACHER_WATCH_CLUB`                                        | `luna`                                           | Preferred teacher club for teacher-watch. If it is unavailable or is the failing student itself, the watcher falls back only to another reachable cheap/local seat.                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_TEACHER_WATCH_RECOVERIES`                                  | `2` (max `4`)                                    | Maximum long-session recovery attempts within one turn. `0` disables recovery attempts without disabling ordinary provider error reporting.                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_WATCH_FIXTURE`                                             | unset                                            | Deterministic file-backed submission-watcher fixture. When unset, the watcher reads workspace-local `.angel/watch`; neither path opens a live competition API.                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_VERIFY_BEFORE_DONE`                                        | `1`                                              | After code changes, retract unsupported final answers until the bounded denial budget is spent or a real verifier is attempted. Git workspace fingerprints catch opaque edits; known direct prose-only edits are exempt. A red or unavailable verifier permits an immediate honest blocker report. `0` disables.                                                                                                                                                                                   |
| `ANGEL_VERIFY_NUDGES`                                             | `2` (max `4`)                                    | Maximum unsupported completion claims denied after the latest unverified code edit. `0` disables only these denials for ablation/legacy compatibility; a real verifier attempt always releases the gate.                                                                                                                                                                                                                                                                                           |
| `ANGEL_TASK_STRICT_EXIT`                                          | `1`                                              | In `--task` mode, emit the normal answer/JSON envelope but exit nonzero when the turn stopped without an answer. `0` restores legacy zero-exit stopped outcomes for envelope-aware callers.                                                                                                                                                                                                                                                                                                        |
| `ANGEL_BANNED_BOXES`                                             | `spark`                                          | Comma-separated box names removed from every automated selection path (delegation roster, consult/spawn panels, failover). Default bans the spark cheap-test box; empty string clears the ban. Interactive Tab selection is unaffected.                                                                                                                                                                                                                                                          |
| `ANGEL_SOTA_SMART_CLUB` / `ANGEL_SOTA_SMART_EFFORT`              | `sol` / `max`                                    | The ONE seat that `smart`/`sota` resolves to in `consult_model` and `spawn`, with its per-call effort pin — escalation goes to the designated smart seat, never a grab-bag of reachable models. When a TUI approval surface is attached the operator gets a popup (approve = primary, deny = the alternative); headless runs take the primary silently.                                                                                                                                            |
| `ANGEL_SOTA_SMART_ALT` / `ANGEL_SOTA_SMART_ALT_EFFORT`           | `kimi` / `high`                                  | The smart seat's sanctioned alternative: the popup's deny choice, and the availability fallback when the primary is not in the roster (e.g. no ChatGPT OAuth token on the box).                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_TASK_HANDOFF_WARM`                                        | `1`                                              | In `--task` mode, start a fresh session with the workspace's persisted agent-authored `handoff` note injected as an Assistant-role handoff snapshot (the same carrier compaction uses). `0` opts out for deliberately-cold ablations.                                                                                                                                                                                                                                                            |
| `ANGEL_HANDOFF_FILE`                                              | `~/.angel0/handoff/<workspace-key>.json`          | Override path for the persisted per-workspace `handoff` note (identity-bound like `ANGEL_NOTES_FILE`).                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_TASK_EVENT_LOG`                                            | `1`                                              | In `--task` mode, surface tool calls/results and harness notices (guard nudges, compaction, club fallbacks) as `[task-event]` lines on stderr — the headless twin of the TUI's live activity trace, so an operator tailing the log sees *why* a turn slowed or stopped. High-frequency stream deltas are never logged. `0` restores the silent drain.                                                                                                                                               |
| `ANGEL_FIRST_WRITE_CALLS`                                         | unset (off)                                      | Explicit operator-only inspection budget before the first source-changing attempt. The harness never infers or auto-tightens this value from prompt intent, pace, competition mode, or workspace size. `0` disables it.                                                                                                                                                                                                                                                                               |
| `ANGEL_FIRST_WRITE_REJECTIONS`                                    | `0` (off)                                        | Optional hard-stop count for post-budget inspection batches when `ANGEL_FIRST_WRITE_CALLS` is explicitly enabled. `0` keeps the operator-selected budget advisory-only.                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_MUTATION_THRASH_NUDGE`                                     | TUI: `0` · `--task`: `3`                         | Inject a corrective nudge after this many repeats of the same effective mutation signature. `0` disables the nudge.                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_MUTATION_THRASH_STOP`                                      | TUI: `0` · headless rapid/deep: `6`/`4`                         | Stop an unattended turn after this many repeats of the same effective mutation signature. `0` disables the stop.                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_PERIPHERAL_MUTATION_NUDGE`                                 | TUI: `0` · `--task`: `4`                         | Nudge after this many mutations confined to docs, fixtures, testdata, or generated examples without a core implementation change. `0` disables.                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_POST_EDIT_LOGIC_REVIEW`                                    | `1` (on)                                         | After the first successful mutation, prompt for one bounded invariant and state-transition review before verification. `0` disables.                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_POST_GREEN_TOOL_BATCHES`                                   | `0` (off) | Post-green grace window: tool batches dispatched normally after a completion-grade green verifier (for protocol steps like commit/artifact dumps) before the turn is forced to finish. Only a full, unfiltered verifier arms the guard. `0` disables the forced-answer budget.                                                                                                                                                                                                                       |
| `ANGEL_NO_EDIT_ANSWER_GUARD`                                      | `1` (on)                                         | Deny one unsupported completion answer in headless coding when no workspace mutation and no concrete blocker were produced. `0` disables.                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_SELF_AUTHORED_VERIFY_GUARD`                                | `1` (on)                                         | Do not treat a green verifier that targets only tests authored or heavily rewritten this turn as sufficient completion evidence. `0` disables.                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_SINGLE_GREEN_VERIFIER`                                     | `1` (on)                                         | Treat one completion-grade green verifier after a mutation as sufficient for an unchanged workspace and skip redundant full verifiers. Filtered/`--ignored` runs are targeted evidence: they neither arm this nor get skipped by it. `0` permits additional full verifiers.                                                                                                                                                                                                                          |
| `ANGEL_TASK_CODING_DISCIPLINE`                                    | TUI: `0` · `--task`: `1`                         | Inject the compact Map→Edit→Verify→Finish discipline block (implementing library, unique `old`, pre-existing tests, Treebeard strategy roots) into headless coding tasks.                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_TASK_WORKSPACE_MAP`                                        | TUI: `0` · `--task`: `1`                         | Inject a free preturn workspace map (absolute coding root + shallow relative file inventory) into headless `--task` warm-start so paid hops are not spent on `find /` / `$HOME` thrash under YOLO. `0` disables.                                                                                                                                                                                                                                                                                     |
| `ANGEL_TASK_WORKSPACE_MAP_MAX`                                    | `80`                                             | Maximum relative paths listed in the workspace map (clamped 8–400).                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_TASK_WORKSPACE_MAP_BYTES`                                  | `6144`                                           | Maximum bytes for the workspace map block (clamped 512–32768).                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_TASK_ACTIVE`                                               | unset · `--task`: `1`                            | Internal latch set by headless `--task` so tools can apply task-only orientation and full-YOLO scope semantics. Not intended as an interactive operator knob.                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_TOOL_RESULT_DEDUP`                                         | `1` (on)                                         | Replace older exact copies of the same read/search result with a compact receipt while retaining the newest full copy. Reuse is scoped to an unchanged effect epoch; any command, mutation, or other effectful call starts a new epoch. `0` disables for causal measurement.                                                                                                                                                                                                                       |
| `ANGEL_TOOL_RESULT_DEDUP_MIN_BYTES`                               | `200`                                            | Minimum exact inspection-result size eligible for duplicate suppression. Smaller receipts remain verbatim.                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_TOOL_ARGUMENT_SHRINK`                                      | `1` (on)                                         | Replace large historical tool-call arguments with a digest-bearing compact receipt after execution. `0` retains full arguments as an ablation control.                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_TOOL_ARGUMENT_KEEP_CALLS`                                  | `4`                                              | Number of newest tool calls whose original arguments are retained when argument shrinking is enabled.                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_TOOL_ARGUMENT_MIN_BYTES`                                   | `1024` (minimum `64`)                             | Minimum serialized argument size eligible for historical argument shrinking.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_GREP_FILE_SCOPE`                                           | `1` (on)                                         | Let `grep.path` name one confined workspace file as well as a directory. Explicit files retain the same hidden/credential/key exclusions and 2 MiB bound. `0` restores the directory-only rejection behavior as an ablation control.                                                                                                                                                                                                                                                               |
| `ANGEL_GREP_CONTEXT_LINES`                                        | `0`                                              | Default number of surrounding lines returned for each `grep` match when a call omits `context`. Values are capped at 10; overlapping windows merge, matched lines retain `path:line:text`, context lines use `path-line-text`, and total output remains capped at 300 lines. Explicit `context: 0` preserves the compact legacy output.                                                                                                                                                            |
| `ANGEL_MISSING_PATH_HINTS`                                        | `1` (on)                                         | When `read_file` gets a not-found error, append at most five deterministic workspace candidates with the same basename, ranked by matching path suffix. Candidates use search-policy filters and are never opened or substituted automatically. `0` leaves the raw error as a causal control.                                                                                                                                                                                                      |
| `ANGEL_SPIN_LIMIT`                                                | `0` (off) | Anti-spin: stop after this many _identical_ tool batches with no new outcome. `0` disables both identical-batch and short-cycle detection.                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_POLL_REPEAT_LIMIT` | `8` | Stop a turn after the same passive status/log batch occurs this many times within 32 polling batches, even when timestamps or output counters change. Non-poll work or an observed workspace mutation resets the window. Independent of `ANGEL_SPIN_LIMIT`, `ANGEL_POLL_GUARD`, and YOLO. `0` disables; maximum `32`. A spin stop pauses autonomous continuation until explicitly resumed. |
| `ANGEL_SPIN_PERTURB`                                              | `true`                                           | At the anti-spin nudge, inject a concrete reframe (vs a plain "try differently").                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_RL_DIR` | workspace store `rl` subdirectory | Override the durable campaign-store root. Blank values use the normal operator store; isolated runners can point it at a private test directory. |
| `ANGEL_RESEARCH_PYTHON` | `python3` | Python executable for optional `loop_research` Sloptomizer advice/feedback. Uses the bundled standard-library-only algorithms with `-I -S -B`; no pip packages or external Sloptomizer checkout. A missing interpreter is reported before launching an experiment. |
| `ANGEL_REINFORCE_AUTHORITY_DIR` | workspace store `reinforce-authority` subdirectory | Override the durable technical-campaign authority root. Blank values use the normal operator store; campaign IDs remain validated and hashed beneath this root. |
| `ANGEL_ROLLING_REWRITE_HOPS` | `12` | In cache-stable mode, flush aged inspection results and duplicate receipts after this many deferred hops (positive values have a floor of 4). `0` preserves the append-only prefix until a real compaction/prune boundary for comparison. This trades periodic cache rewrites for smaller long-turn requests; current verifier and task state remain protected. |
| `ANGEL_POLL_GUARD` | `0` (off) | Opt-in suppression of repeated passive status checks and long shell sleeps. By default the model can choose how to monitor an experiment; competition watcher notifications remain available. Set `1` to apply `ANGEL_POLL_ONLY_LIMIT` and `ANGEL_PASSIVE_SLEEP_MAX_SECS`. |
| `ANGEL_POLL_ONLY_LIMIT`                                           | `1` batch                                        | Number of status-only batches allowed after candidate progress before another poll-only batch is suppressed (capped at 8). In-flight competition status remains watcher-owned and is suppressed immediately.                                                                                                                                                                                                                                                                                       |
| `ANGEL_PASSIVE_SLEEP_MAX_SECS`                                    | `2`                                              | Longest direct `sleep DURATION` allowed on an actionable/competition turn. Longer or unprovable sleeps are returned as not-started instead of parking the turn, even in a mixed batch; productive sibling calls still execute.                                                                                                                                                                                                                                                                       |
| `ANGEL_TOOL_CYCLE_MAX_PERIOD`                                     | `5`                                              | Outcome-aware alternating-cycle detector: maximum batch-cycle period (2–8). Canonical calls plus exact results and typed outcomes must repeat; changing evidence and successful mutations clear the bounded window. `0` disables.                                                                                                                                                                                                                                                                    |
| `ANGEL_TOOL_CYCLE_REPEATS`                                        | `5`                                              | Consecutive repetitions required before a short tool cycle stops the turn (2–10). A period-2 cycle therefore requires 10 unchanged paired batches by default.                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_TOOLCALL_SCAVENGE`                                         | `0` (off)                                        | Recover a tool call the model stranded in its answer text while the structured `tool_calls` array came back empty (the DeepSeek-reasoner failure mode). Strict: valid JSON, a name offered this request, and an object of arguments — anything else stays prose. Speaks a `scavenge:` notice when it fires.                                                                                                                                                                                        |
| `ANGEL_TOOLCALL_STORM`                                            | `0` (off)                                        | Duplicate-call storm guard: the 3rd byte-identical `(tool, arguments)` call inside the window is not dispatched again; the model gets a tool result naming the repeat count and telling it to reuse the earlier result or change approach. Arguments are compared canonically, so key order and whitespace cannot dodge it.                                                                                                                                                                        |
| `ANGEL_TOOLCALL_STORM_WINDOW`                                     | `6` (hops)                                       | Sliding window, in hops, that the duplicate-call storm guard remembers. `0` disarms the guard.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_NEEDS_PRO`                                                 | `0` (off)                                        | Model self-report escalation: adds a fixed contract to the system prompt telling the serving model it is on a fast tier and may emit `<<<NEEDS_PRO>>>` (or `<<<NEEDS_PRO: one-sentence reason>>>`) as the very first line when THIS task clearly needs stronger reasoning. On detection the reply is discarded (nothing about the attempt is re-sent upstream) and the same turn is re-run once on `ANGEL_NEEDS_PRO_SEAT`, with a `needs-pro:` notice naming the seat and the reason. Purely self-report — no failure counters, no automatic escalation, exactly one escalation per user turn. Requires a resolvable seat: armed without one it says so once per turn and injects no contract.                                                                                          |
| `ANGEL_NEEDS_PRO_SEAT`                                            | unset                                            | The one club the self-report escalation re-runs a turn on, named explicitly (no ranking, no implicit fallback). Matched against the session roster the way `consult_model` matches a typed seat: exact label first, then a label/model-id substring. Unset, unmatched, or naming the seat already in hand disables escalation out loud. The escalation seat is served a stable no-op contract stating it is the top tier and the marker does nothing.                                                                                                                                                                                          |
| `ANGEL_ERROR_LIMIT`                                               | `0` (off) | Consecutive-error breaker: stop after this many hops where _every_ call errored. `0` disables.                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_NOPROGRESS_LIMIT`                                          | `6`                                              | No-progress nudge: hint once after this many hops that only re-read known files (no write/exec/new read). `0` disables.                                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_TOOL_REPAIR_LIMIT`                                         | `2`                                              | Per-session cap on `tool_repair` invocations; once spent the tool refuses and asks the user before further attempts. `0` = unlimited. (`tool_repair` is also diagnose-only unless called with `repair=true`.)                                                                                                                                                                                                                                                                                      |
| `ANGEL_PARALLEL_WRITES`                                           | `true`                                           | Run a batch's disjoint-path writes concurrently (Hermes path-overlap). `0` = read-only batches only.                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_HISTORY_MAX_MSGS`                                          | `0` (off)                                        | Hard cap on conversation length; evicts oldest complete turns at a hop boundary. Eviction rewrites the retained prefix, so a positive cap costs prompt-cache reuse beyond that point (`ANGEL_CACHE_STABLE` does not defer it).                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_CONTEXT_BUDGET_TOKENS`                                     | `0` (auto: 333000 target)                        | Explicit token budget above which oldest turns are _summarized_ (auto-compaction). `0` uses angel0's 333k target, capped down to the model's reported usable window when known.                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_CONTEXT_SOFT_CAP`                                          | `333000`                                         | Default target budget for auto-compaction when `ANGEL_CONTEXT_BUDGET_TOKENS=0`. Reported smaller model windows still cap this down. `0` disables the default target unless an explicit budget is set.                                                                                                                                                                                                                                                                                              |
| `ANGEL_SOTA_CONTEXT_BUDGET`                                       | `120000`                                         | Compaction target when the in-hand club is a token-metered SOTA link (applies only with `ANGEL_CONTEXT_BUDGET_TOKENS=0`, which always wins). The loop re-sends the whole history every hop, so on a paid link the budget bounds **cost**, not context fit — the 333k target would let one long agentic turn bill millions of input tokens. Reported smaller model windows still cap it down. `0` disables the special case (the link budgets like a local club).                                   |
| `ANGEL_SOTA_CACHE_BUDGET_LIFT`                                    | `0` (off)                                        | Opt-in cache-first compaction budget lift for SOTA routes. Paid chat APIs still receive, count, and bill cache-hit input tokens, so cache capability alone keeps the 120k `ANGEL_SOTA_CONTEXT_BUDGET` request-volume target. Set `1` to let a detected byte-exact prefix cache (`Club::prompt_cache_capable`) in cache-stable mode lift that target back to the base 333k target (still capped by the model window minus 20%). An explicitly set `ANGEL_SOTA_CONTEXT_BUDGET` always wins, and non-cache-capable SOTA routes are unaffected. The first enabled lift per session announces itself as a spoken notice (`cache-first: compaction budget 120k -> Nk on <club>`). |
| `ANGEL_NO_AUTOCOMPACT`                                            | unset                                            | `1` disables auto-compaction even when a budget/window is available. Manual `/compact` still works.                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_COMPACT_KEEP_RECENT_TOKENS`                                | `20000`                                          | Target size of the recent live tail that auto-compaction preserves, capped to half the active budget for small-window models. `0` selects the legacy message-count policy below.                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_COMPACT_KEEP_RECENT`                                       | `12`                                             | Recent messages preserved when `ANGEL_COMPACT_KEEP_RECENT_TOKENS=0`; retained as a compatibility/ablation control.                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_COMPACT_BG_PCT`                                            | `80`                                             | Percent of the compaction budget at which an early background pass starts (the summarizer runs off-thread while the turn's hops keep flowing; the sync compactor stays the overflow backstop). `0` (or ≥100) leaves the synchronous 100% path as the only compactor.                                                                                                                                                                                                                               |
| `ANGEL_COMPACT_BG_TIMEOUT_SECS`                                   | `30`                                             | Age at which a slow background model summary is marked stale. The worker may finish and release its permit later, but the live turn uses bounded local compaction instead of waiting. Clamped to `5..900`.                                                                                                                                                                                                                                                                                         |
| `ANGEL_COMPACT_BG_RETRY_SECS`                                     | `120`                                            | Cooldown before background compaction retries after a failed or timed-out pass (a degraded summarizer must not respawn-and-spam every boundary). From the third consecutive failure the wait escalates to 5× this value, capped at an hour; a successful pass resets it. Clamped to `10..3600`.                                                                                                                                                                                                    |
| `ANGEL_COMPACT_SYNC_LLM`                                          | `0` (off)                                        | Compatibility/quality control for synchronous overflow and manual `/compact`. The default deterministic compactor performs no network/model call and preserves typed task/plan/workspace/verifier/skill state. `1` restores model-backed compaction; early background model compaction remains enabled either way.                                                                                                                                                                           |
| `ANGEL_COMPACT_CHUNK_TOKENS`                                      | `12000`                                          | Per-call ceiling for every model-backed compaction pass: automatic/background compaction, plus explicit `/compact` and synchronous overflow when `ANGEL_COMPACT_SYNC_LLM=1`; clamped to `1024..64000`. Keeping this independent of the in-hand model's larger context budget prevents oversized map/reduce calls and keeps the dedicated local compactor responsive.                                                                                                                               |
| `ANGEL_COMPACT_PRUNE`                                             | `1` (on)                                         | Cheap no-LLM pre-pass before summarization: collapses redundant/old tool outputs in the window so the summarizer's input shrinks (often turning a map-reduce back into a single pass) at zero model cost. `0` disables.                                                                                                                                                                                                                                                                            |
| `ANGEL_TOOL_AGING`                                                | `1` (on)                                         | Cheap semantic tool-result aging before model compaction. Only successful reconstructable read/search results (plus superseded bounded skill/plan state) may become provenance receipts such as `[tool output elided: read_file\|src/lib.rs (N bytes) — re-run the tool if needed]`. Verifier/effect, mutation, error, denial, current-plan, and current-skill evidence stays complete. Idempotent; `0` is the executable off-control. |
| `ANGEL_TOOL_AGE_KEEP_HOPS`                                        | `4`                                              | Recent assistant tool-call batches protected in full. Parallel calls in one batch count as one hop, so siblings cannot evict each other.                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_TOOL_AGE_EFFECTS`                                          | `0` (off)                                        | Opt-in: also age old effect-tool results (shell/cargo/tests) past the protected window into head+tail excerpt receipts; default keeps verifier evidence complete.                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_TOOL_AGE_EXCERPT_HOPS`                                    | `12`                                            | Effect-result receipts older than this many tool hops lose their head/tail excerpt and keep only identity, size, and handle; `0` keeps excerpts forever. |
| `ANGEL_TOOL_AGE_PROTECT_TOKENS`                                   | `8000`                                           | Additional newest-first completed-result token tail protected in full. `0` disables this token guarantee while retaining hop protection.                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_TOOL_AGE_MIN_BYTES`                                        | `512`                                            | Minimum eligible inspection size. Smaller results stay complete even outside both protected tails. Values below 128 are clamped.                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_LANE` | unset (`treebeard`) | Harness product lane. Treebeard/RLM/HiQ is the default strategy-only root contract: generic decomposition preamble, auto `handle_read` (unless forced off), and lower default offload floors. Set `default` for the classic ReAct ablation; aliases `rlm`, `hiq`, and `hi/q` also select Treebeard. GPU-competition frontier prose is gated separately and never enters ordinary Treebeard tasks. |
| `ANGEL_RL_REWARD`                                                 | unset (`code_health`; GpuComp → `popcorn_peer`)  | RL scorer for coding-eval / reinforce. `popcorn_peer` scores measured B200 µs vs living peer shape HOLD floor (PRIMARY 32768×1 → r7@38300). `code_health` / `test` / `lint` use verifier evidence. GpuComp formation pins `popcorn_peer` when unset; leaving the formation clears that default. See `reinforce::reward_from_env` / `score_coding_eval_reward`.                                                                                                                                 |
| `ANGEL_TASK_TREEBEARD`                                            | `--task` default: `auto`                         | Headless compatibility selector. Treebeard is already the global default; `1`/`always` pins it explicitly, while `0`/`off` now pins `ANGEL_LANE=default` for a real classic-ReAct ablation. Explicit `ANGEL_LANE` always wins. |
| `ANGEL_TASK_TREEBEARD_HOPS`                                       | `32`                                             | Floor for `ANGEL_TASK_TREEBEARD=auto`. Hop budgets at or above this (or explicitly unbounded via `ANGEL_MAX_HOPS=0`) opt into Treebeard for long headless coding. YOLO does not alter the hop budget.                                                                                                                                                                                                                                  |
| `ANGEL_HANDLE_STORE`                                              | `1` (on)                                         | Session handle store for RLM/HiQ-style context offload. When aging elides bulk inspection results, bodies are retained under opaque `hnd_*` handles and root history keeps a handle-bearing receipt instead of discarding content. `0` restores discard-only aging receipts (ablation/off control).                                                                                                                                                                                              |
| `ANGEL_HANDLE_MAX_ENTRIES`                                        | `256`                                            | Maximum live handles in the process session store; oldest entries are evicted first.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_HANDLE_MAX_TOTAL_BYTES`                                    | `33554432` (32 MiB)                              | Aggregate body budget for the session handle store.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_HANDLE_MAX_BODY_BYTES`                                     | `2097152` (2 MiB)                                | Per-handle body cap; larger deposits are UTF-8-safe truncated on put.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_HANDLE_MAX_DISCLOSE_BYTES`                                 | `16384`                                          | Hard cap on a single `handle_read` / `session_disclose` slice.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_HANDLE_MAX_PATHS` | `4` | Maximum file paths listed in a handle receipt before the list is elided. Keeps a receipt for a wide multi-file result readable and bounded in root history. |
| `ANGEL_HANDLE_MAX_PREVIEW_CHARS` | `80` | Characters of body preview carried in a handle receipt. Enough to recognize what was parked without re-inlining the content the handle exists to keep out of history. |
| `ANGEL_HASHLINE_ANCHORS` | `1` (on) | When on (default), `read_file` prefixes a `[path#tag]` header and absolute line numbers so the model can author token-cheap hashline `apply_patch` edits. Set `0`/`false`/`off` for plain unnumbered pages. The same content tag backs the optional `expect_tag` stale-edit guard on `str_replace`/`multi_edit`. |
| `ANGEL_HANDLE_READ_TOOL`                                          | `0` (off); auto-on under Treebeard               | Register the model-facing `handle_read` tool for explicit capped disclosure. Off by default so the ordinary schema set does not grow. When `ANGEL_LANE=treebeard` and this var is unset, the tool registers automatically; set `0` to force off.                                                                                                                                                                                                                                                  |
| `ANGEL_HANDLE_CODE_MODE_MIN_BYTES`                                | `4096` (default); `1024` under Treebeard if unset | When `code_mode` returns a successful body at least this large, offload it under a session handle and keep only the handle receipt plus the strategy-level orchestration line in root history. `0` offloads every success (still subject to `ANGEL_HANDLE_STORE`). Explicit values always win over lane defaults.                                                                                                                                                                                 |
| `ANGEL_HANDLE_SUBCALL_MIN_BYTES`                                  | `2048` (default); `512` under Treebeard if unset | Floor for offloading `spawn` seat digests and `delegate` answer/diff bulk under session handles. Strategy headers (formation line, club/branch) stay root-visible. `0` offloads every non-empty bulk (still subject to `ANGEL_HANDLE_STORE`). Explicit values always win over lane defaults.                                                                                                                                                                                                     |
| `ANGEL_HANDLE_EAGER_TOOL_MIN_BYTES`                               | `16384`                                          | Soft bulk veto: successful inspection tool results at least this large are parked under a session handle *before* entering root history (mutations/verifiers/errors never). Eager parking is disabled when `handle_read` is unavailable, and the implicit floor matches one complete default disclosure so smaller results do not cost a second provider hop merely to recover the same bytes. Explicit values always win.                                                                                                                                    |
| `ANGEL_TREEBEARD_MAX_DEPTH`                                       | `2` under Treebeard; `1` otherwise               | Nested LM depth for spawn/delegate seats (0 = root). Spawn is refused when `subcall_depth >= max`. Default lane keeps max `1` so seats cannot re-spawn; Treebeard allows one nested formation level.                                                                                                                                                                                                                           |
| `ANGEL_DESCENDANT_CALL_BUDGET`                                   | `0` (unbounded) | Cumulative logical model-descendant allowance for one root turn, constrained to `1..=1024`. Spawn panels reserve the complete formation atomically (including an MoA synthesizer); agent graphs reserve every declared node before launch and each retry wave before invalidation. Nested seats inherit the same allowance, failed/cancelled calls are not refunded, and the independent live-seat limit remains in force. Invalid values fail descendant admission closed. |
| `ANGEL_ROOT_TRAJECTORY`                                           | `0` (off for unlabeled)                          | Root-only strategy fingerprint on trajectory rows. **Reward-labeled training rows always include `root_trajectory` + `harness_treatment`** (including `lane`) regardless of this flag (RL/forge contract). Rows also carry `offload_ratio` and `hiq_priority` for Forge curriculum weighting. Set `1` to also attach them on unlabeled analysis logs. Bulk tool bodies are classified, not embedded.                                                                                                                                                                               |
| `ANGEL_COMPACT_URL` / `ANGEL_COMPACT_MODEL` / `ANGEL_COMPACT_KEY` | unset / `default` / unset                        | Optional cheap OpenAI-compatible summarizer endpoint for compaction; always wins when set. Otherwise see `ANGEL_COMPACT_LOCAL`.                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_COMPACT_LOCAL`                                             | `1` (on)                                         | Local-first model summarization: when a background or explicitly model-backed pass would use a token-metered SOTA link or fan-out pipeline, use the first reachable plain local fleet model instead. `0` restores summarize-in-hand everywhere — the sync path uses the in-hand club and the early background pass (which cannot borrow the busy in-hand club) simply does not arm. The default critical-path compactor is already deterministic and model-free.                                                                                                                                                                                  |
| `ANGEL_MEMPALACE_CMD`                                             | unset                                            | Command line that starts the [MemPalace](https://github.com/mempalace/mempalace) MCP server over stdio (e.g. `mempalace-mcp --palace /srv/models/angel-palace --backend chroma`, or `ssh local-model-host mempalace-mcp --palace …`). Enables the long-form memory "palace": auto-compaction/`/compact` deposit distilled notes here and the `recall` tool reads them back. Unset → no palace (no-op).                                                                                                   |
| `ANGEL_MEMPALACE_TIMEOUT`                                         | `30` (s)                                         | Per-request deadline for MemPalace MCP calls (deposit/search/status).                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_MEMPALACE_CONNECT_TIMEOUT`                                 | `8` (s)                                          | Startup handshake deadline; a slow/wedged MemPalace (or stalling `ssh`) degrades to no palace instead of hanging launch.                                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_COMPACT_FANOUT`                                            | `8`                                              | Max concurrent summarizer calls during map-reduce compaction (bounds the thread/connection fan-out on a huge window).                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_DEBUG_CTX`                                                 | unset                                            | `1` emits a per-turn notice showing detected model context window and effective compaction budget.                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_AUTO_RECALL`                                               | `true`                                           | Search long-form memory for the current project/query and inject relevant compacted notes. `0` disables.                                                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_AUTO_RECALL_K`                                             | `12`                                             | Number of long-form memory drawers to recall per distinct project/query, clamped to `1..50`.                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_RECALL_TIMEOUT_SECS`                                       | `3`                                              | Deadline for the auto-recall palace search (run on a helper thread): a slow palace skips recall for the turn instead of stalling the model call.                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_BACKPLANE`                                                 | `1` (active)                                     | Unified route/resource registry and deterministic Knowledge Broker. The completed shadow-parity path is active by default and emits one replaceable Harness context block capped at 8,000 tokens and the selected route's remaining budget. `shadow` keeps legacy injection while reporting broker selections; `0` preserves legacy routing/context injection exactly. |
| `ANGEL_DOSSIER_MAX_AGE_DAYS`                                      | `30`                                             | Active-backplane read-time freshness ceiling for Dossier facts and thread state. Age is recomputed from the artifact timestamp even when the external Dossier compiler has stopped. Legacy mode retains the historical renderer. |
| `ANGEL_ATLAS`                                                     | `1` (on)                                         | Enable the project-bound Living Atlas service, `/atlas` UI/commands, and deferred `atlas` observation/search/trace/proposal tool (model claims and relations require review). Project snapshots live under `~/.angel0/atlas/projects/`, explicitly promoted knowledge under `~/.angel0/atlas/shared.json`, and reviewed lessons in a separate `lessons/` lane. `0` removes the tool, commands, lens, UI changes, and Atlas inspection state so the prior cockpit behavior is restored.                                                                                                               |
| `ANGEL_ATLAS_LENS`                                                | `1` (on)                                         | Enable reviewed Atlas candidates for task context. In legacy/shadow mode this keeps the historical six-item, 2-KiB lens; active backplane mode feeds the candidates through the one combined Knowledge Broker cap instead. `0` leaves the Atlas UI, review queue, persistence, and deferred tool available while disabling automatic context. |
| `ANGEL_ATLAS_LINKS` | `1` (on) | Follow reviewed `related` and `derived_from` project links when building the bounded Atlas lens. `0` is the lexical-only retrieval ablation; digest validation and revocation remain active. Source observations use native confined, symlink-free reads (512 KiB/file); selected evidence checks cap at 64 dependency items, 32 distinct files and 8 MiB total source bytes. |
| `ANGEL_ATLAS_DIR`                                                 | `~/.angel0/atlas`                                 | Override the Atlas storage root. Intended for isolated tests and controlled migrations; repository identity fields are still validated on every read and write.                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_DEFERRED_ACTION_LIMIT`                                     | `4`                                              | Consecutive assistant false-starts (announced work or raw tool markup with no real tool call) before the turn stops instead of nudging again.                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_NOPROGRESS_STOP`                                           | `2×ANGEL_NOPROGRESS_LIMIT`                       | Hard stop for read churn: after the no-progress nudge, this many hops that still only re-read known files halt the turn. `0` disables the hard stop (the nudge remains).                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_RELENTLESS_EXECUTION`                                      | `--task` default: `1` (on)                       | Headless `--task` runs: prepend the relentless-execution directive (the standing "do the work through tools, don't announce it" steer) to the task prompt. Production coding defaults on; set `0` to disable. Interactive TUI uses `/relentless` latch instead.                                                                                                                                                                                                                                    |
| `ANGEL_SPAWN_MAX`                                                 | `8`                                              | Seat cap for the `spawn` tool's ad-hoc formations (solo/panel/moa/quorum).                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_SPAWN_INFLIGHT_MAX`                                        | `max(64, ANGEL_SPAWN_MAX)`                       | Process-wide admission limit for spawn-seat threads. Timed-out seats remain counted until their provider call exits, preventing repeated formations from accumulating unbounded detached workers.                                                                                                                                                                                                                                                                                                  |
| `ANGEL_GRAPH_MAX_SEATS`                                           | `3`                                              | Concurrent node seats per agent-graph run (`/graph`, `agent_graph` tool). Ready nodes beyond the cap queue until a landing frees a seat; the process-wide spawn admission limit still applies on top.                                                                                                                                                                                                                                                                                              |
| `ANGEL_GRAPH_DEADLINE_SECS`                                       | `0`                                              | Optional whole-run deadline for an agent-graph run when the spec sets no `deadline_secs`; zero (the default) means no deadline. On expiry the run cancels its seats, marks the graph failed, and says so.                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_GRAPH_DEP_CAP`                                             | `12000`                                          | Per-dependency character cap when upstream node outputs flow into a downstream graph-node prompt (floor 1000). Truncation always leaves a visible marker; raise it for code-heavy graphs where cutting a module would corrupt integration.                                                                                                                                                                                                                                                          |
| `ANGEL_GRAPHS_DIR`                                                | `~/.angel0/graphs`                                | User agent-graph spec dir (TOML, one graph per file). User specs win over bundled ones on graph-name collision.                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_BUNDLED_GRAPHS_DIR`                                        | `cockpit/graphs`                                 | Override for the bundled agent-graph spec dir (tests / packaging).                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_AGENT_GRAPH_DIR`                                           | `~/.angel0/agent-graphs`                          | Where finished agent-graph run records (nodes, outputs, answer, verdict) are written, one JSON per run under a per-workspace key.                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_KNOWLEDGE_DIR`                                             | `~/.angel0/knowledge`                             | Directory of typed knowledge-graph stores (`knowledge_graph` tool, `/kg`), one JSON per workspace key. Entities carry a declared type (PERSON/ORGANIZATION/LOCATION/EVENT/ARTIFACT) and every node/edge carries source-document provenance.                                                                                                                                                                                                                                                          |
| `ANGEL_KG_DOC_CAP`                                                | `24000`                                          | Character cap on one document fed to knowledge-graph extraction (floor 1000). Truncation is reported in the ingest receipt, never silent.                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_KG_QUERY_EDGES`                                            | `400`                                            | Max edges serialized into a knowledge-graph query prompt (floor 10), picked by question-term overlap. The answer notes when the subgraph was capped.                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_KG_HUB_DEGREE`                                             | `3`                                              | Edge count at which a knowledge-graph entity counts as a hub and becomes eligible for a synthesized profile (`knowledge_graph` op=summarize).                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_KG_SUMMARIZE_CAP`                                          | `5`                                              | Hub profiles written per summarize run (one club call each). A remaining backlog is reported so repeat runs are an informed choice, not a guess.                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_SPAWN_TIMEOUT`                                             | `0` (unbounded) | Default per-seat deadline for `spawn` (the call's `timeout_secs` overrides; clamped `10..3600`).                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_PERSONAS_DIR`                                              | `~/.angel0/personas`                              | User persona bundles for `spawn` and agent graphs: canonical `<name>/PERSONA.md` (frontmatter name/description + body), with legacy `<name>/SKILL.md` and flat `<name>.md` accepted. User personas win case-insensitively over bundled names.                                                                                                                                                                                                                                                            |
| `ANGEL_BUNDLED_PERSONAS_DIR`                                      | unset                                            | Optional bundled-persona overlay for tests/packaging. Matching names override the embedded shipped defaults; unmatched embedded defaults remain available after binary relocation. Uses the same canonical `PERSONA.md` and legacy compatibility layouts as `ANGEL_PERSONAS_DIR`.                                                                                                                                                                                                                   |
| `ANGEL_HABIT_PROPOSED_DIR`                                        | `~/.angel0/skills-proposed`                       | Where Habitsmith writes draft skills awaiting `/habits` approval.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_SWARM_PALACE`                                              | `1` (on)                                         | When a palace is configured, file each swarm synthesis to it (via the Librarian) so multi-agent answers persist. `0` disables; curation (dedup/importance) is still pass-through, so this files one drawer per swarm turn.                                                                                                                                                                                                                                                                         |
| `ANGEL_REASONING_EFFORT`                                          | unset                                            | Initial `reasoning_effort` for compatible clubs. The OpenAI/Codex agent copies it into model-local session state so the agent panel can change effort safely between turns; other compatible HTTP clubs still read it as a request hint.                                                                                                                                                                                                                                                           |
| `ANGEL_EFFORT_SNAPSHOT`                                           | `1` (on)                                         | Revision+TTL snapshot of dialect/gate/override/env effort facts on HTTP (and Grok) clubs. One lock replaces the multi-mutex + env reads the draw path paid every frame for `reasoning_effort` / THINK ladder / body dialect. Invalidates immediately when `route_state_revision` bumps (THINK override, rejection learn, model swap); env dialect/effort re-reads after a 150ms TTL. Gate-reason strings stay verbatim so effort gates keep speaking. `0`/`false`/`off`/`no` restore the historical per-call path. |
| `ANGEL_ROUTE_MEMORY`                                              | on                                               | Remember the last explicit interactive MODEL/THINK choice across cockpit restarts. Restore is advisory: `ANGEL_DRIVER` / `ANGEL_REASONING_EFFORT` pins win, stale or malformed records are ignored, and an unavailable route is never selected. `0` disables both restore and writes; `/model auto` clears the remembered choice.                                                                                                                                                                  |
| `ANGEL_ROUTE_MEMORY_FILE`                                         | `~/.angel0/brain-route.json`                      | Override the schema-versioned route-memory file. It contains only agent/driver/model/reasoning-effort metadata.                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_ROUTE_EVIDENCE_BYTES`                                      | `2097152`                                        | Maximum recent byte tail read from the privacy-safe experience ledger when the Brain Route deck opens (clamped to 16 MiB). Operational reliability/latency/failover/token evidence and explicit `/rate useful                                                                                                                                                                                                                                                                                      | miss` labels are displayed as separate channels; prompt/response text is never read. |
| `ANGEL_PROMPT_CACHE`                                              | `on` (capability-gated)                          | Send a stable **per-`HttpClub` instance** `prompt_cache_key` so prefix-caching backends (vLLM APC, llama.cpp, OpenAI-compatible clouds) reuse the KV cache across the loop's full-history re-sends. The key is minted once per instance from the club label plus process id/timestamp material, is shared by every handle that instance hands out (in-hand route, delegates, spawn seats) for that process's lifetime, and is **never persisted** — a restarted cockpit, including `--resume`, starts a new key. `ANGEL_PROMPT_CACHE_KEY` overtakes the generated key and always wins. Default on **only** for backends detected cache-capable (live window probe or static model map), so unknown/strict servers keep byte-identical bodies. `0` disables; an explicit truthy value restores the old opt-in semantics (send unless known non-caching). |
| `ANGEL_PROMPT_CACHE_KEY`                                          | unset                                            | Explicit `prompt_cache_key` to pass through to the model API (always wins).                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_CACHE_STABLE` | `1` | Preserve previously sent history across hops and user turns, including unknown providers. Aging, completed-argument shrinking and inspection dedup run only at compaction/prune boundaries; the per-turn `cache-stable:` notice reports retained content bytes and actual saved bytes. Emergency context fitting can still trim payloads to the provider window. Explicit `0` retains the historical per-hop token-first ablation. Explicit `ANGEL_HISTORY_MAX_MSGS` pruning is **not** deferred: a positive cap evicts the oldest messages at a hop boundary, which breaks the cached prefix from the eviction point on. |
| `ANGEL_CACHE_HOP_LEDGER`                                          | `1` (on)                                         | Master switch for the per-hop prompt-cache ledger. Every provider call in `run_turn` snapshots the club's cumulative cache/token counters into a per-route sample with prefix-breaker tags; `/usage` renders one row per route and a hit-rate drop emits a Notice naming its breakers. `0` disables both sampling and notices — `/usage` then shows an explicit "per-hop ledger off" row instead of silently vanishing. Measurement-only: never mutates a request.                                     |
| `ANGEL_CACHE_LEDGER_DROP_PCT`                                     | `20`                                             | Percentage-point fall in a route's per-hop cache hit rate (vs its previous reported hop) that earns a `cache ledger: <route> hop N hit X% → Y% after <breakers>` Notice. Values ≤1 mean any fall notices. Hops whose prompt denominator is under 512 tokens are exempt as numerically noisy.                                                                                                                                                                                                        |
| `ANGEL_TOOL_KILL_GRACE_MS` | `0` (raw commands); helper minimum `500` ms | SIGTERM-first grace before timeout SIGKILL of the process group. Supervised sandbox tools receive at least 500 ms so the helper can reap detached descendants (its escalation grace is 250 ms). Raw commands retain the configured grace, including zero. The timeout diagnostic reports the grace actually used. |
| `ANGEL_TOOL_TIMEOUT`                                              | unset (`0` = unlimited)                         | Optional external-command timeout for shell/cargo/verifier tool runs (fixed verifier/recon deadlines are exempt). Headless `--task` runs derive it (and `ANGEL_TOOL_HARD_TIMEOUT`) from the turn deadline when unset; an explicit operator value still wins. **In-flight extension**: the kill decision re-reads the live value, so raising it (or setting `0`) while a job runs extends it instead of killing — the receipt then notes `deadline extended in flight`. `harness/exec.rs::tool_timeout` + `output_timed_extensible_cancellable_with_progress`. |
| `ANGEL_TOOL_HARD_TIMEOUT`                                         | unset (`0` = unlimited while busy)              | Optional wall-clock ceiling for a busy process group. Headless tasks derive it from their turn deadline. |
| `ANGEL_TOOL_IDLE_FLOOR_SECS`                                      | unset (`0` = off)                               | Optional idle-kill heuristic. It is off by default because silence and sampled process state cannot reliably distinguish a hang from legitimate GPU or remote work. Set a positive value only when the operator wants that tradeoff. |
| `ANGEL_TOOL_OUTPUT_MAX_BYTES`                                     | `65536`                                          | Ceiling on each tool result before it enters history (head-then-tail). `0` disables.                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_TOOL_OUTPUT_MAX_LINES`                                     | `800`                                            | Line ceiling companion to the byte cap.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

## Technical evaluator and build identity

| Variable | Default | Purpose |
| --------------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_EVALUATOR_ARTIFACT_DIR`    | unset                    | Optional append-only destination for persisted evaluator evidence. Technical release campaigns normally use their framework-owned authority instead. |
| `ANGEL_EVALUATOR_CANDIDATE_PATH`  | framework-injected       | Absolute scratch path supplied to a pinned evaluator adapter for the frozen candidate input. User configuration is not expected.                     |
| `ANGEL_BUILD_SOURCE_SHA256`       | unset compile-time value | Build-time source identity exposed by diagnostics when the build pipeline injects it.                                                                |
| `ANGEL_PINNED_GIT_TEST_WORKSPACE` | unset; tests only        | Test-only workspace override used to verify that pinned Git discovery remains stable across hostile environment changes.                             |

## LSP code intelligence (`lsp.rs`)

Opt-in. When on, registers `lsp_diagnostics`, `lsp_definition`, `lsp_references`,
`lsp_hover`, `lsp_symbols`, `lsp_workspace_symbol` — all backed by one warm
language server per language.

| Variable | Default | Purpose |
| --------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_LSP`           | auto                | `1` enables the LSP tools (servers spawn lazily, per language, on first use). `0` forces off. Unset auto-enables when a known server binary (rust-analyzer / pyright-langserver / typescript-language-server / bash-language-server / yaml-language-server) for a language actually present in the workspace is on PATH. An unprovisioned rustup proxy does not count as a runnable rust-analyzer component. This keeps post-edit self-correction on for real projects but off where nothing can run. |
| `ANGEL_LSP_PREWARM`   | `1` (on)            | Pre-spawn and pre-index the workspace's detected language servers off-thread at registry build, so the first edit doesn't pay the cold spawn + full index synchronously. `0` = spawn on first use only.                                                                                                                                                                                                                                                                                               |
| `ANGEL_LSP_TIMEOUT`   | `30` (s)            | Per-request timeout; also the cold-index wait. Raise on large/contended projects.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_LSP_SETTLE_MS` | `1200`              | After the first diagnostics push, how long to keep draining for a refined set.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_LSP_CONFIG`    | `~/.angel0/lsp.json` | Override/extend the built-in server map (rust-analyzer/pyright/tsserver/bash/yaml); user wins by name.                                                                                                                                                                                                                                                                                                                                                                                                |

## Tools & dev environment

| Variable | Default | Purpose |
| ----------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_CODE_MODE`                   | `true`                    | Register `code_mode` (model-authored JS in V8 with every tool bound). V8 inits only on first call.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_CODE_MODE_TIMEOUT_MS`        | `300000`                  | Watchdog for a `code_mode` program. `0` = unbounded.                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_CODE_MODE_HEAP_MB`           | `256`                     | V8 heap cap for `code_mode`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_CODE_MODE_BATCH_CONCURRENCY` | machine ≤16               | Concurrency for `code_mode`'s `batch()` mass-testing primitive.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_CODE_MODE_MAX_BATCH_ITEMS`   | `48`                      | Maximum entries a single `code_mode` `batch()` may materialize.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_CODE_MODE_MAX_SCRIPT_BYTES`  | `65536`                   | Maximum UTF-8 bytes in a model-authored `code_mode` program.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_CODE_MODE_MAX_CALLS`         | `48`                      | Maximum nested tool calls made by one generic `code_mode` program.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_CODE_MODE_MAX_NESTED_OUTPUT_BYTES` | `8388608`           | Cumulative nested-tool output budget for one generic `code_mode` program.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_CODE_MODE_MAX_CONSOLE_BYTES` | `65536`                   | Maximum captured `console.log` bytes per `code_mode` execution.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_CODE_MODE_EFFECTS`           | `false`                   | Second operator-controlled gate for effectful nested tools. Both this knob and the call's `allow_effects=true` are required; the built-in repo-recon recipe remains read-only.                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_TASK_RECON`                  | TUI: `off` · `--task`: `repo` | Headless pre-turn repository reconnaissance: `off`, `repo`, or byte-shape-matched `placebo`. Task mode defaults to free `repo` recon (symbol map + lockfile hits); interactive TUI stays off unless set.                                                                                                                                                                                                                                                                                                      |
| `ANGEL_TASK_RECON_MAX_BYTES`        | `12288`                   | Maximum injected reconnaissance context bytes, clamped to 1–65536.                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_TASK_RECON_MAX_CALLS`        | `24`                      | Nested-call ceiling for the trusted read-only repo-recon recipe (v2: manifests, defs symbol map, outlines); it cannot exceed the generic call ceiling or 32.                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_TASK_RECON_MAX_NESTED_OUTPUT_BYTES` | `4194304`          | Cumulative nested output ceiling for repo recon; it cannot exceed the generic budget or 4 MiB.                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_TASK_RECON_TIMEOUT_MS`       | `15000`                   | Repo-recon recipe deadline, clamped to 100–60000 ms.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_REUSE_VERIFIER_RESULTS`      | `1`                       | Reuse a conclusive semantically identical verifier result when the Git-backed workspace fingerprint is unchanged. Specialized Cargo tools, direct Cargo calls, and a plain shell Cargo invocation share conservative identities; composed shell commands remain distinct. Disable with `0` for an exact re-execution control. Non-Git workspaces and inconclusive or denied attempts always execute normally.                                                                                                      |
| `ANGEL_POST_EDIT_DIAGNOSTICS`       | `true`                    | After a successful supported edit, query registered LSP diagnostics for the touched paths.                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_POST_EDIT_DIAGNOSTIC_MAX_PATHS` | `4`                   | Maximum edited paths diagnosed per tool result, capped at 8.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_POST_EDIT_DIAGNOSTIC_TIMEOUT_MS` | `1500`                | Per-path post-edit diagnostic deadline, clamped to 50–5000 ms.                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_POST_EDIT_DIAGNOSTIC_MAX_BYTES` | `8192`                 | Maximum diagnostic bytes returned to the model, clamped to 512–32768.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_FINAL_MILE_HOPS`             | TUI: `0` · headless rapid/deep: `6`/`4`  | Mutation-scoped hop reserve that steers a changed workspace toward verification or a concrete diagnostic-backed edit near the horizon. Explicit values always win; `0` is the exact off control.                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_FINAL_MILE_ANSWER_HOPS`      | TUI: `0` · headless rapid/deep: `2`/`1`  | Within an active final-mile reserve, withdraw structured tool schemas for the last N policy calls and require an evidence-backed final answer or blocker. This prevents a bounded coordinator from spending every remaining hop on inspection after its candidate is settled. `0` preserves advisory-only final-mile behavior.                                                                                                                                                                                        |
| `ANGEL_EVALUATE_MAX_HOPS_WORKSPACE` | `false`                   | Evaluator-only: return a structured max-hops outcome so the caller can score the actual workspace instead of dropping the cohort row.                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_WEB_SEARCH`                  | `true`                    | Register the `web_search` tool over local SearXNG.                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SEARXNG_URL`                 | local SearXNG             | SearXNG endpoint for `web_search`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_WEB_FETCH`                   | `true`                    | Register the `web_fetch` tool (HTTP GET a URL → readable text; HTML stripped). `0` disables.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_HTTP_REQUEST`                | `true`                    | Register the guarded `http_request` tool (GET/HEAD plus headers/body → status + raw body) for JSON APIs. `0` disables. Mutating methods require the separate explicit opt-in below.                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_HTTP_ALLOW_MUTATIONS`        | `false`                   | Permit `POST`, `PUT`, `PATCH`, and `DELETE` in `http_request`. Mutating calls never follow redirects and interactive-root calls also enter the action-capsule approval flow, whose modal shows the canonical exact action-batch scope separately from the free-form prompt. Leave disabled for read-only research.                                                                                                                                                                                         |
| `ANGEL_HTTP_ALLOW_PRIVATE_NETWORK`  | `false`                   | Permit HTTP tools to resolve RFC1918, IPv6 ULA, or CGNAT/Tailscale targets. Explicit loopback remains available for local services. Link-local/cloud-metadata, unspecified, multicast, documentation, benchmark, and reserved ranges remain blocked even when enabled; every redirect hop is revalidated.                                                                                                                                                                                                         |
| `ANGEL_PROC_TOOLS`                  | `true`                    | Register `proc_run`/`proc_status`/`proc_stop` — background process management (launch a daemon detached with output to a log file, poll its state + log tail, stop the whole process group). Same default-confined sandbox posture as `shell`; `ANGEL_SANDBOX=0` is the explicit escape hatch. `0` disables the tools.                                                                                                                                                                                         |
| `ANGEL_PROC_DIR`                    | `~/.angel0/proc`           | Base directory for project-keyed `proc_run` logs (`<project-key>/...`). Status and stop handles are visible only to the project that launched them.                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_PROC_RECEIPTS` | `1` (on) | Persist project-bound running and terminal process receipts under `ANGEL_PROC_DIR`. Jobs survive successful turns in the cockpit; cancellation/failure stops that turn's jobs. Explicit `proc_stop` or cockpit exit stops remaining jobs. Historical live receipts remain starttime-checked for adoption; vanished workers report an unknown exit as an error and retain their receipt. `0` disables persistence and is announced in tool output. |
| `ANGEL_FLEET_TOOLS`                 | `true`                    | Register the read-only fleet recon tools: `gpu_stat` (nvidia-smi → rocm-smi → xpu-smi), `fleet_status` (tailscale), `vast_instances` (vastai CLI). Recon only — nothing mutates. Missing CLIs surface as call-time messages, not registration failures. `0` disables.                                                                                                                                                                                                                                          |
| `ANGEL_YUKON_FLEET_BENCHMARKS`      | unset                     | Optional comma-separated exact Yukon benchmark names for the competition Miniviz. When set, the watcher skips open-benchmark discovery and queries only this bounded, deduplicated allowlist (maximum 32); unset preserves discovery of every open benchmark.                                                                                                                                                                                                                                             |
| `ANGEL_MACHINE_QUEUE_HOST`          | unset                     | SSH destination for the scarce shared test machine. When set, registers the deferred `machine_test` tool. Every angel0 invokes the broker on that machine, so independent rigs share one crash-recoverable SQLite queue instead of maintaining conflicting local queues. |
| `ANGEL_MACHINE_QUEUE_RESOURCE`      | `mac-test`                | Queue/resource name on the shared host. All clients competing for the same physical machine must use the same value. |
| `ANGEL_COMPETITION_ID`              | workspace directory name  | Stable competition label for remote-test receipts and, unless an explicit owner is set, the round-robin owner identity. |
| `ANGEL_MACHINE_QUEUE_OWNER`         | `ANGEL_COMPETITION_ID`    | Stable fair-share identity. The scheduler grants one oldest request for the least-recently-served owner, so an owner with a backlog goes behind other waiting owners after every test quantum. Do not generate a new value per request. |
| `ANGEL_MACHINE_QUEUE_REMOTE_CWD`    | `.`                       | Existing working directory on the remote machine for `machine_test`. Workspace synchronization is deliberately separate from the scarce-machine lease. |
| `ANGEL_MACHINE_QUEUE_REMOTE_SCRIPT` | `.local/bin/angel-machine-queue.py` | Broker path under the remote user's home. Install with `python3 scripts/runtime/angel-machine-queue.py client-install --host HOST`. |
| `ANGEL_MACHINE_QUEUE_REMOTE_DB`     | `~/.angel0/machine-queue.sqlite3` | Owner-only SQLite scheduling ledger on the scarce machine. It stores bounded receipts and command SHA-256 digests, never raw commands or command output. |
| `ANGEL_MACHINE_QUEUE_CLIENT`        | repository `scripts/runtime/angel-machine-queue.py` | Local client path override for packaged/non-checkout deployments. |
| `ANGEL_MACHINE_QUEUE_WAIT_SECS`     | `0` (unbounded)           | Maximum queue wait for one `machine_test` call. `0` keeps the caller queued until its fair turn or operator cancellation. Queue waiting consumes no model hops. |
| `ANGEL_MACHINE_QUEUE_LEASE_SECS`    | `90`                      | Heartbeat lease window, clamped to at least 15 seconds. A vanished runner is expired and the machine becomes grantable again. This is failure detection, not the test-duration quantum. |
| `ANGEL_MACHINE_QUEUE_QUANTUM_SECS`  | `1800`                    | Maximum runtime of one queued remote test command. At the boundary its process group is stopped and the owner must queue a new test at the back of the rotation. `0` allows an unbounded command only when the operator explicitly accepts possible monopolization. |
| `ANGEL_SIDECAR_API_KEY_FILE`        | unset                      | Service-side bearer token file for shared model-kit sidecars and the on-demand supervisor. When configured, every route including health requires the token. At the shared-server layer a valid token permits a non-loopback bind, but an individual sidecar may impose an earlier stricter preflight: Forge currently performs that preliminary unauthenticated check and therefore remains loopback-only unless the separate break-glass waiver is also present. The absolute regular file must be owned by the service user, mode `0600`/`0400` or stricter, at most 4 KiB, and contain one RFC 6750 token of at least 32 bytes. The service reopens and validates it per request: atomic replacement rotates live, while a missing/invalid/exposed replacement rejects all credentials without retaining the old token. |
| `ANGEL_HEAD_<ID>_API_KEY_FILE`      | unset                      | Descriptor-client token file for one specialist head (for example `ANGEL_HEAD_DICE_API_KEY_FILE`). The same permission, size, and token rules apply. Registry health, terminal autoresearch, and compatibility head calls attach it to submit/status/result requests without logging it. Clients reload it per request.                                                                                                                       |
| `ANGEL_ALLOW_UNAUTHENTICATED_REMOTE_BIND` | unset               | Break-glass only: exact value `1` permits a shared sidecar/supervisor non-loopback bind without angel0 bearer auth because a separate authenticated perimeter is asserted. The shared-server supported remote-bind profile uses `ANGEL_SIDECAR_API_KEY_FILE`; bearer auth still requires loopback, an encrypted overlay, or TLS for transport confidentiality. Forge's current supported profile remains loopback plus SSH/tunnel because of its stricter preliminary bind check. |
| `ANGEL_LLM_TOOLS`                   | `true`                    | Register `llm_probe` (GET /v1/models on an OpenAI-compatible endpoint) and `llm_bench` (streamed single-stream TTFT + decode tok/s, warmup excluded). Meant for local/tailnet endpoints. `0` disables.                                                                                                                                                                                                                                                                                                         |
| `ANGEL_VISION_URL` / `ANGEL_VISION_MODEL` / `ANGEL_VISION_KEY` | unset | OpenAI-compatible **vision sidecar** for text-only drivers (DeepSeek V4 Flash and friends). Point at any VLM that accepts `image_url` content parts — lightweight picks from community practice: local Qwen2.5-VL / gemma3 / llava via Ollama·vLLM·llama.cpp, cloud Grok vision, or DashScope Qwen-VL. URL is the `/v1` base (e.g. `http://127.0.0.1:11434/v1`). Key optional for local. Resolution: explicit endpoint, then Kimi, then the signed-in Codex route (`CODEX_HOME` / `~/.codex`). Codex keeps its configured model and reasoning effort and requires cached image-input support; it needs no extra API key. Powers `vision_look`, `video_look`, auto `/see` rewrite, and `angel --look-image IMAGE QUESTION` (one image request without an agent loop). |
| `ANGEL_VISION_SIDECAR`              | `1` (on when backend set) | When the in-hand club is text-only and the user attaches images (`/see`), describe them through the vision backend and inject text before the first hop so DeepSeek never receives bare `image_url` (provider 400). `0` disables auto-rewrite (tools still work). `force` / `always` rewrites for every club that has a backend. |
| `ANGEL_VISION_TOOLS`                | `true`                    | Register `vision_look` whenever a vision backend is configured (`ANGEL_VISION_*`, Kimi, or signed-in Codex with image support). Independent of `ANGEL_VIDEO_TOOLS` so DeepSeek sessions get eyes without the full video-edit suite. `0` opts out. |
| `ANGEL_VIDEO_TOOLS`                 | `true`                    | Register video probe/cut/contact-sheet tools plus `video_look` (shares the vision backend). `0` disables the edit suite; `vision_look` can still register via `ANGEL_VISION_TOOLS`. |
| `TYPESAFE_API_KEY` | unset | Server-side Jev bearer credential; keep in ignored private `.angel.env` or inherited runner environment. Never pass as a tool argument. |
| `ANGEL_JEV` | `true` when key present | Register deferred `jev_decide`; `0` disables. No request at startup. Advisory technical probabilities never grant verification or benchmark acceptance. See [JEV.md](JEV.md). |
| `ANGEL_JEV_MODEL` | `jev-latest` | TypeSafe model identifier for typed decisions. |
| `ANGEL_JEV_TIMEOUT_MS` | `5000` | Per-request deadline, clamped to 250–30000 ms and the enclosing tool deadline. No automatic retries. |
| `ANGEL_JEV_MAX_CALLS` | `64` | Attempt budget per tool registry, clamped to 1–256. The Yukon research runner defaults to 8; cache hits do not consume attempts. |
| `ANGEL_EMBED`                       | `true`                    | Register the deferred `semantic_read` tool when the global embedding config or an explicit endpoint is present. The tool ranks bounded chunks from explicitly named, workspace-confined files and returns only relevant passages; `0` is the exact no-service control.                                                                                                                                                                                                                                    |
| `ANGEL_EMBED_CONFIG`                | `$XDG_CONFIG_HOME/angel0/embed.json` or `~/.config/angel0/embed.json` | Global, cross-repository service configuration using schema `angel0.embedding-service/v1` with `url`, `model`, and `api_key_file`. The endpoint must be HTTPS unless it is loopback HTTP. Because this file is outside every repository, multiple angel0 checkouts share the service without sharing project evidence or state.                                                                                                               |
| `ANGEL_EMBED_URL` / `ANGEL_EMBED_MODEL` | unset / `nvidia/Nemotron-3-Embed-1B-BF16` | Explicit environment override for the shared embedding endpoint and model. When set, credentials come from `ANGEL_EMBED_KEY` or `ANGEL_EMBED_KEY_FILE`; non-loopback endpoints fail closed without one.                                                                                                                                                                                                                                      |
| `ANGEL_GROK_TOOL`                   | `true`                    | Register the `grok_research` tool when a Grok account OAuth login is configured; a box without `~/.grok/auth.json` simply doesn't advertise it. `0` disables only the tool registration. |
| `ANGEL_GROK_RESEARCH`               | `1` (on)                  | Enable the Grok ACP research scout / `grok_research` tool (one persistent `grok agent stdio` process). Does **not** gate the ordinary HTTP `grok` OAuth seat — that loads whenever `~/.grok/auth.json` is present so `ANGEL_DRIVER=grok` and SOTA judge/aggregate pins keep working. The ACP child uses the account login; API credentials belong to the separate `grok-api` HTTP route. `0` disables only the ACP research surface. |
| `ANGEL_GROK_RESEARCH_ALWAYS`        | `0` (off)                 | Front-load the Grok research scout on every eligible automatic MoA turn. The default runs it only when live research is requested or a formation persists the choice, so a scout cannot delay unrelated early turns. |
| `ANGEL_GROK_TIMEOUT_SECS`           | `60`                      | Wall-clock deadline for one ACP prompt. Timeout or Esc sends `session/cancel`, invalidates the connection, and leaves the next call to start a clean authenticated process. This is not the lifetime of the persistent process. |
| `ANGEL_GROK_MAX_TURNS`              | `0` (provider-native)     | Optional nested-turn cap for the ACP research agent. A positive value is passed when the persistent process starts; `0` leaves Grok's own research/tool loop uncapped inside the prompt deadline. |
| `ANGEL_GROK_CMD` / `GROK_CMD`       | discovered `grok`         | Grok executable used for the persistent ACP research process. It must support `grok agent stdio` and the `cached_token` ACP authentication method. |
| `ANGEL_GROK_MODEL` / `GROK_MODEL`   | `grok-4.6`                | Model for both the stable `grok` account-OAuth alias and the persistent research agent. Compatibility aliases such as `grok`, `latest`, and `grok-4` resolve locally before reaching xAI. |
| `ANGEL_GROK_REASONING_EFFORT`       | catalog default           | THINK rung for Grok research ACP (`low`, `medium`, `high`, and `xhigh` on Grok 4.6). Changing the rung replaces the cached process only after existing Arc-held in-flight calls finish. |
| `ANGEL_GROK_HARNESS_SURFACE`        | `0` (off)                 | Experimental. Grok 4.6's Build agent refuses `<tool_call>` host-tool markup, so Angel driver hops with tools belong on the HTTP `grok` OAuth seat (native function calling). `1` retries the denied-tools + markup ACP path against an older CLI; default is fail-closed with a pointer at the HTTP seat. |
| `ANGEL_SCIENCE_TOOL`                | `true`                    | Register `science_search` (free, key-less literature fan-out — arXiv/OpenAlex/etc.). `0` disables.                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_REPO_TOOL`                   | `true`                    | Register `repo_search` (free, key-less GitHub repository search → ranked, cited shortlist of the latest/best repos for a topic; also the `/repos [best\|latest] <query>` command). `0` disables.                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_GITHUB_TOKEN`                | *(unset)*                 | Optional GitHub token for `repo_search`. Absent → key-less (10 search req/min); present → authenticated (30/min). `GITHUB_TOKEN`/`GH_TOKEN` are honoured as fallbacks. Read-only; never required.                                                                                                                                                                                                                                                                                                               |
| `ANGEL_WORK_LANDING`                | `true`                    | Inject the per-workspace work-context block (established mode/goals from past sessions) into the system preamble. `0` removes it.                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_WORK_CONTEXT_DIR`            | `~/.angel0/work-context`   | Per-workspace work-context store (`<key>.json`; tests point this at a tempdir).                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_SANDBOX`                     | `1` (on)                  | Landlock write-confinement for agent `shell`/`cargo` commands. The ordinary write posture grants the active workspace; `/tmp`, `/var/tmp`, `/dev/shm`, and `/proc/self/task`; existing `XDG_RUNTIME_DIR`, `XDG_CACHE_HOME`, `~/.cache`, on macOS the per-user `confstr` temp/cache roots (`/var/folders/…/T` and `/C`) even when `TMPDIR` is unset, Cargo registry/Git/bin, npm cache, and `~/.local/{bin,lib}` roots; project-owned process logs; linked-worktree Git metadata; and existing recognized GPU device roots. It does **not** grant all of `$HOME` or the launch cwd. It also blocks privilege escalation via `no_new_privs`. `0` is an explicit operator escape hatch. Read-only/review delegates stay confined regardless. The ordinary write posture still reads the whole filesystem and does not block network access. |
| `ANGEL_SANDBOX_PROFILE` | unset | `sealed` selects the mandatory Linux Bubblewrap profile in headless task mode (also `--sandbox-profile sealed`). Only workspace/Git metadata, OS runtime roots (including compiler alternatives), and resolved toolchain roots are mounted; network and PID namespaces are private. `CARGO_HOME`, `RUSTUP_HOME`, and `NVM_DIR` may name pinned runtime roots. Grants overlapping denied HOME secrets or `/etc/shadow` fail closed; a HOME-wide workspace is refused. YOLO plus sealed is an error. `identity.sandbox` records the profile and plan digest; `authority_profile` distinguishes sealed, ordinary, and YOLO task receipts. Requires Bubblewrap with bind-fd support and a namespace-capable kernel; unsupported hosts are unqualified. |
| `ANGEL_SANDBOX_BACKEND` | `landlock` | Linux-only opt-in `bwrap` uses the trusted `/usr/bin/bwrap` to create a read-only host mount view with only explicit writable roots, a private PID namespace and `/proc`, and a minimal private `/dev` containing the standard pseudo-devices (`null`, `zero`, `full`, `random`, `urandom`, `tty` — the same set a nested `bwrap --dev` rebuilds from) plus approved device/shared-memory roots. It permits nested verifier namespaces while retaining no-new-privileges and dropping capabilities. Denied-network policies retain the socket filter and add a private network namespace. Mandatory read-only tools keep Landlock. Missing Bubblewrap, unsupported backends, and rejected root mounts fail closed; there is no unconfined retry. This does not implicitly grant `/proc` or `/sys` host writes. Programs needing non-approved device paths (pts, host shm, block or GPU devices outside the policy) may fail. Requires Linux `openat2` and Bubblewrap with `--bind-fd`. Run the actual verifier under the selected backend before unattended solving. |
| `ANGEL_SANDBOX_HELPER`             | unset                     | Sandbox helper executable for tool spawns: a path, or `self` to re-exec the cockpit binary; unset prefers a sibling `angel-sandbox` next to the binary, else self. |
| `ANGEL_BWRAP_STRICT`                | unset                     | Test-only gate for the Bubblewrap contract suite. The suite normally skips (with a note) on hosts whose kernel or AppArmor policy denies unprivileged user namespaces; `1` turns that skip into a failure so a release or rig-qualification run cannot report the nested-sandbox contract as green without actually executing it. Never read by the cockpit at runtime. |
| `ANGEL_TOOL_STRIP_SECRETS`          | `1` (on)                  | Remove secret-named environment variables (`KEY`, `TOKEN`, `SECRET`, `AUTH`, or `PASSWORD`) from autonomous command children and from MCP server children (`mcp.rs`, every spawn and crash-respawn). On by default since 2026-09-07 so a child that prints its environment cannot put a live key into the trajectory log; set `0` to pass credentials through. YOLO bypasses the strip for tool children only: an MCP server is a long-lived daemon, not the operator's shell, so its environment never depends on the session posture — a server's own key goes in its `mcp.json` `env` entry, which is applied after the strip and always reaches it. Trajectory records are additionally redacted at write time (known values → `«redacted:NAME»`, key shapes → `«redacted»`).                                                                                                                                                                                                                                                                                          |
| `ANGEL_PROJECT_DOC`                 | `true`                    | Auto-inject scoped `AGENTS.md`/`AGENTS.override.md` files from git root to cwd. Each section names its source and later/deeper scopes take precedence. `0` disables.                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_PROJECT_DOC_MAX_BYTES`       | 32 KiB                    | Exact aggregate project-instruction cap. Every scope receives a fair head/tail share; unused bytes flow toward the deepest scope, so a large root file cannot erase nested overrides. `0` disables injection.                                                                                                                                                                                                                                                                                                  |
| `ANGEL_CADDY` | `true` | The **Caddy** recipes/hazards card (`caddy.rs`): renders `~/.angel0/caddy/<repo_key>/{recipes,hazards}.jsonl` into a bounded `[caddy · …]` block — commands that produced verified results here (with env prefixes and duration) and commands that failed with their diagnostic — injected right after the Dossier block in the startup prompt and the headless `--task` warm-start. Each `--task` receipt folds its tool history back into the store. `0` disables both render and write-back. Board arming mirrors the turn classifier: `ANGEL_COMPETITION_MODE`/`ANGEL_GPU_COMP_LOCAL_MOA`, else an Active `/goal` with competition vocabulary. |
| `ANGEL_CADDY_CARD_BYTES`            | `1536` (min 256)          | Byte cap on the rendered caddy card. When over cap, lines drop from the bottom — doors first, then oldest hazards, then oldest recipes; lines are never split.                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_CADDY_DIR`                   | `~/.angel0/caddy`          | Caddy store directory override (mirrors `ANGEL_DOSSIER_DIR`; tests point this at a tempdir).                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_RUNTIME_SHIMS`               | `true`                    | Per-process runtime shims appended to the **end** of every child command PATH (`workspace_lang.rs`): `~/.angel0/shims/python → python3` (and `pip → pip3`) when the host has the `3` variant but not the bare name. Real programs always win. The caddy header prints `host: python3 (`python` → python3 shim) · node · …` so the model sees what resolves. Measured 2026-09-07: every python arena task lost its first shell hop to `python -m unittest` on a python3-only host. `0` disables the shims (the exit-127 hint still names the missing program). |
| `ANGEL_SELF_MODEL`                  | `true`                    | Inject the generated **self-model** (a map of the cockpit's own source: modules, purposes, build/test) into the preamble and register the `self_map` tool. `0` disables the injection.                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_SELF_SRC`                    | auto-detect               | Override the cockpit source root. In the interactive cockpit, unset uses the compiled-in root or a cwd walk. In headless tasks, an explicit nonblank value opts into a read-only `self_map` tool: the root is canonicalized, invalid pins fail without fallback, and `write=true` is denied. No source-map context is injected automatically into headless tasks. |
| `ANGEL_SELF_WORKTREE_DIR`           | `~/.angel0/self-worktrees` | Where `/self` creates the isolated git worktrees a self-modification run edits (tests point it at a scratch dir).                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SKILLS_DIR`                  | `~/.angel0/skills`         | User skills directory. Learned/project skills must carry matching canonical repo root and key; legacy unbound project habits are inert.                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_BUNDLED_SKILLS_DIR`          | `<crate>/skills`          | Optional filesystem overlay for the skill library. Public-safe shipped playbooks are embedded in the binary so relocated installs retain them; operator-specific playbooks remain filesystem-only. Matching filesystem/user/project names override an embedded body and can expose supporting resource paths.                                                                                                                                                                                                   |
| `ANGEL_NOTES_FILE`                  | `~/.angel0/notes/<key>.json` | Optional notes-store override. Notes records carry canonical project root and key; foreign or legacy content is unreadable and cannot be overwritten or cleared by another project.                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_MARKDOWN_SYNTAX`             | on                        | Syntect syntax highlighting for fenced markdown code blocks. `0`/`off`/`false` restores the dim monochrome code style.                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_BACKDROP`                    | `in_process`              | How visual side panels (agent bay + Scryglass) are hosted. `in_process` (default) paints them in the main TUI. `off` / `text` skips backdrop paint and expands the transcript to the full cockpit body (lean interactive path). `lazy` is reserved for an external backdrop process and currently behaves like in-process. Headless/`--task` should prefer `off`. |
| `ANGEL_COMP_MODE` / `ANGEL_LEAN_MODE` | `0` (off)               | Ultra-lean high-speed harness posture (also `angel --comp` or `/comp on` / `/lean on`). Tables the miniworld, Kitty graphics, and terrain raycasting to eliminate background simulation and maximize frame throughput (~sub-millisecond frame times). Visual tools, image viewers, and artifacts remain fully discoverable and callable on demand. |
| `ANGEL_SURFACE_GAPS`                | `0` (shared borders)      | `1` / `on` / `gap` separates transcript and side column with a one-cell gap instead of overlapping borders. Isolates clipboard selection so reverse-video and native terminal selection cannot straddle chat and miniviz. Shared borders remain the dense default. |
| -------------------------------------------------- | --------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |

## MCP & lifecycle hooks

| Variable | Default | Purpose |
| -------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_MCP_CONFIG`   | `~/.angel0/mcp.json`   | User-global trusted config for external stdio MCP servers. Each connection is spawned in the active project cwd; warm clients are never reused across project identities. Secret-named variables the cockpit holds are withheld from every server (`ANGEL_TOOL_STRIP_SECRETS`); a server's own key must be set in its `env` entry. |
| `ANGEL_MCP_TIMEOUT`  | `15` (s)              | Per-call timeout for MCP tools.                                                                                                                                           |
| `ANGEL_HOOKS_CONFIG` | `~/.angel0/hooks.json` | PreToolUse (block-capable) / PostToolUse lifecycle hooks.                                                                                                                 |
| `ANGEL_HOOK_TIMEOUT` | `10` (s)              | Per-hook subprocess timeout.                                                                                                                                              |
| `ANGEL_PRE_HOOK_FAIL_OPEN` | off             | Explicitly allow a configured PreToolUse hook timeout/spawn failure to pass. The safe default denies when policy infrastructure is unavailable.                           |
| `ANGEL_GIT_TIMEOUT`  | `120` (s)             | Deadline for trusted harness git operations, including `/diff`, `/review`, delegate, and integrate. `0` disables the deadline.                                            |

Hooks are invoked with these set in their environment (read by the hook, not by
the cockpit): `ANGEL_TOOL_NAME`, `ANGEL_TOOL_ARGS`, `ANGEL_TOOL_RESULT` (PostToolUse only), `ANGEL_TOOL_TIMEOUT`.
Argument/result values above 32 KiB are replaced by a bounded JSON receipt with
their byte length and SHA-256. Any configured hook makes its emitted tool batch
a serial barrier because hook side effects are outside filesystem footprints.

## HTTP / resilience (`club.rs`)

| Variable | Default | Purpose |
| ----------------------------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_HTTP_TIMEOUT`                | `60` (s)                   | Socket read timeout. It bounds the whole response for buffered calls and one blocking read for SSE calls. The bounded default prevents a silent provider socket from parking the foreground turn; `ANGEL_STREAM_TOOL_SILENCE_SECS` defines the narrow local-tool exception below.                                                                                                                                 |
| `ANGEL_STREAM_STALL_SECS`           | `45` (s)                   | Streaming data deadline: a live SSE stream must deliver a real chunk this often. Tool HTTP requests also use this as their body idle bound, including in YOLO; each received body chunk resets it. The turn deadline remains an independent absolute bound. Tool HTTP supervision polls every 25 ms (up to two ticks for a forwarded cancel signal), then kills and reaps its request helper. Keep-alive comments reset the plain read timeout but not this one, so a server that queues forever while pinging fails loudly instead of hanging the turn. Blank frames are not progress: an empty `choices` array, a keep-alive comment, or a tool-call envelope carrying no id, name, or arguments never resets the deadline, and none of them counts as completion or token progress. A stall that arrives after partial prose is classified exactly like any other missing terminal event, so the hop may be replayed with the partial retracted. Detection happens at the next completed read, so a socket that stops sending even keep-alives is reported once the read deadline (`ANGEL_HTTP_TIMEOUT`) has also elapsed. `0` disables. |
| `ANGEL_STREAM_HARD_SECS`            | `900` (s)                  | Wall-clock deadline for one streaming provider attempt, even when real SSE deltas keep arriving. This bounds providers that dribble output indefinitely and therefore evade the idle/data watchdog. `0` disables; the sealed Yukon GLM runner pins `300`.                                                                                                                                             |
| `ANGEL_STREAM_TOOL_SILENCE_SECS`    | `900` (s)                  | Bounded recovery window for an already-generating private/LAN Qwen stream that was offered tools. Some Qwen/SGLang tool parsers buffer a large string argument until its closing delimiter, producing no SSE bytes long enough to cross `ANGEL_HTTP_TIMEOUT`; angel0 retries timed-out reads in place and coalesces zero-payload watchdog heartbeats whether the server is silent or sending SSE comments. Cold streams, public providers, non-Qwen models, and tool-free calls keep the ordinary fail-fast behavior. `0` disables this exception. |
| `ANGEL_REASONING_MARKERS`           | `1` (on)                   | Parse common `<think>` / `<response>` markers from OpenAI-compatible content streams so private reasoning stays in the reasoning pane and the public answer remains clean. Parsing is incremental and UTF-8 safe across chunk boundaries. `0`/`false`/`no`/`off` leaves the provider text untouched.                                                                                                  |
| `ANGEL_REASONING_ONLY_RETRY`         | `1` (on)                   | Allow one direct-answer recovery request when a provider ends after reasoning without a usable public answer. `0` disables the recovery and surfaces the original failure immediately.                                                                                                                                                                                                               |
| `ANGEL_STREAM_MAX_LINE_BYTES` | `16777216` (16 MiB) | Hard cap on a single SSE line before the stream is failed. Guards against a server that never emits a newline turning one response into unbounded memory growth. |
| `ANGEL_STREAM_RULES` | unset (`~/.angel0/stream_rules.json`) | Path to a Time-Traveling Stream Rules (TTSR) JSON file: `[{"pattern": "<regex>", "reminder": "<text>"}]`. A rule sits dormant until the streamed output matches its regex, then the club aborts mid-generation, injects the reminder as a system message, and retries — course-correcting without prepending every rule to every turn. Opt-in: with none configured the hot path is a single `is_empty()` check. |
| `ANGEL_STREAM_RULE_RETRIES` | `2` | Maximum TTSR course-corrections per request. Once spent, the stream is allowed to finish uninterrupted so a pathological rule cannot loop a turn forever. |
| `ANGEL_STREAM_USAGE` | capability-gated | Send `stream_options: {"include_usage": true}` on streaming chat requests so OpenAI-compatible backends attach a usage frame (incl. cached-token counts) to the final SSE chunk. Some providers (DeepSeek, OpenRouter) volunteer usage unasked; OpenAI and most gateways stay silent without this, leaving the `/usage` cache meter at `n/a`. Default follows detected cache capability: a backend with a known prefix cache is asked, unknown/strict servers keep a byte-identical body. An explicit value overrides detection either way. |
| `ANGEL_HTTP_CONNECT_TIMEOUT`        | `10` (s)                   | Connect timeout.                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_QUICK_LOOKUP`                | `1` (on)                   | When idle, selecting exactly one word in the transcript may fetch a bounded Wikipedia science/math definition into a sourced Scryglass world lesson. `0` keeps ordinary selection/copy but disables the network lookup.                                                                                                                                           |
| `ANGEL_HTTP_WRITE_TIMEOUT`          | `60` (s)                   | Write timeout.                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_HTTP_RETRIES`                | `1`                        | Extra attempts after a transient failure. `0` = no retry. One bounded retry avoids multiplying a dead provider into a many-minute apparent hang.                                                                                                                                                                                                                                                                  |
| `ANGEL_HTTP_BACKOFF_MS`             | `500`                      | Base backoff between retries.                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_HTTP_RATELIMIT_RETRIES`      | `5`                        | Extra attempts for transient HTTP 429 request-window pressure. This counter is independent of transport/5xx retries; weekly/monthly quota exhaustion still fails immediately. |
| `ANGEL_HTTP_RATELIMIT_BACKOFF_MS`   | `1000`                     | Base backoff for the transient-429 retry ladder. |
| `ANGEL_HTTP_RATELIMIT_BACKOFF_CAP`  | `30` (s)                   | Upper bound for a transient-429 backoff or honored `Retry-After`. |
| `ANGEL_HTTP_JITTER`                 | `0.5`                      | Decorrelated-jitter fraction `[0,1]` on backoff (anti-thundering-herd).                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_HTTP_IDLE_CONNECTIONS`       | local `96`, WAN `16`       | Reusable sockets retained per model host. The local default preserves an 80-agent wave across swarm stages without reconnect churn; values are capped at `512`.                                                                                                                                                                                                                                      |
| `ANGEL_OPENROUTER_REASONING_EFFORT` | provider default           | OpenRouter-specific reasoning control (`none`, `minimal`, `low`, etc.). The provider URL supplies the env namespace even when the visible club label is a model id such as `tencent/hy3:free`; use `none` for a terse cheap verifier so reasoning tokens cannot consume the whole completion cap.                                                                                                    |
| `ANGEL_RATELIMIT_PROACTIVE`         | on                         | Sleep out a `0`-remaining rate-limit window before firing. `0` disables.                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_RATELIMIT_MAX_WAIT`          | `30` (s)                   | Cap on a proactive rate-limit wait.                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_TRUNCATION_RETRY`            | `1` (on)                   | Master switch for output-cap recovery. Only unusable `finish_reason=length` replies (half-written tool calls, empty bodies, fail-closed prose) whose request carried `max_tokens` are retried. Usable truncated prose is never replayed. `0` disables.                                                                                                                                               |
| `ANGEL_TRUNCATION_RETRIES`          | `4` (max `4`)              | Extra attempts for unusable truncated output. Each asks for 2×/4×/8×/16× the original output room; capped duplicate requests are omitted. Streaming retries continue only while no visible content has been emitted. `0` restores single-shot behavior; `1` is the legacy one-retry control.                                                                                                         |
| `ANGEL_TRUNCATION_RETRY_MAX`        | `65536`                    | Ceiling on progressive `max_tokens` asks; a sent cap already at/above it is not retried, and reaching it ends the ladder rather than resending identical bytes.                                                                                                                                                                                                                                      |
| `ANGEL_CLUB_MAX_TOKENS`             | unset (provider-native)    | Explicit global `max_tokens` override for HTTP clubs. When unset, angel0 omits the field and the provider/model's native output limit governs; this is not unlimited. Per-club controls still take precedence. Headless `--task-json` results report the effective policy, token count, source, and provenance in `output_budget`.                                                                           |
| `ANGEL_<CLUB>_MAX_TOKENS`           | unset                      | Per-club `max_tokens` override (club name uppercased, dashes→underscores — e.g. `ANGEL_OPENROUTER_MAX_TOKENS` or `ANGEL_LONGCAT_MAX_TOKENS`); beats `ANGEL_CLUB_MAX_TOKENS`. LongCat has no product-supplied cap: setting its exact variable is an explicit operator choice.                                                                                                                                                                                        |
| `ANGEL_<CLUB>_REASONING_EFFORT`     | unset                      | Per-club `reasoning_effort` request hint (club name uppercased, dashes→underscores — e.g. `ANGEL_DEEPSEEK_V4_PRO_REASONING_EFFORT`); beats the global `ANGEL_REASONING_EFFORT`. Sent only when the backend is detected reasoning-capable; the THINK deck's in-memory override and a MoA seat's per-call effort both outrank it. More examples: `ANGEL_GLM_REASONING_EFFORT`, `ANGEL_ATLAS_REASONING_EFFORT`, `ANGEL_DEEPSEEK_FLASH_REASONING_EFFORT`. |
| `ANGEL_<CLUB>_REASONING_DIALECT`    | auto                       | Wire spelling for the club's reasoning control: `openai` (top-level `reasoning_effort`), `glm` (Z.ai `thinking: {type}` block), or `qwen` (`chat_template_kwargs.enable_thinking`), e.g. `ANGEL_GLM_REASONING_DIALECT`, `ANGEL_ATLAS_REASONING_DIALECT`, `ANGEL_DEEPSEEK_REASONING_DIALECT`, or `ANGEL_DEEPSEEK_V4_PRO_REASONING_DIALECT`. Unset auto-detects from base URL/model (z.ai/bigmodel/glm* → glm; qwen3* → qwen; OpenRouter always openai). A resolved glm/qwen dialect also marks the club reasoning-capable, bypassing the name-substring guess, and offers the binary `none/high` THINK ladder. |
| `ANGEL_<CLUB>_PROMPT_CACHE`         | unset                      | Per-club prompt-cache pin, checked before the global `ANGEL_PROMPT_CACHE` gate: `1` sends the stable per-instance club `prompt_cache_key` (see `ANGEL_PROMPT_CACHE`) even when the backend never proved cache support (aggregators like OpenRouter don't appear in the static model map, so detection alone leaves their cache cold); `0` never sends it for this club. Unset keeps the global capability-gated behavior byte-identical. |
| `ANGEL_<CLUB>_CACHE_STABLE`         | unset                      | Per-club cache-first pin, checked before the global `ANGEL_CACHE_STABLE` gate (same club-name spelling as the prompt-cache pin, so an OpenRouter-hosted model uses `ANGEL_OPENROUTER_CACHE_STABLE`): `1` defers mid-turn tool-result aging, argument shrinking, and inspection dedup for this club even when the global is off, `0` keeps the historical per-hop rewrites for it even when the global is on. Unset follows the global. |
| `ANGEL_<CLUB>_STREAM_USAGE`         | unset                      | Per-club pin for `stream_options.include_usage`, checked before the global `ANGEL_STREAM_USAGE` gate (same club-name spelling as the other pins): `1` requests the streaming usage frame for this club even when the global is off, `0` withholds it even when the global is on. Unset follows the global. |
| `ANGEL_METADATA_TTL_SECS`           | `120`                      | How long a probed model metadata snapshot (context window, capability flags) is trusted before re-probing — picks up a server relaunched with a different `--ctx-size` without a cockpit restart. `0` = probe once, never re-probe.                                                                                                                                                                  |
| `ANGEL_QUOTA_COOLDOWN_SECS`         | `3600`                     | Bench time after a quota-exhausted 429 (weekly/monthly plan cap or a spent balance). While benched, the club fails calls instantly (no network), reports unavailable, and the SOTA-MoA reroutes its role calls to the next spendable link; after the cooldown the next call re-probes the provider. `/model` status shows benched links as `label(quota Nm)`.                                        |
| `ANGEL_FALLBACK`                    | off                        | Enable eager failover across the bag (`FallbackClub`) when a club fails. By default this excludes token-metered SOTA clubs such as `openai`/`codex`/`longcat`/`grok`.                                                                                                                                                                                                                                |
| `ANGEL_FALLBACK_INCLUDE_SOTA`       | off                        | Allow `ANGEL_FALLBACK` to fail over into token-metered SOTA clubs.                                                                                                                                                                                                                                                                                                                                   |

The effective route policy is shown in Brain Route and the focused Agent
inspector as `output: provider-native`, `output: N · ENV_NAME`, or
`output: endpoint-managed`. Provider-native means angel0 omits `max_tokens`; it
does not mean the model is unlimited. Explicit request-cap, native-limit, and
Responses endpoint-managed incompletions use distinct transcript markers.
Truncation telemetry counts episodes, retained partials, retry attempts,
recoveries, and terminal failures separately. Visible streamed prose is never
replayed, and partial tool calls always fail closed.

## Fleet / clubs

See [Model setup](../../docs/MODELS.md) for onboarding.

Fleet HTTP fallbacks are loopback-only. Set a club URL explicitly for a remote
endpoint, or opt into Tailscale resolution with `ANGEL_TAILNET_RESOLVE=1`.
Scanning and Hydra registry traffic are separate opt-ins.

| Variable                                                                                                        | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_LOCAL_URL` / `ANGEL_LOCAL_MODEL` | Generic local endpoint. Default URL `http://127.0.0.1:8080/v1`; an unset model follows `/models`. Provides local chat and swarm modes. |
| `ANGEL_MODEL_SERVING_EXCLUDE_HOSTS` | Optional comma-separated exact hostnames or IPs excluded from model discovery; case-insensitive, no wildcard. Default empty. |
| `ANGEL_OPENAI_API_URL` / `OPENAI_BASE_URL` / `ANGEL_OPENAI_API_MODEL` / `OPENAI_MODEL` / `ANGEL_OPENAI_KEY` / `OPENAI_API_KEY` | Separate `openai-api` Chat Completions route. Requires credentials and an explicit model. URL defaults to `https://api.openai.com/v1`. Independent of ChatGPT OAuth; optional API allowlist uses family `openai`. |
| `ANGEL_GEMMA_URL` / `ANGEL_GEMMA_MODEL` | Optional named `gemma` and `swarm` endpoint. Only registered when the URL is set. An unset model follows the endpoint's `/models` response. |
| `ANGEL_SPARK_URL` / `ANGEL_SPARK_MODEL` / `ANGEL_SPARK_R1_URL` / `ANGEL_LEANSTRAL_URL` / `ANGEL_LEANSTRAL_MODEL` / `ANGEL_DSFLASH_URL` / `ANGEL_DSFLASH_MODEL` / `ANGEL_QWEN38_URL` / `ANGEL_QWEN38_MODEL` | Compatibility names for explicitly configured model endpoints. Only URLs supplied by the user create these routes; no hardware hostname or port is assumed. Optional model pins override live `/models` discovery. |
| `ANGEL_SPARK_HOST` | Comma-separated, case-insensitive list of extra host labels that fold into the cockpit's `spark` box (typically the Spark peer's tailnet name and its Hydra host id). The source ships no hardware hostnames: unset, only the literal `spark` label resolves that box and every other label is looked up verbatim. Operators set it in the repo-local `.angel.env` (gitignored; sourced by `bin/angel0`), never in source. |
| `ANGEL_ORNITH_URL` / `ANGEL_ORNITH_MODEL` | Optional named endpoint grouped under `toymaker` for compatibility. Registered only when the URL is configured; no machine address or port is assumed. |
| `ANGEL_TURBO_URL` / `ANGEL_TURBO_MODEL` | Optional named `turbo` endpoint; registered only when its URL is configured. An unset model follows live `/models` discovery. |
| `ANGEL_ATLAS_URL` / `ANGEL_ATLAS_MODEL` | Explicit endpoint/model for the optional legacy `atlas` model route. Requires `ANGEL_ATLAS_MODEL_SERVING=1`. Separate from Atlas project memory. |
| `ANGEL_ATLAS_MODEL_SERVING` | Set `1` with `ANGEL_ATLAS_URL` to register the optional legacy model route. General fleet discovery treats Atlas-named hosts like any other host. |
| `ANGEL_SCAN`                                                                                                    | Fleet discovery port sweep (**off** by default; opt-in): probe every online tailnet peer for serving endpoints. Set `1` so a model pulled up on turbo/spark becomes a selectable tab. `0` disables discovery entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_SCAN_INTERVAL_SECS`                                                                                      | Seconds between discovery sweeps (default `45`, so a checkpoint pulled up on any rig becomes selectable within a minute). `0` = sweep once at launch.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_SCAN_PORTS`                                                                                              | Comma-separated ports probed on each online peer during discovery (default: the fleet's known serving ports).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_SCAN_CONCURRENCY`                                                                                        | Maximum concurrent discovery probes (default `32`, cap `256`). This bounds control-plane threads only; it does not limit model/swarm concurrency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_HYDRA_URL`                                                                                               | Local Hydra head address for surface registry reads/writes (default `http://127.0.0.1:17876`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_HYDRA_DISCOVER`                                                                                          | Read Hydra-registered chat surfaces during discovery — covers localhost-bound heads the port sweep can't see (**off** by default; requires `ANGEL_SCAN=1`). `1` enables registry reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_HYDRA_PUBLISH`                                                                                           | Publish discovered surfaces to Hydra each sweep so other rigs/agents can route to them (**off** by default). `1` enables registration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_OPENAI_MODEL` | Initial model for the ChatGPT account route `openai`. Otherwise reads the Codex model from `~/.codex/config.toml`, honoring `CODEX_HOME`. Model choices and reasoning levels come from the local catalog. The separate `openai-api` route uses API credentials and its own model setting. |
| `ANGEL_OPENAI_REASONING_EFFORT`                                                                                 | Optional initial reasoning effort for the OpenAI/Codex agent; wins over `ANGEL_REASONING_EFFORT`, then falls back to `model_reasoning_effort` in `~/.codex/config.toml`. Unsupported values are replaced by the selected model's declared default. The panel's THINK button changes only the selected model's in-memory setting and never mutates Codex config on disk.                                                                                                                                                                                                                                                                                                          |
| `ANGEL_OPENAI_AUTH_JSON`                                                                                       | Process-only ChatGPT OAuth snapshot for sealed evaluator launches. When present it is used in memory instead of reading `CODEX_HOME`; malformed input fails closed. The benchmark sandbox strips this secret-named variable from every autonomous command child. Ordinary interactive use should continue to rely on the Codex CLI login file.                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_CODEX_STREAM_STALL_SECS`                                                                                | `120`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | ChatGPT-OAuth/Responses route: give up on a stream that sends no events for this many seconds (the chat route has `ANGEL_STREAM_STALL_SECS`); `0` keeps the plain 300 s socket read timeout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_OPENROUTER_URL` / `ANGEL_OPENROUTER_MODEL` / `ANGEL_OPENROUTER_KEY`                                      | Optional OpenRouter OpenAI-compatible API-token link for the `sota` box and `sota-moa` preset. URL defaults to `https://openrouter.ai/api/v1`. The `openrouter` seat defaults to `stealth/ox-alpha`; additional free catalog seats (Thinking Machines Inkling / Inkling Small, `nvidia/nemotron-3-ultra-550b-a55b:free`, `openrouter/free`, Laguna S 2.1, North Mini Code, GLM 5.2:free, Nemotron 3.5 Lightning) appear beside it when a key is set. Retired `tencent/hy3:free` pins migrate to Ox Alpha; an explicit paid `tencent/hy3` pin is unchanged. The key also falls back to `OPENROUTER_API_KEY`. A configured OpenRouter catalog is **not** auto-elected as `ANGEL_DRIVER`.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_KIMI_URL` / `ANGEL_KIMI_MODEL` / `ANGEL_KIMI_KEY`                                                        | Optional Moonshot OpenAI-compatible API-token link for the `sota` box and `sota-moa` preset. URL defaults to `https://api.moonshot.ai/v1`, model defaults to `kimi-k3`, and the key also falls back to `KIMI_API_KEY` or `MOONSHOT_API_KEY`.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_DEEPSEEK_URL` / `ANGEL_DEEPSEEK_MODEL` / `ANGEL_DEEPSEEK_KEY`                                            | Optional DeepSeek API-token link for the `sota` box and `sota-moa` preset. URL defaults to `https://api.deepseek.com/v1`, model defaults to the provider-published Pro id `deepseek-v4-pro`; `deepseek-flash` (V4.1 Flash — native `image_url` input) is also selectable. Both have 1M context and expose the provider's real `low` / `high` / `max` effort rungs, thinking on by default at `high`; the THINK deck's `none` rung sends the provider's `thinking: {type: "disabled"}` toggle rather than an invalid `reasoning_effort`. Retired ids (`deepseek-chat`, `deepseek-reasoner`, `deepseek-v4-flash`, `deepseek-v4-flash-vision-exp`) are accepted locally and migrated to `deepseek-flash` before a request is built; explicit unknown ids remain untouched. Key also falls back to `DEEPSEEK_API_KEY`. Official catalog: <https://api-docs.deepseek.com/api/list-models>. |
| `ANGEL_QWEN_URL` / `ANGEL_QWEN_MODEL` / `ANGEL_QWEN_KEY`                                                        | Optional Alibaba **Qwen Coding Plan** link for the `sota` box and `sota-moa` preset. URL defaults to `https://coding-intl.dashscope.aliyuncs.com/v1`, model defaults to `qwen3.7-plus`, key also falls back to `QWEN_API_KEY` / `DASHSCOPE_API_KEY`. Plan keys look like `sk-sp-…`; general DashScope keys (`sk-ws-…`) are rejected by this endpoint. The route also fronts `kimi-k2.5`, `glm-5`, `MiniMax-M2.5`, `qwen3-coder-next` and friends — pin one with `ANGEL_QWEN_MODEL`.                                                          |
| `ANGEL_DEEPSEEK_FLASH_MODEL`                                                                                    | Model for the second DeepSeek seat (`deepseek-flash`), which shares `ANGEL_DEEPSEEK_URL`/`ANGEL_DEEPSEEK_KEY` with the pro seat. Defaults to `deepseek-flash` (V4.1 Flash, the native multimodal id); also falls back to `DEEPSEEK_FLASH_MODEL`. The legacy `deepseek-chat` / `deepseek-reasoner` ids were retired 2026-07-24, and the superseded `deepseek-v4-flash` / `deepseek-v4-flash-vision-exp` ids now alias `deepseek-flash`. Distinct from Spark-local `dsflash` (see `ANGEL_DSFLASH_*` above).                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_CEREBRAS_URL` / `ANGEL_CEREBRAS_MODEL` / `ANGEL_CEREBRAS_KEY`                                            | Optional Cerebras OpenAI-compatible API-token link for the `sota` box and `sota-moa` preset. URL defaults to `https://api.cerebras.ai/v1`; model must be set with `ANGEL_CEREBRAS_MODEL` or `CEREBRAS_MODEL`; key also falls back to `CEREBRAS_API_KEY`.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_META_URL` / `ANGEL_META_MODEL` / `ANGEL_META_KEY`                                                       | Optional Meta Model API link (Muse Spark) for the `sota` box and `sota-moa` preset. URL defaults to `https://api.meta.ai/v1` (OpenAI format, Angel appends `/chat/completions`); model defaults to `muse-spark-1.3` (`muse-spark-1.3-contributor` is the cheaper data-sharing tier and must be an explicit choice); key also falls back to `META_API_KEY`. Metered: excluded from the cheap MoA profile. |
| `ANGEL_LONGCAT_URL` / `ANGEL_LONGCAT_MODEL` / `ANGEL_LONGCAT_KEY`                                               | Optional LongCat OpenAI-format API-token link for the `sota` box and `sota-moa` preset. URL defaults to `https://api.longcat.chat/openai/v1` (Angel appends `/chat/completions`, yielding `/openai/v1/chat/completions`); model defaults to `LongCat-2.0`; key also falls back to `LONGCAT_API_KEY`.                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_GLM_URL` / `ANGEL_GLM_MODEL` / `ANGEL_GLM_KEY`                                                           | Optional GLM (Z.ai / BigModel) API-token link for the `sota` box and `sota-moa` preset. Key also falls back to `GLM_API_KEY` / `ZHIPU_API_KEY` / `BIGMODEL_API_KEY` / `ZAI_API_KEY`; with only `ZAI_API_KEY` set the URL defaults to the z.ai coding endpoint (`https://api.z.ai/api/coding/paas/v4`), otherwise `https://open.bigmodel.cn/api/paas/v4`. The configurable `glm` route defaults to `glm-5.3`; `glm-5.3-flash` and `glm-5.2` remain available as separate selectable routes, and an explicit pin (including the local `glm-flash` nickname) is de-duplicated from its catalog sibling.                                                                                                                                                                                                                                                    |
| `ANGEL_GROK_OAUTH_FILE` / `ANGEL_GROK_HOME` / `ANGEL_GROK_MODEL` / `ANGEL_GROK_MODELS_CACHE` | **First-class Grok OAuth.** When `~/.grok/auth.json` exists (from `grok login --oauth`), angel0 loads the SOTA `grok` seat as normal HTTP to `https://api.x.ai/v1` with the OAuth access token and **silent refresh** (same idea as ChatGPT/Codex OAuth). Default model is xAI's official stable id `grok-4.6` (500k context; `low` / `medium` / `high` / `xhigh`). The model list always includes `grok-4.6` and adds other supported Grok models found in the local CLI model cache, sharing one OAuth authority without duplicate rows. Bare `grok` is an angel0-only compatibility alias that resolves to `grok-4.6` and is never sent upstream; `grok-4` / `grok-4-latest` prefer the newest installed Grok 4 model and fall back to 4.6. Official model page: <https://docs.x.ai/developers/models/grok-4.6>. |
| `ANGEL_GROK_CMD` | Optional path to the `grok` binary for the **research scout / `grok_research` tool only** (nested web tools). Not required for the SOTA judge/aggregate HTTP seat. |
| `ANGEL_GROK_REASONING_EFFORT`                                                                                  | Optional default THINK level for the Grok CLI research surface (`low` / `medium` / `high` / `xhigh` on Grok 4.6; model-scoped). Wins over `ANGEL_REASONING_EFFORT`. |
| `ANGEL_GROK_API_URL` / `ANGEL_GROK_URL` / `ANGEL_GROK_BASE_URL` / `GROK_API_URL` / `XAI_BASE_URL` / `ANGEL_GROK_API_MODEL` / `GROK_API_MODEL` / `ANGEL_GROK_KEY` / `ANGEL_XAI_KEY` / `XAI_API_KEY` / `GROK_API_KEY` | Separate `grok-api` HTTP route. Credential required; default URL `https://api.x.ai/v1`, default model `grok-4.6`. API-specific model settings do not change the OAuth model. Optional `ANGEL_API_CLUBS` restriction uses family `grok`. The account route and its CLI child keep OAuth authentication. |
| `ANGEL_ALLOW_SOTA_DELEGATE`                                                                                     | Include token-metered SOTA clubs (`openai`/`codex`/`kimi`/`glm`/`grok`/`longcat`/…) in `delegate` / `spawn` rosters. **On by default** — SOTA seats are full agents. Set `0` only for an explicit local-only fleet (no paid sub-agent spend from a local driver).                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_BRAIN_KEY`                                                                                               | Optional API key for gated endpoints (most local clubs need none).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_PORT`                                                                                                    | Bridge/server port.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_PROBE`                                                                                                   | Startup readiness probe: when set, `Bag::standard` contacts every endpoint once at launch to pick the in-hand club (off by default so the TUI opens instantly). Independent of the one-shot probe of an explicit `ANGEL_DRIVER` preferred slot (always performed so headless pins like `dsflash` are not silently replaced by a configured SOTA seat).                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_BAG_PROBE`                                                                                               | Background availability prober (**on** by default): re-probes every club off the UI thread so offline ones drop out of the tab strip and `Tab` skips them, and the in-hand club drifts to a reachable one when its endpoint dies. `0`/`off`/`false`/`no` freezes availability at the startup seed.                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_BAG_PROBE_SECS`                                                                                          | Seconds between background availability sweeps (default `5`, min `1`). Each sweep probes all clubs concurrently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_TAILNET_RESOLVE`                                                                                         | Resolve club endpoints from Tailscale status instead of static fallback URLs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `ANGEL_GROK_HARNESS_SURFACE` / `ANGEL_GROK_MAX_TURNS`                                                                                                          | Host-tool driver hops use the HTTP `grok` OAuth seat. `ANGEL_GROK_HARNESS_SURFACE` defaults off; `1` is an experimental ACP markup path that live Grok 4.6 refuses. Research ACP calls have no cockpit-authored turn cap by default (`ANGEL_GROK_MAX_TURNS=0`); a positive value passes an explicit CLI `--max-turns`. The separate wall-clock timeout still applies.                                                                                                                                                                                    |
| `ANGEL_ALLOW_SOTA_CONSULT` | Remote consultation/review is enabled by default for configured models. Set `0` to restrict it; `/solo` also restricts remote consultation. |
| `ANGEL_GROK_USE_API` | Retired mode switch. Configure an API credential and select `grok-api`; account OAuth remains the separate `grok` route. |
| `ANGEL_SOLO`                                                                                                                                                  | Mirror for the `/solo` latch. `1` restricts outsourcing to the in-hand agent and blocks paid SOTA consults; `0` disables that restriction.                                                                                                                                                                                                                                                                                                                                                                                                                         |

## Driver & swarm

| Variable | Default | Purpose |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_DRIVER`                                                                                                                                                                                                                                                                                                                                    | `openai` → `longcat` → auto | Cockpit driver: `swarm` (MoA fan-out), `deli` (single-session deep-research), or a club `<label>`. Unset prefers the OpenAI ChatGPT-OAuth link (when a token is on disk), then a configured LongCat link, then the smartest reachable fleet model. A configured OpenRouter catalog is selectable (`ANGEL_DRIVER=openrouter`) and is not auto-elected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_SWARM_WIDTH`                                                                                                                                                                                                                                                                                                                               | `4` (2–36)                                 | Wave-0 proposer width. With `ANGEL_SWARM_MAX_WAVES=1`, this is the legacy fixed proposer width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_MAX_WIDTH`                                                                                                                                                                                                                                                                                                                           | `32` (2–36)                                | Total proposer cap across progressive waves. The wave loop only spends up to this on unresolved, still-novel turns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_SWARM_MAX_WAVES`                                                                                                                                                                                                                                                                                                                           | `3`                                        | Progressive proposer waves. `1` restores the legacy single static fan-out.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SWARM_WAVE_GROWTH`                                                                                                                                                                                                                                                                                                                         | `2.0`                                      | Next proposer wave size multiplier, e.g. `4→8→16`, capped by `ANGEL_SWARM_MAX_WIDTH`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_SWARM_NOVELTY_FLOOR`                                                                                                                                                                                                                                                                                                                       | `0.35`                                     | Minimum novel fraction in a returned wave before expanding again. Novelty is local near-duplicate detection against the existing pool.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_SWARM_DISSENT_EPSILON`                                                                                                                                                                                                                                                                                                                     | `0.05`                                     | Stop expanding when mean proposer dissent changes by at most this amount between waves.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_SWARM_HARD_FACTOR`                                                                                                                                                                                                                                                                                                                         | `2`                                        | Wave-0 multiplier for locally-estimated hard prompts when progressive waves are enabled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_SWARM_REFINE_WIDTH`                                                                                                                                                                                                                                                                                                                        | `6`; formation decks pin `=width`           | Refinement-layer fan-out, decoupled from the total proposer cap. Engaging a multi-layer formation writes this to the formation's own width (so layers=2 spends `width` aggregator calls per layer, each carrying full history plus all drafts) and clears it on disengage; single-layer formations leave it unset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_SWARM_LAYERS`                                                                                                                                                                                                                                                                                                                              | `2` (3 on max)                             | MoA layers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_SWARM_SAMPLES`                                                                                                                                                                                                                                                                                                                             | `1` (3 on max)                             | Self-consistency samples.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SWARM_KEEP`                                                                                                                                                                                                                                                                                                                                | `0`                                        | Drafts kept between layers (`0` = all).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_SWARM_QUORUM` / `ANGEL_SWARM_GRACE_SECS` / `ANGEL_SWARM_WAVE_DEADLINE`                                                                                                                                                                                                                                                                     | `0.75` / `20` / `240`                      | Wave barrier: once the quorum fraction of a proposer wave has landed, stragglers get the grace window and are then cut (the mixture already provides redundancy); the deadline bounds a wedged endpoint that would otherwise hold the wave through its full retry budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_MOA_DRAFT_MAX_CHARS`                                                                                                                                                                                                                                                                                                                       | `12000`                                    | Per-draft ceiling for a draft copied into judge/critique/synthesis payloads (tail trimmed, with a `[MOA draft truncated…]` marker), because the aggregator fan-out multiplies every draft by every seat. `0` via env unbounds proposer output; route-context pressure can still trigger hierarchical reduction when the real context window requires it. Aliases `ANGEL_SOTA_MOA_DRAFT_MAX_CHARS` and `ANGEL_SWARM_DRAFT_MAX_CHARS` remain supported.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_DRAFT_DEBUG`                                                                                                                                                                                                                                                                                                                         | off                                        | Emit bounded per-seat draft diagnostics to stderr for MoA debugging. Short/blank outputs include at most a 240-character preview; full drafts are never mirrored. Leave off for ordinary or captured runs because even bounded previews may contain task text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_MOA_FINAL_TARGET_CHARS`                                                                                                                                                                                                                                                                                                                    | `0` (no numeric target)                    | Optional soft final-answer target placed only in the synthesis instruction. The default uses a semantic completeness/directness/non-repetition contract with no numeric anchor. A positive value still does not set provider `max_tokens`, truncate the answer, or reduce internal panel spend.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_MOA_DEDUP_SIM`                                                                                                                                                                                                                                                                                                                              | `0.92`                                     | Local near-duplicate collapse across MoA stages (word-3-gram Jaccard, zero model calls). The default collapses near-identical drafts — converged proposers add nothing to the mixture while each copy costs real tokens in judge/synthesis payloads — and self-consistency / unchanged-revision convergence checks use the same threshold, so an all-agreeing candidate set skips the chooser call. `0` disables collapse to preserve every independently generated draft.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_MOA_CACHE_ALIGN`                                                                                                                                                                                                                                                                                                                           | on                                         | Cache-aligned MoA call shape: the system slot carries only the conversation's own stable system prompt (none for score-only stages) and each stage/persona instruction rides at the end of the trailing task message. Every stage call then opens with the byte-identical `[system][conversation]` prefix that provider prefix caches key on (DeepSeek/OpenAI-compatible provider caches; vLLM/llama.cpp locally) — the re-sent history becomes a cache hit across the proposer fan-out, across stages on the same club, and across turns. `0` restores the legacy stage-prompt-first shape (no prefix ever repeats).                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_MOA_PERSONAS`                                                                                                                                                                                                                                                                                                                              | unset (full roster)                        | Operator-selectable proposer lens roster (the roster card's _focus_ column): comma-separated persona keys selecting **and ordering** the active lenses, e.g. `red-team,systems,empiricist`. Keys are trimmed and matched case-insensitively against the built-in `PERSONAS`; unknown keys are skipped with one stderr warning naming the bad key and the valid set; duplicate keys collapse to their first occurrence; if nothing valid remains the full roster is restored with one warning. Valid keys: `first-principles`, `red-team`, `pragmatist`, `lateral`, `systems`, `empiricist`. Unset or empty ⇒ the full six-lens roster in built-in order (today's behavior). Read fresh each call so per-turn selection is deterministic.                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_MOA_COMPACT_EVERY_LOOPS`                                                                                                                                                                                                                                                                                                                   | `3`                                        | MoA checkpoint-prep cadence. Every MoA worker is told to assume the thread may be compacted after this many loop passes and to leave checkpoint-ready decisions, evidence/source URLs, paths/commands, risks, and next actions. `0` disables the prompt hint. Actual transcript summarization remains budget-triggered by the harness compactor above, so angel0 does not spend a summarizer call when there is nothing useful to compact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_MOA_AUX_CONTEXT_CHARS`                                                                                                                                                                                                                                                                                                                     | `24000`                                    | Conversation-context budget for score-only stages—judge, critic, chooser, verifier. They re-read the whole history on every call (most MoA input spend), so the default keeps the newest ~24,000 chars — the most recent user ask plus trailing tool results — with a leading `[context trimmed: N chars omitted]` marker when the newest message alone busts the budget. `0` restores the same full history answer-producing stages see.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_MOA_DISSENT_GATE`                                                                                                                                                                                                                                                                                                                          | off                                        | Optional dissent-gated per-turn spend (pure local text similarity, zero model calls). When enabled, agreeing drafts relax refine/judge/verify/sample stages and divergent drafts escalate scrutiny. It is off by default so a surface-vocabulary heuristic cannot silently reduce a configured formation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_MOA_DISSENT_HI`                                                                                                                                                                                                                                                                                                                            | `0.7`                                      | Dissent at or above this escalates. Dissent = 1 − mean pairwise content-word Jaccard across the drafts, suppressed when two drafts carry materially different negation density — a surface heuristic, not semantics. The polarity term exists because plain Jaccard scores contradictory drafts ("we should ship" vs "we should not ship") as _identical_; it can only raise dissent, never lower it. `/moa` shows the observed dissent distribution; tune from that data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_MOA_DISSENT_LO`                                                                                                                                                                                                                                                                                                                            | `0.45`                                     | Dissent at or below this relaxes. Between the two thresholds the configured knobs run unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_MOA_DISSENT_VERIFY`                                                                                                                                                                                                                                                                                                                        | `1`                                        | Verify rounds an escalation grants (never takes away rounds already configured). Gated verify suits a quota-capped verify seat: rare, high-value calls only on contested turns.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_ADVISOR`                                                                                                                                                                                                                                                                                                                                   | off                                        | Lightest-MoA advisor. Values: `0`/`off` (default), `1`/`true`/`final` (review final answers on ordinary turns and swarm), `hops`/`every`/`always` (final review **plus** a sparse NOTE/BLOCK after each tool hop as a harness note). On a NOTE/BLOCK the concern is appended so the next hop/turn sees what the doer rushed past. Review failures are swallowed — the advisor never costs the answer. Distinct from judge/verify, which score/revise same-turn drafts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_MOA_SCALE_THRESHOLD`                                                                                                                                                                                                                                                                                                                       | `6`                                        | Draft count above which scale coordination engages: cluster representatives, pod judging, and map-reduce synthesis.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_MOA_CLUSTER` / `ANGEL_MOA_CLUSTER_SIM` / `ANGEL_MOA_REP_MODE`                                                                                                                                                                                                                                                                              | off / `0.55` / `detail`                    | Optional greedy local clustering over draft content-word similarity. Disabled by default so every generated draft reaches configured downstream review. When enabled, representatives are either the most detailed draft in a cluster (`detail`) or the medoid (`medoid`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_MOA_POD_SIZE` / `ANGEL_MOA_POD_KEEP` / `ANGEL_MOA_JUDGE_FANOUT`                                                                                                                                                                                                                                                                            | `6` / `2` / `18`                           | Tournament judging controls for large draft pools: contiguous pods, winners advanced per pod, and reviewer fan-out cap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_MOA_JUDGE_WEIGHTS`                                                                                                                                                                                                                                                                                                                         | off                                        | Show cluster consensus weights to judge prompts, e.g. “7 of 32 drafts converged here”. Off by default to avoid anchoring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_MOA_AGG_FANIN`                                                                                                                                                                                                                                                                                                                             | `0` (context-only)                         | Optional flat synthesis fan-in. By default map-reduce runs only when the resolved route's real context window requires it. A positive value forces wider sets through concise pod fusions first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_MOA_CTX_TOKENS` / `ANGEL_MOA_CTX_RESERVE_OUT` / `ANGEL_MOA_DRAFT_MIN_CHARS`                                                                                                                                                                                                                                                                | unknown / `8192` / `700`                   | Context-fit math for judge/critique/synthesis. The resolved club's reported window wins. If it reports none, no guessed 32K/128K ceiling is imposed unless `ANGEL_MOA_CTX_TOKENS` is explicitly set. Output headroom protects the final generation when a real/configured window is known; this never becomes provider `max_tokens`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_MOA_DOMINANT_RELAX` / `ANGEL_MOA_ECLUSTERS_HI`                                                                                                                                                                                                                                                                                             | `0.67` / `4.0`                             | Scaled gate thresholds: a dominant cluster relaxes optional stages; high effective cluster count escalates scrutiny.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_MOA_LEDGER`                                                                                                                                                                                                                                                                                                                                | on                                         | The MoA outcome ledger: every swarm/sota-moa turn appends one line to `~/.angel0/moa/ledger.jsonl` — route (direct/deliberate/fallback), dissent score, gate decision, effective knobs, draft counts, per-club token deltas, latency, success. Local best-effort writes; no model calls. `/moa` renders the report, `/moa clear` resets. `0` disables recording.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_MOA_LEDGER_PATH`                                                                                                                                                                                                                                                                                                                           | `~/.angel0/moa/ledger.jsonl`                | Ledger file override.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_EXPERIENCE` | on | The **experience ledger** (`experience.rs`): one append-only JSONL line per turn recording the _configuration that ran_ (a curated `ANGEL_*` knob snapshot + a stable `cfg.hash`) and the _outcome_ (stop reason, latency, hops, anti-spin counters, per-club token delta, and any text-stage failovers — the LongCat raw-markup brownout lands here). Record kinds include `turn` (single-agent harness), `moa_turn` (SOTA-MOA), `route_verdict` (one explicit `useful`/`miss` label for the last answer, containing route metadata but no conversation text), and `event` (per-command observations, below). Every record carries a `repo` stamp (`{key, root, slug}` — the same `workspace_key` the work-context store files under) identifying the workspace it happened in. Used by workspace memory and outcome analysis. Local best-effort writes; secret-named vars (`KEY`/`TOKEN`/`SECRET`/`AUTH`/`PASSWORD`) are never recorded. `0` disables. |
| `ANGEL_EXPERIENCE_LOG`                                                                                                                                                                                                                                                                                                                            | `~/.angel0/experience/ledger.jsonl`         | Experience ledger file override. Self-rotates to `ledger-<ts>.jsonl` at 50 MB.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_EXPERIENCE_EVENTS`                                                                                                                                                                                                                                                                                                                         | on                                         | The per-command `event` records within the experience ledger: each `shell`/`cargo`/`run_tests` execution appends `{text, exit, verdict, verdict_reason, shell, pipefail, source, independent, timed_out, dur_ms, tool, bytes_out}` — exit status is observed here and nowhere else (the tool's return string to the model is unchanged). **Read `verdict`, not `exit`:** it is a tristate (`pass` / `fail` / `no_verdict`) and it is the only field that knows whose status `exit` actually is. A `no_verdict` row carries a `verdict_reason` (`no_pipefail` / `sigpipe` / `signal` / `timed_out`) and must not vote. `independent: false` (equivalently `source: "dossier"`) means the dossier asserted that command in this session's prompt, so the run is its own echo and may not support the fact that suggested it. Command text is secret-scrubbed at the record builder (`NAME=value`, `--token …`, `Bearer …`, URL userinfo) and capped at 400 chars. The Repo Dossier mines these into per-repo rituals and traps. `0` disables just the events; turn records keep flowing. |
| `ANGEL_SHELL`                                                                                                                                                                                                                                                                                                                                     | `bash`                                     | The shell the `shell` tool runs commands under. It is **not** `sh`: `/bin/sh` is `dash` here, which has no `pipefail`, and without `pipefail` a pipeline's exit status is its _last_ stage's — so `cargo check 2>&1 \| tail -20` reported `tail`'s 0 whether or not the build passed, and **every piped build/test command angel ever ran was recorded as a pass**. 56% of recorded commands contain a pipe. `bash` is also a superset of what models already write by reflex (`[[ … ]]`, arrays, `&>`, `{1..10}`), all of which failed silently under dash. Resolved once per process by probing, falling back to `sh` if the named shell cannot run; the shell actually used is stamped on every `cmd` record.                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_SHELL_PIPEFAIL`                                                                                                                                                                                                                                                                                                                            | on                                         | Whether the shell propagates a failing pipeline stage into the exit status. `0` restores the old (POSIX) semantics — and, because the choice is recorded, every piped command then records `verdict: "no_verdict"` (`verdict_reason: "no_pipefail"`) instead of a laundered pass. There is no configuration in which the ledger both lies and claims not to. Set it if `pipefail` is breaking a pipeline you rely on; the cost is the evidence, not the correctness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_REPO_IDENTITY` | on | **Repo identity** (`workspace_store.rs::repo_identity`): resolve a workspace to the _repository_ it belongs to — the **main worktree** (`git worktree list` prints it first), never a linked worktree or a subdirectory — before computing the `workspace_key` that the experience ledger, The Cut's manifest, and the dossier artifact all join on. A nested _independent_ repo (its own `.git`) still keeps its own identity, because that is what git reports. A repo whose workspace already is its main worktree keeps its **exact** existing key, so no history is re-keyed. Not a repo / no git / timeout → falls back to the workspace path. `0` restores the legacy path-only keying. |
| `ANGEL_DOSSIER` | on | The **Repo Dossier** warm-start block (`dossier.rs`; see [repository memory](../../docs/MEMORY.md)): renders `~/.angel0/dossier/<workspace_key>.json` (compiled with `node scripts/runtime/repo-dossier.mjs --refresh` from the experience ledger) into a `[repo dossier …]` context block — the repo's verified rituals, standing traps, and where the last session left off. Injected into the startup system prompt, the one-shot `/cd` context, and the headless `--task` prompt. Confidence-gated: facts below the belief threshold are withheld and counted, never asserted. `0` removes the block everywhere. |
| `ANGEL_DOSSIER_TASK`                                                                                                                                                                                                                                                                                                                              | on                                         | The dossier warm-start inside **headless `--task`** specifically (`harness/task_mode.rs::task_warm_start`, wired at `main.rs`). Every autonomous coding run — the Conductor's code rung included — goes through `--task`, and it used to open cold: no rituals, no traps, no "last session here", so a capable model burns its hop budget re-reading a repo angel already learned. Same renderer and same confidence gate as the TUI. `0` suppresses it in the headless seat only (for benchmarks that need a byte-exact preamble); `ANGEL_DOSSIER=0` still kills both seats.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_DOSSIER_DIR`                                                                                                                                                                                                                                                                                                                               | `~/.angel0/dossier`                         | Dossier artifact directory override.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_DOSSIER_MIN_BELIEF`                                                                                                                                                                                                                                                                                                                        | `0.70`                                     | Facts below this belief are withheld from the block (decay pushes stale facts under it, making them probe targets instead of stale assertions).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_DOSSIER_MAX_BYTES`                                                                                                                                                                                                                                                                                                                         | `2048`                                     | Size cap on the rendered block (truncates with `…`, always closes the sentinel).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_CONDUCTOR` | off | **Conductor** night scheduler arm. Exact value `1` allows the packaged Conductor worker to dispatch inside its window; `measure` folds evidence and ranks without dispatch; unset disables it. Code work still requires `ANGEL_CONDUCTOR_DRIVER`. |
| `ANGEL_CONDUCTOR_DRIVER`                                                                                                                                                                                                                                                                                                                          | unset                                      | Tool-calling driver label for Conductor code-rung runs. Required in addition to `ANGEL_CONDUCTOR=1`; when unset, `/conductor` reports that code dispatch is waiting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_CONDUCTOR_DIR`                                                                                                                                                                                                                                                                                                                             | `~/.angel0/conductor`                       | Conductor state directory for `status.json`, `queue.json`, `heartbeat.json`, budget, lock, verdict spool, and parked worktrees.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_MAX`                                                                                                                                                                                                                                                                                                                                 | —                                          | Max-effort preset toggle (bumps widths/layers/verify).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_SWARM_ALWAYS`                                                                                                                                                                                                                                                                                                                              | —                                          | Always run the swarm (vs adaptive engagement).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_SWARM_SMART_ROUTE`                                                                                                                                                                                                                                                                                                                         | off                                        | Route each turn with a **model classifier** (adaptive, +1 round-trip/turn). Default off: a fast heuristic routes with **no** routing call — answer directly unless the request clearly wants multi-angle deliberation (`design`/`compare`/`tradeoffs`/long asks). The default-speed lever.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SWARM_FASTPATH`                                                                                                                                                                                                                                                                                                                            | on                                         | When `ANGEL_SWARM_SMART_ROUTE` is on, still skip the classifier on trivial conversational turns (greetings/acks).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_SWARM_JUDGE` / `ANGEL_SWARM_JUDGE_PANEL` / `ANGEL_SWARM_JUDGE_DIMS`                                                                                                                                                                                                                                                                        | panel `1` (3 on max)                       | Peer-review judging: enable, panel size, per-dimension scoring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_VERIFY` / `ANGEL_SWARM_VERIFY_GUARD`                                                                                                                                                                                                                                                                                                 | `0` (2 on max)                             | Adversarial verify passes / guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_SWARM_HEDGE` / `ANGEL_SWARM_CITE` / `ANGEL_SWARM_REFLECT`                                                                                                                                                                                                                                                                                  | —                                          | Calibrated-hedge ladder / citation pass / self-reflection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SWARM_RESEARCH` / `ANGEL_SWARM_SEARCH_URL`                                                                                                                                                                                                                                                                                                 | —                                          | Autoresearch stack + its search endpoint.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_GROK_RESEARCH` / `ANGEL_GROK_RESEARCH_ALWAYS` / `ANGEL_GROK_TIMEOUT_SECS`                                                                                                                                                                                                                                                                  | on when OAuth is available / off / `60`    | Optional Grok ACP research scout controls. The scout auto-runs only for prompts that request live/latest/trending/web research, or when an operator/formation explicitly enables it; ordinary SOTA deliberation does not put a CLI child in front of the turn. `ANGEL_GROK_RESEARCH=0` disables only the ACP scout/`grok_research` tool — the HTTP `grok` OAuth driver still loads from `~/.grok/auth.json`. `ANGEL_GROK_RESEARCH_ALWAYS=1` opts every deliberate MoA turn back in. One ACP prompt is bounded to 60 seconds by default; Esc cancels it promptly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SWARM_DELEGATE` / `ANGEL_SWARM_PEER` / `ANGEL_SWARM_PEER_CMD` / `ANGEL_SWARM_REMOTE`                                                                                                                                                                                                                                                       | —                                          | Sub-agent delegation / peer model / peer command / remote execution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_SWARM_PHONE`                                                                                                                                                                                                                                                                                                                               | —                                          | Phone-a-SOTA escalation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_SWARM_APPROVE`                                                                                                                                                                                                                                                                                                                             | —                                          | Approval-gated swarm actions (token-efficient pipeline).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_ACTION_CAPSULES`                                                                                                                                                                                                                                                                                                                           | `approve`                                  | Interactive-root action capsule mode. `approve` is the fail-safe default and adds one token-free approval modal per model-emitted effectful batch; unknown values also resolve to `approve`. `observe` emits UI-only scoped previews/receipts, and `off` is the explicit no-classification fast path. It never adds a model call, preflight subprocess, automatic Git scan, verifier command, or per-action disk write. Headless tasks, delegates, worktrees, and evaluation registries ignore it. `/approvals actions observe\|on\|off` changes it live; `/approvals selftest` exercises a harmless typed broker/UI/worker round trip without starting a model turn or action.                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_MAX_TESTS` / `ANGEL_SWARM_TEST_DIR` / `ANGEL_SWARM_TEST_TIMEOUT`                                                                                                                                                                                                                                                                     | `3`                                        | RLVR test matrix size / dir / timeout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_SWARM_RUNS_DIR`                                                                                                                                                                                                                                                                                                                            | `~/.angel0/swarm-runs/<workspace-key>`      | Durable proof graphs, event ledgers, routing policy, and transient verification worktrees for `swarm_compile`. Run snapshots are atomic; candidates stay on parked branches until explicit integration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_SWARM_VERIFY_TIMEOUT_SECS`                                                                                                                                                                                                                                                                                                                 | `900`                                      | Per-command deadline for the compiler's base, red, candidate, and quality proof commands (`10`–`7200`). Timeout is a failed proof, never an implicit pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SWARM_SSH_` (prefix)                                                                                                                                                                                                                                                                                                                       | —                                          | Required `user@host` connection details for remote swarm workers (for example, `ANGEL_SWARM_SSH_SPARK`). There are no compiled SSH identities or endpoints.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ANGEL_SWARM_PROPOSE_CLUB` / `ANGEL_SWARM_JUDGE_CLUB` / `ANGEL_SWARM_VERIFY_CLUB` / `ANGEL_SWARM_AGG_CLUB`                                                                                                                                                                                                                                        | — (inner)                                  | Cross-model mixture: route each role (propose/judge/verify/aggregate) to a different fleet club by label (`spark`/`gemma`/`turbo`/`atlas`). Unset/unresolvable → the inner worker, so the default is a homogeneous swarm.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SOTA_MOA_COST_PROFILE`                                                                                                                                                                                                                                                                                                                     | smartest-first                             | Set to `cheap` for a hard spend boundary: local Gemma, OpenRouter-free, and LongCat are the entire eligible role/failover pool; OpenAI, Kimi, DeepSeek, GLM, Grok, and Cerebras cannot be reached by an MoA fallback. Gemma is availability-gated, so an offline Spark is omitted rather than stalling the mixture. Unset retains the frontier smartest-first policy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_SOTA_MOA_CHEAP_LINKS`                                                                                                                                                                                                                                                                                                                      | `gemma,openrouter,longcat`                 | Hard allowlist inside the cheap profile. Set `longcat` for homogeneous LongCat-only proposal and synthesis; excluded links cannot appear as extra proposers or fallbacks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SOTA_MOA_PROPOSE_CLUB` / `ANGEL_SOTA_MOA_JUDGE_CLUB` / `ANGEL_SOTA_MOA_VERIFY_CLUB` / `ANGEL_SOTA_MOA_AGG_CLUB`                                                                                                                                                                                                                            | profile order                              | Override the ready-made `sota-moa` role routing. The normal profile uses configured SOTA links in canonical intelligence order: Codex/OpenAI OAuth → Kimi → DeepSeek → GLM → Grok → LongCat → OpenRouter/Hy3 → Cerebras. Under `ANGEL_SOTA_MOA_COST_PROFILE=cheap`, defaults become LongCat propose/judge/aggregate and OpenRouter verify; the local Gemma seat is added separately as proposer-only breadth.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_SOTA_MOA_PROPOSE_EFFORT` / `ANGEL_SOTA_MOA_JUDGE_EFFORT` / `ANGEL_SOTA_MOA_VERIFY_EFFORT` / `ANGEL_SOTA_MOA_AGG_EFFORT`                                                                                                                                                                                                                    | unset                                      | Per-seat reasoning-effort request for the MoA roster (the roster card's THINK column). Rides each stage call — never written into club state — so one club shared across seats (e.g. the homogeneous LongCat cheap profile) runs different efforts per seat, and a stage failover keeps the seat's effort. Values are requests: each seat's club applies its own validation/dialect (HTTP ladder `none/low/medium/high`); an unsupported value simply isn't sent. The tool-capable driver hop is not a seat and keeps the in-hand club's own THINK/env state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_SOTA_MOA_EXTRA_PROPOSERS`                                                                                                                                                                                                                                                                                                                  | `gemma`                                    | Comma-separated configured proposer-only breadth seats (local or explicitly enabled remote models, e.g. `glm-5.3`). The primary remains `ANGEL_SOTA_MOA_PROPOSE_CLUB`; cheap mode still enforces its allowlist. The default adds a live Gemma seat to SOTA-MoA without allowing it to judge, verify, aggregate, drive tools, or enter frontier failover. `none` disables it for the token-efficiency control run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_SOTA_MOA_WIDTH` / `ANGEL_SOTA_MOA_LAYERS`                                                                                                                                                                                                                                                                                                  | `2` / `1`                                  | Fast SOTA-MOA shape for prompts routed to MOA. Default is one proposer wave plus one streamed synthesis wave; increase only when latency/token cost is acceptable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_SOTA_MOA_MAX_WAVES` / `ANGEL_SOTA_MOA_MAX_WIDTH`                                                                                                                                                                                                                                                                                           | `1` / `=width`                             | Paid SOTA-MOA progressive-wave caps. Defaults never auto-expand; override only for explicit live testing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SOTA_MOA_MAX_PARALLEL`                                                                                                                                                                                                                                                                                                                     | `8`                                        | Maximum simultaneously admitted SOTA-MoA requests inside one wave. Seats above the cap remain queued inside the same deadline and still count toward the exact requested width; this limits provider bursts without silently shrinking a formation. |
| `ANGEL_SOTA_MOA_FORMATION`                                                                                                                                                                                                                                                                                                                        | unset (no formation)                       | Read-only marker naming the engaged formation (e.g. `Full Muster`), written when the roster deck arms a formation and cleared on disengage. The MoA ledgers copy it onto each turn record so per-formation token economics are measurable; it is not an operator input.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_SOTA_MOA_MODE`                                                                                                                                                                                                                                                                                                                             | `manual`                                   | Amplification policy for the tool-capable driver. `manual` (default): the coordinator (AGG seat) answers every turn solo at standard single-model spend; the mixture fan-out engages only when the turn opens with `moa:` (or `/moa`). `auto`: the old heuristic gate (research routing / synthesis-marker prompts amplify on their own — the pre-2026-07-07 token-blowout behavior). `always`: every turn amplifies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `ANGEL_SWARM_MODE`                                                                                                                                                                                                                                                                                                                                | `auto`                                     | Same policy knob for the local swarm driver. Stays `auto` by default — local fleet calls are free, so the heuristic gate remains the right trade.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_GPU_COMP_LOCAL_MOA`                                                                                                                                                                                                                                                                                                                        | off / formation-owned                      | Set while the GPU Comp formation is active so its local Turbo/Leanstral/DICE routing contract is distinguishable from ordinary swarm mode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SOTA_MOA_ALWAYS`                                                                                                                                                                                                                                                                                                                           | off                                        | Legacy force switch that amplifies every prompt when set. Off preserves `ANGEL_SOTA_MOA_MODE`: the default `manual` mode stays coordinator-only unless the turn explicitly opens with `moa:` or `/moa`; heuristic work/research routing applies only in `auto` mode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_SOTA_MOA_MAX`                                                                                                                                                                                                                                                                                                                              | off                                        | Bump SOTA-MOA to a heavier preset (`width=3`, `layers=2`, `samples=2`, `verify=1`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_SOTA_MOA_JUDGE` / `ANGEL_SOTA_MOA_VERIFY` / `ANGEL_SOTA_MOA_RESEARCH`                                                                                                                                                                                                                                                                      | off / `0` / off                            | Optional SOTA-MOA quality passes. Off by default because each adds provider/network round trips.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_SOTA_MOA_DISSENT_GATE`                                                                                                                                                                                                                                                                                                                     | inherits `ANGEL_MOA_DISSENT_GATE` (off)    | SOTA-only adaptive spend gate. Set `1` to let measured disagreement relax or escalate the configured pipeline; the quality-first default never silently changes formation stages. The **Tag Team** formation pins this on for the duration of its engagement — that gate is what keeps its metered advice seat conditional — and clears it again when another formation is engaged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `ANGEL_SOTA_MOA_REFLECT` / `ANGEL_SOTA_MOA_SAMPLES` / `ANGEL_SOTA_MOA_KEEP` / `ANGEL_SOTA_MOA_JUDGE_PANEL` / `ANGEL_SOTA_MOA_JUDGE_DIMS` / `ANGEL_SOTA_MOA_VERIFY_GUARD` / `ANGEL_SOTA_MOA_HEDGE` / `ANGEL_SOTA_MOA_CITE` / `ANGEL_SOTA_MOA_DELEGATE` / `ANGEL_SOTA_MOA_GROK_RESEARCH` / `ANGEL_SOTA_MOA_MAX_TESTS` / `ANGEL_SOTA_MOA_SEARCH_URL` | fast defaults; Grok scout off              | `sota-moa` twins of the corresponding `ANGEL_SWARM_*` knobs, read only by the paid preset — so a maxed local swarm config can never silently make SOTA-MOA slow/expensive (see `ANGEL_SOTA_MOA_USE_SWARM_KNOBS` to opt into inheritance instead). Live-research prompts and formation decks can still engage the Grok scout; set `ANGEL_SOTA_MOA_GROK_RESEARCH=1` to put it in front of every deliberate SOTA-MoA turn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_SOTA_MOA_ALLOW_DEGRADED`                                                                                                                                                                                                                                                                                                                   | off                                        | Allow `sota-moa` to fall back to direct/single-draft synthesis when the proposer quorum collapses (link down/cut mid-wave). Default fails closed so a "mixture" answer is never quietly one model. The **Tag Team** formation pins this on while engaged (and clears it on leave): its corners are fleet boxes that are allowed to be slow or terse, and a strict quorum there discards the live partner's draft to fail the whole turn.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_SOTA_MOA_USE_SWARM_KNOBS`                                                                                                                                                                                                                                                                                                                  | off                                        | Make `sota-moa` inherit the general `ANGEL_SWARM_*` knobs. Off by default so local-swarm max settings cannot make SOTA-MOA painfully slow.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `ANGEL_SOTA_MOA_ALLOW_SINGLE`                                                                                                                                                                                                                                                                                                                     | off                                        | Allow the normal `sota-moa` preset to appear with only one configured SOTA link. The cheap profile permits one allowlisted provider so a homogeneous LongCat calibration can still run multiple independent agent calls.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| — (automatic preset only)                                                                                                                                                                                                                                                                                                                         | on                                         | Ready-made SOTA-MoA failover: the normal automatic preset walks the smartest configured SOTA links after quota/auth/text-stage failure. The `cheap` preset instead walks only `ANGEL_SOTA_MOA_CHEAP_LINKS`, including connection failure, while excluding every frontier/token-plan link. Formation-board engagements use the exact per-seat roster and attach no out-of-roster fallback bench, so a failed seat cannot silently change the operator's model budget. Streaming stages fail over only if nothing has streamed yet, so text is never duplicated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_SOTA_CAVEMAN` / `ANGEL_SOTA_CAVEMAN_LEVEL`                                                                                                                                                                                                                                                                                                 | off / `full`                               | Optional Caveman-derived output brevity for SOTA-tuned outbounds, including `sota-moa` role calls and direct SOTA tabs. Off by default so proposer and reviewer work is not compressed before final synthesis. When enabled it keeps technical content, code, commands, JSON, tool-call args, paths, commit keywords, and exact errors verbatim; levels are `lite`, `full`, and `ultra`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_PXPIPE` / `ANGEL_PXPIPE_MODELS`                                                                                                                                                                                                                                                                                                            | off / `gpt-5,gpt-4o,gpt-4.1`               | Experimental pxpipe request compression for SOTA-tuned outbounds only. It is disabled by default and must not be treated as an authority, verifier, required transport, or completion gate. When explicitly enabled, eligible OpenAI/Codex-family MOA calls render bulky prompt/history/tool context as image input parts if the model id matches an allowlisted image-input family; labels alone do not enable imaging. Unsupported providers such as DeepSeek, LongCat, and Cerebras pass through unchanged unless explicitly allowlisted. This uses image input, not image generation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_PXPIPE_MIN_CHARS` / `ANGEL_PXPIPE_COLLAPSE_HISTORY` / `ANGEL_PXPIPE_COMPRESS_TOOLS`                                                                                                                                                                                                                                                        | `2000` / on / on                           | pxpipe helper options forwarded to `pxpipe-proxy`. The upstream profitability gate can still choose pass-through when images are not cheaper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_PXPIPE_REQUIRED` / `ANGEL_PXPIPE_HELPER` / `ANGEL_PXPIPE_NODE`                                                                                                                                                                                                                                                                             | off / repo helper / `node`                 | After explicitly enabling pxpipe, failures fail open and send the original text request. Set `ANGEL_PXPIPE_REQUIRED=1` only for controlled experiments that intentionally need fail-closed behavior. Override the helper path or Node executable only for custom deployments.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_PXPIPE_PERSISTENT`                                                                                                                                                                                                                                                                                                                         | on                                         | Keep one pxpipe helper daemon alive across calls instead of spawning a Node process per request. `0` = per-call helper.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_PXPIPE_DEBUG`                                                                                                                                                                                                                                                                                                                              | off                                        | `1` bypasses the persistent daemon (per-call helper only) for debugging transforms. The helper itself is invoked with `ANGEL_PXPIPE_LABEL` / `ANGEL_PXPIPE_MODEL` set in its environment (read by the helper, not the cockpit).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_SWARM_DEBUG`                                                                                                                                                                                                                                                                                                                               | —                                          | Verbose swarm tracing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_DELI_ROUNDS`                                                                                                                                                                                                                                                                                                                               | `6`                                        | Deli driver: deep-over-time iterations.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_DELI_PIVOT`                                                                                                                                                                                                                                                                                                                                | `2`                                        | Stale-count at which to force a structural pivot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `ANGEL_DELI_STALL_STOP` | `0` (off) | Optional early-stop threshold for consecutive rounds without a new evidenced finding. Default behavior keeps working through the configured Deli rounds; duplicate claims still do not count as progress. |
| `ANGEL_DELI_MIN_FINDINGS` | `0` (off) | Optional early completion once this many distinct evidenced findings accumulate. Zero runs the configured rounds unless cancelled or an explicit stall threshold is reached. |
| `ANGEL_DELI_STATE_DIR`                                                                                                                                                                                                                                                                                                                            | —                                          | Deli state-file directory.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

## Autonomous loop (`loop_ctl.rs`)

`/loop` drives successive full agentic turns at a goal/task until a stop condition.
Budgets guarantee termination. On a stall it brings progressively more of the engine
to bear — **Local → Swarm** (local Mixture-of-Agents, automatic) **→ SOTA** (a frontier
model, only behind an approval modal). `/loop deli` drives each iteration through the
**deli** deep-think-over-time method (composes with any tier). Done-detection is hybrid:
a goal's pinned `cmd` (`/goal cmd <check>`) is run and, when it emits a libtest summary,
scored through the **LEVI / reinforce** verifiable reward (`CompositeReward::code_health`)
as ground truth — guarded against reward-hacking (zero-test green rejected; a green whose
pass-count regresses below the pre-edit baseline rejected). Otherwise the model's
`LOOP_DONE` is "claimed". The pinned command guards against the _model_ swapping the
predicate mid-run; the operator can re-pin a live run with `/goal cmd <check>` (the old
baseline is dropped). `/goal go` starts (or resumes) the loop at the active goal. State
persists to `~/.angel0/loop.json` (crash → resumes
`Paused`); with `ANGEL_LOOP_STATE_DIR` set it also mirrors a deli-style `state/` dir for
an external watchdog.

| Variable | Default | Purpose |
| ------------------------------ | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_LOOP_MAX_ITERS`         | `0`                 | Hard iteration ceiling (`0` = off).                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_LOOP_DEADLINE_SECS`     | `0`               | Whole-loop wall-clock budget (`0` = off).                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_LOOP_TOKEN_BUDGET`      | `0`            | Cumulative (estimated) token budget — input + output — across iterations (`0` = off).                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_LOOP_STALL_STOP`        | `4`                  | Consecutive no-progress iterations per tier before escalating / stopping (`0` = off).                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_LOOP_BLOCKED_REPEAT_LIMIT` | `3` | Podrace: pause and record an operator escalation after this many consecutive iterations with the same verifier failure digest. `0` explicitly disables this pause. `/loop resume` resets the streak. |
| `ANGEL_LOOP_FIRST_CANDIDATE_ITERS` | `3`              | Podrace: stop the run after this many iterations without a verified measured candidate (`0` = off). |
| `ANGEL_LOOP_FIRST_SUBMISSION_ITERS` | `4`             | Podrace: after the first measured candidate, this many further iterations without a submission receipt inject the submission-overdue directive first in the prompt; twice this many stop the run (`0` = off).                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_LOOP_SOTA`              | on                   | Offer a third escalation tier after Local→Swarm stalls. `0` stops without a SOTA seat.                                                                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_LOOP_LOCAL_CLUB` | unset | Explicit override for the loop's initial model (e.g. `qwen38`). Unset: honor the selected concrete model, including ChatGPT/Grok OAuth. Only a logical-wrapper or unavailable selection falls back to the smartest reachable non-SOTA fleet seat. Provider billing/auth/quota failures pause the loop without stall escalation. |
| `ANGEL_LOOP_SOTA_CLUB`         | unset                | Exact club label for the loop SOTA tier (e.g. `deepseek-v4-flash`). When set, overrides ranking.                                                                                                                                                                                                                                                                                                                                                              |
| `ANGEL_LOOP_SOTA_ALLOW_OPENAI` | on | OpenAI/Codex/API routes are eligible for loop escalation by default. Set `0` to exclude them from automatic selection. An explicit `ANGEL_LOOP_SOTA_CLUB` pin remains authoritative. |
| `ANGEL_LOOP_PIVOT`             | `2`                  | Stall-count at which the next iteration is forced to pivot structure.                                                                                                                                                                                                                                                                                                                                                                                         |
| `ANGEL_LOOP_VERIFY_THRESHOLD`  | `1.0`                | RLVR reward a verifiable `cmd` must reach to count as done.                                                                                                                                                                                                                                                                                                                                                                                                   |
| `ANGEL_LOOP_STATE_DIR`         | —                    | Mirror run state to `<dir>/state/` (progress/findings/iteration_log/acceptance) for a watchdog.                                                                                                                                                                                                                                                                                                                                                               |
| `ANGEL_LOOP_FILE`              | session-scoped file under `~/.angel0/loops/` | Explicit single-file loop-state override. The default key includes both repository and cockpit session, so concurrent shells in one directory cannot inherit or overwrite one another's autonomous loop. Tests may set this to isolate state; operators should set it only when intentionally directing one checkpoint path. |
| `ANGEL_GOAL_FILE`              | `~/.angel0/goal.json` | Durable `/goal` store, injected into every turn. (Tests set this to isolate.)                                                                                                                                                                                                                                                                                                                                                                                 |
| `ANGEL_CAMPAIGN`               | `1` (on)             | Native project-bound campaign authoring, persistence, and bounded Harness lens. `0` makes `/campaign` inert without deleting state. `/campaign advance` freezes one clean-base criterion contract and parks a cancellable internal swarm proof round. `/campaign review` runs its exact, independent, read-only alignment gate. A pass advances only the campaign's logical head; neither command merges, moves a live ref, or changes the operator checkout. |
| `ANGEL_CAMPAIGN_STATE_DIR`     | `~/.angel0/campaigns` | Campaign record/event root. Records are keyed to the canonical project and stored with private permissions.                                                                                                                                                                                                                                                                                                                                                   |

Loop runs and the ordinary loop/handoff-RL workshop default to no cap. Positive
loop cap environment values and workshop selections are operator choices.
`/loop endless` clears iteration, elapsed-time, and token caps and resumes a
cap-paused loop; `/loop max 0` clears only iterations and reports other caps.
Saved loop records predating cap provenance cannot distinguish inherited defaults
from explicit choices, so their ambiguous caps are cleared on load. Reapply any
wanted cap explicitly. The explicitly selected podrace profile retains its
five-day cap.

## The village (miniworld fleet mirror, `village.rs`)

The miniworld's hamlet is an opt-in fleet mirror — the granary is the forge's
candidate dataset, and cottages come only from an explicit `ANGEL_HEADS_FILE`.
Built-in service URLs and cottage fallbacks are loopback-only; set explicit
routes when the village should mirror a remote deployment. Once enabled, one
background thread polls with staggered probes and bounded timeouts; an
unavailable endpoint only darkens its building (state renders from
`~/.angel0/village.json`).

| Variable | Default | Purpose |
| ------------------------- | ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_VILLAGE`           | off                      | The whole village layer. Set `1` to enable polling, persistence, and drawing.                                                                                          |
| `ANGEL_HEADS_FILE`        | unset                    | Explicit JSON registry for cottage health endpoints. No checkout-relative or bundled fleet registry is loaded when unset.                                              |
| `ANGEL_FORGE_URL`         | `http://127.0.0.1:8017`  | Forge sidecar route. Set an explicit remote URL when mirroring another host. Its `/health` drives smithy/candidate status; polling never starts training or promotion. |
| `ANGEL_VILLAGE_FLAVOR`    | on                       | Villager chatter generated by the 4B itself (OpenAI-compatible `qwen3.5:4b`). `0` keeps the canned fallback lines only — no model calls.                               |
| `ANGEL_VILLAGE_VOICE_URL` | `http://127.0.0.1:11434` | Local Ollama-compatible voice route. Set an explicit remote URL when needed.                                                                                           |
| `ANGEL_VILLAGE_STATE`     | `~/.angel0/village.json`  | Village persistence path override (versioned JSON, atomic tmp+rename writes).                                                                                          |

Forge defaults `FORGE_ADAPTER_FAMILY` to `coding-agent`; set it explicitly to
`atlas-clerk` or `utility-judge` for a specialist adapter. Every training family
requires an exact `FORGE_BASE_FINGERPRINT`. Specialist families use independent
dataset, adapter, registry, and ledger roots. Held-out receipts arrive through
`FORGE_BEHAVIOR_GATE_FILE`, and promotion additionally requires the matching
`FORGE_SERVING_BASE_FINGERPRINT` plus the exact candidate digest. `/train`
accepts only an explicit `operator_confirmed: true` request carrying the
one-shot 64-hex `FORGE_TRAIN_CONFIRMATION_DIGEST` armed by Conductor.

## Angel's Share barrel (`barrel.rs`)

Capped, redacted capture of context and teacher-answer pairs. Default off;
`ANGEL_BARREL_MAX_MB` bounds retained shard storage.

| Variable | Default | Purpose |
| ----------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_BARREL`          | `0`               | Arm Angel's Share capture. Exact string `1` arms (the forge-latch posture); anything else leaves it off.                                                                                                   |
| `ANGEL_BARREL_TEACHERS` | empty             | Comma list of club labels whose final answers may be barreled (matches the club label or its resolved route driver, case-insensitive). Empty = capture nothing — this is the per-provider ToS opt-in seam. |
| `ANGEL_BARREL_DIR`      | `~/.angel0/barrel` | Barrel state: day shards `barrel-YYYYMMDD.jsonl` plus the counters-only `writer-status.json`.                                                                                                              |
| `ANGEL_BARREL_MAX_MB`   | `1024`            | Hard cap on total shard bytes. At cap the oldest closed shard is evicted first; today's shard is never evicted — new captures drop instead (counted in `writer-status.json`).                              |

## The Cut (authored-diff manifest + machine verdict, `cut.rs`)

Every mutation angel makes earns two labels: the machine's immediately (did the
project still build?) and yours eventually (did the diff survive into a commit?).
Rust writes the authored-change manifest. **On by default**:
an agent that does not check its own work is the thing this fixes.

| Variable | Default | Purpose |
| ------------------------------- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_CUT`                     | on             | `0` kills capture. The four mutation tools (`write_file`, `str_replace`, `multi_edit`, `apply_patch`) are wrapped at their one registration site, so capture covers code-mode and delegate writes too.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `ANGEL_CUT_DIR`                 | `~/.angel0/cut` | Manifest location: day shards `authored-YYYYMMDD.jsonl`, one row per authored file per call (redacted with the barrel's redactor; `authored_sha256` digests the text as written).                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `ANGEL_CUT_MAX_MB` | `256` | Cap on authored manifest shards; oldest closed shards are evicted first. Separate final-verification shards have a cap of one sixteenth of this budget. Today's active shard follows the existing barrel append discipline. |
| `ANGEL_CUT_VERIFY` | on | `0`/`off` opts out; any other nonempty value is the explicit command at the workspace root. Unset resolves each source path to its learned build ritual or cheapest project default: nearest-crate `cargo check`, TypeScript typecheck, file-scoped Node syntax check, or isolated Python source-byte compilation without bytecode writes. Identical concrete plans share one execution per completed tool batch. Only incomplete default Cargo scaffolds defer until target creation or the mandatory answer-boundary check. Failures remain model-visible; per-file authored rows identify shared receipts. |
| `ANGEL_CUT_VERIFY_TIMEOUT_SECS` | `60`           | Hard deadline on the verify (clamped 5–600). A timeout is not a verdict: it backs the command off for 5 minutes rather than taxing every subsequent write — the agent's speed owns the priority lane, instrumentation yields.                                                                                                                                                                                                                                                                                                                                                                                                    |

Post-write verification resolves every changed source path and runs each concrete
command/directory/source plan once per fully dispatched tool batch. Separate
crates and file-scoped syntax checks remain distinct. Authored rows retain their
own file provenance, with `verification_id` and `shared` identifying one reused
receipt; rollout reward observations count that execution once. Forge summaries
report execution counts separately from per-fragment machine labels. A subsequent
batch always invalidates the prior verdict for its changed scope before checking.

Only the default Cargo check can defer a small, valid package manifest that has
no declared or conventional target yet. A complete scaffold is checked on its
next source edit; a normal answer forces any remaining deferred check, independently
of optional verify-before-done controls. Interrupted scaffolds and later writes
whose verifier is cancelled, timed out or unavailable remain unverified and cannot
inherit an earlier passing reward. Explicit `ANGEL_CUT_VERIFY=0` and externally
owned evaluator labels retain their existing semantics. Standalone final-check
receipts use `verification-YYYYMMDD.jsonl` beside authored shards, with a separate
cap of one sixteenth of `ANGEL_CUT_MAX_MB`; they are not authored training examples.
Python defaults compile source bytes without executing them or writing bytecode.

## Outbound provisioning (`club/provision.rs`)

Two regimes at the club-dispatch choke point: the **economizer** trims what a
metered call buys, the **optimizer** spends tokens on purpose for accuracy.
Every splice is tail-side so the pinned, cacheable prefix stays byte-identical;
local fleet bodies change only where a rule is explicitly scoped to them.

| Variable | Default | Purpose |
| -------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_ECON`                                 | `1` (on)              | Master switch for the economizer on metered (SOTA-tuned) links: a tail-side output contract when the ask demands a fixed structure, the bounded budget paired with that contract, and whitespace-squeezing of harness-authored messages. Local fleet bodies stay byte-identical.                  |
| `ANGEL_ECON_EXTRACT_MAX_TOKENS`              | `0` (provider-native) | Optional output cap sent only alongside a detected extraction contract. The semantic format contract remains active by default, but no numeric `max_tokens` is invented. Set a positive value to opt into a cap.                                                                                  |
| `ANGEL_STOP_SEQS` / `ANGEL_<CLUB>_STOP_SEQS` | unset                 | Comma-separated stop sequences (`\n` escapes honored, max 4) sent as the request's `stop` field; the per-club spelling wins. Never auto-derived from the prompt — a guessed stop sequence can amputate code or tool JSON. The judge may also supply up to 2 for a call it provisioned.            |
| `ANGEL_OPT_DUP`                              | on (free links)       | Prompt-duplication accuracy optimizer: repeats the final ask once for backends **declared** non-reasoning, on tool-less turns. Unset/`1` scopes it to free local links (it doubles input spend by design); `all` extends it to metered links; `0` disables.                                       |
| `ANGEL_OPT_DUP_MAX_CHARS`                    | `4000`                | Largest ask (chars) the optimizer will duplicate — beyond this, doubling the context costs more than the accuracy is worth.                                                                                                                                                                       |
| `ANGEL_PROVISION_URL`                        | unset (judge off)     | OpenAI-compatible base URL of the resident provisioner model — the 30B ship, e.g. `http://<turbo-tailnet>:8093/v1`. Setting it arms the pre-flight judge: metered, tool-less asks above the size floor are classified (extraction/reasoning/chat) and provisioned before any paid token is spent. |
| `ANGEL_PROVISION_JUDGE`                      | `1` (when URL set)    | Gate for the judge consult; `0` keeps the deterministic economizer only.                                                                                                                                                                                                                          |
| `ANGEL_PROVISION_MODEL`                      | `default`             | Model id sent to the provisioner endpoint (llama.cpp serves whatever is resident regardless of the id).                                                                                                                                                                                           |
| `ANGEL_PROVISION_MIN_CHARS`                  | `1500`                | Minimum final-ask length before a consult is worth its latency; shorter asks rely on the deterministic heuristics.                                                                                                                                                                                |
| `ANGEL_PROVISION_TIMEOUT_MS`                 | `4000`                | Hard wall for the consult. Any failure logs why, arms a 60s cooldown so a dark ship costs one fast timeout per minute, and the real call proceeds unmodified — strictly fail-open.                                                                                                                |

## Sessions, logging & misc

| Variable | Default | Purpose |
| ---------------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `ANGEL_SESSION_DIR`                                        | `~/.angel0/sessions`                   | Saved-thread location (`/resume`).                                                                                                                                                                                                                                                                                                            |
| `ANGEL_MISSION_DIR`                                        | `~/.angel0/missions`                   | Directory containing persisted mission snapshots surfaced in the cockpit status line. Tests and isolated deployments can point it at a separate store.                                                                                                                                                                                       |
| `ANGEL_STALL_PULSE_SECS`                                   | `20`                                  | Silence interval before the live tool strip emits a bounded stall pulse for a busy turn. `0` disables the pulse; invalid values fall back to the default.                                                                                                                                                                                      |
| `ANGEL_PROC_LOG_MAX_BYTES`                                 | `8388608` (8 MiB)                     | Maximum retained stdout+stderr capacity per `proc_run` process. The bounded pump retains two equal segments (`.log.1` plus current `.log`) after stop; `proc_status` snapshots metadata under the process-table lock, then reads a bounded tail after releasing it.                                                                                |
| `ANGEL_MEMORY_FILE`                                        | `~/.angel0/memories.json`              | Persistent `/memories` store, injected into every turn. (Tests set this to isolate from the real store.)                                                                                                                                                                                                                                      |
| `ANGEL_CONTINUAL_HARNESS`                                  | on                                    | Prime-style **continual harness**: durable supplemental `prompt`/`memory`/`skill`/`subagent` entries refined via `/refine` or the `continual_harness` tool. Injected into the system prompt and headless `--task` warm-start. Base system prompt stays immutable. `0` disables store injection and the tool's writes still error closed. |
| `ANGEL_CONTINUAL_HARNESS_TASK`                             | on                                    | Continual-harness block inside headless `--task` specifically. `0` keeps hermetic task preambles while interactive `/refine` still works.                                                                                                                                                                                                     |
| `ANGEL_CONTINUAL_HARNESS_DIR`                              | `~/.angel0/continual-harness`          | Store directory: per-repo JSON via workspace key, plus `global.json`. Tests isolate here.                                                                                                                                                                                                                                                     |
| `ANGEL_SKILLS_DIR`                                         | `~/.angel0/skills`                     | Directory of `/skills` markdown bundles (`<name>.md`).                                                                                                                                                                                                                                                                                        |
| `ANGEL_TRAJECTORY_LOG` / `ANGEL_TRAJECTORY_DIR`            | off                                   | RLVR trajectory logging + its directory.                                                                                                                                                                                                                                                                                                      |
| `ANGEL_HARNESS_ROLLOUTS`                                   | `off`                                 | Step-exact semantic policy-call capture at the real harness boundary: `off` performs no allocation or route lookup, `shadow` stores only digests/metadata, and `local` additionally stores secret-screened semantic bodies. Captures remain training-ineligible until independent reward evidence is attached. |
| `ANGEL_HARNESS_ROLLOUT_DIR`                                | `~/.angel0/harness-rollouts`           | Project-keyed private store for rollout journals, content-addressed local blobs, and immutable manifests. Audited exports are explicit: `angel --export-harness-rollout <id\|--all> <path\|->`; paths are create-new private files and `-` is the only stdout form. |
| `ANGEL_TOOL_SCHEMA_PROFILE`                                | `auto`                                | Tool-schema payload policy: `auto` advertises the discoverable coding/action core for metered root routes and bounded task/delegate runs; roomy unbounded local routes retain the full set. `essential` always advertises the core, and `full` advertises every non-deferred tool. Hidden native schemas activate through the sticky tool bubble or `tool_search`. |
| `ANGEL_BOUNDED_TASK_SCHEMAS`                               | `1`                                   | In `auto` schema mode, use only the discoverable essential set for bounded turns, including the default interactive 64-hop turn. Hidden capabilities remain available through `tool_search`. Set `0` only as a fixed-payload ablation control; explicit `full` and `essential` profiles take precedence.                                     |
| `ANGEL_SKILL_HINT`                                         | `1`                                   | Deterministically suggest at most one installed playbook when the current task matches a sanitized skill name. The bounded User-role hint never embeds the skill body or description; `0` is the causal off control.                                                                                                                          |
| `ANGEL_TOOL_BUBBLE`                                        | `1`                                   | On metered root turns, select a small hidden-tool bubble once from the startup-built local catalogue. The bubble is sticky for the turn, so stable hops keep byte-identical definitions. `0` leaves only the coding core plus manual `tool_search`. |
| `ANGEL_TOOL_BUBBLE_MAX`                                    | `3`                                   | Maximum schemas preactivated by the turn-start bubble (clamped to 8 and also bounded by `ANGEL_TOOL_SEARCH_ACTIVE_MAX`). |
| `ANGEL_TOOL_BUBBLE_CANDIDATES`                             | `16`                                  | BM25 shortlist size offered to the optional local reranker (clamped to 48). Only compact names/descriptions are shown locally; parameter schemas are never copied into the router prompt. |
| `ANGEL_TOOL_BUBBLE_ROUTER`                                 | `heuristic`                           | `heuristic` uses the zero-latency BM25 ranking. `local`/`auto` asks one reachable local utility model once per turn to rerank the shortlist, with strict allow-list parsing and automatic BM25 fallback on any failure. |
| `ANGEL_TOOL_BUBBLE_CLUB`                                   | first reachable local utility club    | Optional exact local club label or live model id for the bubble reranker. A missing/unavailable pin falls back to BM25 and never touches the metered root. |
| `ANGEL_DEBUG_TOOL_BUBBLE`                                  | `0`                                   | Emit one activity notice naming the router source and selected tools. Off by default so routing adds no transcript/UI noise; rollout attempts already capture the exact advertised schema set. |
| `ANGEL_TOOL_SEARCH_ACTIVE_MAX` | unset (unbounded) | Optional operator limit on hidden schemas activated during a turn. Automatic preactivation remains a small shortlist; subsequent `tool_search` calls can activate any further available tools by default. Names and summaries stay in tool results; native schemas arrive on the next request. Explicit `0` preserves text-only discovery. The former default eight-tool ceiling and hard maximum of 32 are removed. |
| `ANGEL_OPENROUTER_ANTHROPIC_CACHE`                         | `1` for OpenRouter Claude             | Add explicit ephemeral cache breakpoints for OpenRouter Claude requests: one on a sufficiently large first System message, plus a moving breakpoint on the last plain-text message so the growing conversation body caches too (Anthropic checks earlier breakpoint positions on the next request, turning full-history re-sends into cache reads). Tool-role tails are skipped and both markers respect the size floor. Local and non-Claude request bodies remain byte-identical; set `0` for one-shot/cold-write ablation.                                                                                                                  |
| `ANGEL_ANTHROPIC_CACHE_PREFIX_FLOOR`                       | `1024`                                | Approximate minimum tokens before an explicit OpenRouter Claude breakpoint is emitted (clamped to 256–65,536): gates the System marker by prefix size and the moving tail marker by total body size.                                                                                                                                                                                                                 |
| `ANGEL_ANTHROPIC_CACHE_TTL`                                | `5m`                                  | Explicit cache lifetime: default/unknown values use provider-default 5 minutes; exact `1h` requests the supported one-hour TTL.                                                                                                                                                                                                               |
| `ANGEL_TASK_ACCEPT_CMD` / `ANGEL_TASK_ACCEPT_TIMEOUT_SECS` / `ANGEL_TASK_ACCEPT_REJECTIONS` | off · `120` · `2` | Optional pinned deterministic completion contract for headless action runs. It arms only when red before the first model call, reruns after effectful tool hops, rejects zero-test libtest output, and ends the turn immediately when green. A final answer cannot bypass a still-red contract: unchanged red is replayed without spawning another process, and 1–4 repeated false completion claims stop the turn (default 2). Timeout clamps to 5–600 seconds. |
| `ANGEL_TASK_RENDERED_REQUIREMENT` | `0` | Operator-owned rendered-output requirement for `--task` / `--task-json`. `1`/`on`/`true`/`yes` (or `--require-rendered-output`) attaches `angel-rendered-acceptance/v1` as **unverified** even when `accept_cmd` is disabled on the public task path. Model prose, unit/build success, and arbitrary repo JSON cannot promote it to accepted; there is no trusted positive rendered path. Explicit activation is not automatic media verification. |
| `ANGEL_IMAGE_PROTOCOL`                                     | auto                                  | Image protocol for resident agent portraits and the explicit foreground portrait viewer (`kitty`, `sixel`, `iterm2`, or `halfblocks`). Scryglass still images use native foreground graphics with a halfblock fallback, including source-detail zoom/pan inspection. World views use generated Dotmax dots or ordinary Braille; video retains its existing optional playback path, not the still inspector. The executable gate covers the local native-PTY halfblocks path; it does not separately release-qualify SSH transport or tmux. |
| `ANGEL_WEBGPU_PORTAL`                                      | on (off on Darwin)                    | Show the bounded WebGPU activity portal in the Agent pane when the calibrated image protocol is Kitty and the isolated renderer is available. Darwin defaults off: the renderer crate is vulkan/gles, not Metal, and its first compile can fail a rustup rust-src download. Set `1` to force a portal rebuild; `0` disables it. The terminal-native Miniviz remains the fallback. |
| `ANGEL_BACKED_MAP` (`ANGEL_TILE_MAP` alias)                 | on                                    | Draw World Map and Workshop as the authored SNES Realm background plus independent landmark, hero, and live-state sprites. `0`, `off`, `false`, or `no` forces the braille map back. The braille map is also used automatically on terminals without a real graphics protocol (kitty, sixel, or iterm2); halfblocks falls back because its resolution cannot hold the map. The detected protocol is reported on the GRAPHICS line at startup. |
| `ANGEL_PORTAL_RENDERER`                                    | launcher-built sibling                | Optional exact path to the isolated `angel-portal-renderer` executable. The normal launcher builds and exports this automatically; an absent, invalid, failed, or timed-out renderer falls back without blocking the Cockpit.                                                                                                             |
| `ANGEL_SCRYGLASS` | `1` | Set `0` (also `off`, `false`, `no`) to remove the Scryglass/Realm side column and return its columns to transcript + composer at every geometry. `/world off` and `/world on` override visibility for the running session, like `/world view`; they do not change the selected renderer or write launch settings. `ANGEL_BACKDROP=off` independently suppresses side panels. `/show` queues media while hidden and reports the minimum 100-column, 30-row layout. `angel --build-info --json` reports `video_decode` from the compiled `scryglass-video` feature; `/show` notices repeat it. |
| `ANGEL_WORLD_3D` | retired compatibility flag | Ignored for renderer selection. All values, including `0`, empty, false-like values and typos, retain Dotmax 3D outdoors. Use `ANGEL_SCRYGLASS=0` or `/world off` to hide the pane. |
| `ANGEL_WORLD_VIEW` | Dotmax 3D | Dotmax is the sole outdoor renderer at startup, on Realm/Explore, and after `/cd`. `3d`/`mesh`/`mesh3d`/`dotmax` name it; retired `top`/`topdown`/`top-down`/`2d`/`journey`/`living`/`raycast`/`ray`/`ambient`/`art` values cannot select another style. Unset and unknown values also retain Dotmax. `/world view`, `/world 3d`, and Explore `v` report this view; named compatibility commands select Dotmax. Explicit Enter opens a Dotmax conversion of the retained location artwork, and Leave returns to the Dotmax outdoor scene. `ANGEL_TUI_MOTION` still controls localized room animation. |
| `ANGEL_DOTMAX_PITCH` | `2` | Generated world-dot spacing in physical pixels on Kitty: `2` through `8` use equal horizontal and vertical pitch with one-pixel dots. The source is composed Dotmax/Braille cells, never a native world PNG. `0`, `text`, or `off` uses ordinary terminal Braille. Other protocols, tmux, and oversized canvases fall back to Braille; surrounding text and controls retain their normal font size. |
| `ANGEL_WORLD_INK`                                          | `rgb444`                              | World/ride paint color quantizer for SGR run coalescing (terminal-parse cost). Default `rgb444` (4 bits/channel, max error 8/255) cuts truecolor headers further after the 2026-07 RGB555 wave. `rgb555`/`555`/`snes` restores the gentler SNES-word quantizer (error ≤4). `full`/`off`/`0`/`exact` leaves exact truecolor (debug only — emulator parse cost balloons). Media stills and video keep exact ink. Read once at launch in release. |
| `ANGEL_WORLD_CINEMATIC`                                    | obsolete                              | Retired and ignored. A guard test proves it cannot replace the always-on Scryglass world.                                                                                                                                                                                                                                                       |
| `ANGEL_WORLD_FP` | obsolete | Retired and ignored. Scryglass routes own visibility and camera interaction; outdoor and entered room/location views both render composed Dotmax; source plates are converted and never displayed directly. |
| `ANGEL_LAYOUT_DIR`                                         | `~/.angel0/layouts`                    | Saved module-layout store for the runtime workbench. (Tests point this at a tempdir.)                                                                                                                                                                                                                                                         |
| `ANGEL_OBSERVATORY_MANIFEST`                               | `~/.angel0/reports/manifest.json`      | Explicit observatory report-manifest path. When unset, `OMNISPACE_REPORT_DIR/manifest.json`, then the live default are tried; the bundled 3-report test fixture is read only when `ANGEL_OBSERVATORY_DEV_FIXTURE` is set (see below).                                                                                                             |
| `ANGEL_OBSERVATORY_DEV_FIXTURE`                            | *(unset)*                             | Explicit opt-in to the bundled 3-report test fixture as the observatory catalog when no live manifest exists. Never automatic — in every build profile an absent manifest surfaces as an empty "no catalog" state that names the missing path, so fixture reports are never presented as the operator's real catalog.                            |
| `ANGEL_AGENT_INFO_HEADER`                                  | off                                   | Restore the 6-row host/telemetry info header above the agent bay (identity, "micro-lab agent" detail, C/M/G metrics, token meter). Removed from the default layout 2026-07-22. The roomy default now centers the avatar in the top chrome bar, stacks identity/status on the left, and keeps MODEL/THINK on the right; the bay is caption + thought flow. This flag restores the old split instead of that bar.                                                                                                                                                                       |
| `ANGEL_PORTRAIT_STATES`                                    | on                                    | Stateful knight-helm portraits: live thinking, tool use, approval wait, settled and idle poses. Thinking at at least 80% reported context usage has a distinct long-context pose; no known capacity means no invented pressure. Full motion adds infrequent idle glances, Reduced/Off keep idle still. Existing state/border signals remain. Set `0` for the prior effort-based portraits and caption/border behavior. Read once at launch.                                                                                                                                                                                                                                                                   |
| `ANGEL_VIZ_SEAT_STATES`                                    | on                                    | Per-seat live status telemetry in agentviz (muster banners, portal return pips). Set `0`/`off`/`false`/`no` to suppress seat-state publishing (the stage-level muster keeps working). The gate speaks once on stderr when it suppresses.                                                                                                                                                                                                                                                            |
| `ANGEL_TUI_MOTION`                                         | `full`                                | Display-only motion policy: the bottom Dotmax bar keeps a dim catalog loop while idle, after tool results, and beside stall warnings (5 fps `full`, 1 fps `reduced`, frozen `off`). It pauses during explicit reading/selection or lost terminal focus; comp mode skips idle ambience. `full` also runs the 12 fps tourney/card timelines, immediate transcript arrival emphasis, overflow-only secondary note rolling, and gold-coin milestone toss; `reduced` uses restrained transitions, a one-cell coin lift (no flip or rolling text), and chrome clocks/load at most 1 Hz; `off` freezes clocks, elapsed counters, and non-transcript chrome while idle — they update only on user input, with no 33 ms animation tick.                                         |
| `ANGEL_TERM_PIPE`                                          | off                                   | Tee everything written to the terminal to a path: a FIFO gets a live mirror, a regular file an appended session recording.                                                                                                                                                                                                                    |
| `ANGEL_TERM_PIPE_CAP`                                      | `2048`                                | Tee channel depth in chunks (backpressure bound so a stalled reader can't wedge the UI).                                                                                                                                                                                                                                                      |
| `ANGEL_DOC_STRICT`                                         | off                                   | When set, the `env_knob_doc_completeness` test fails on any undocumented `ANGEL_*` knob. Off by default, so doc drift is a non-blocking heads-up during interactive work; set it for a release/CI gate.                                                                                                                                       |
| `ANGEL_PTY_SMOKE_STRICT`                                   | off                                   | Set to `1` to make the real-terminal cockpit smoke fail when a native PTY cannot be allocated instead of reporting a truthful local skip. `npm run test:cockpit-pty` enables it; CI is strict automatically.                                                                                                                                     |
| `ANGEL_GLYPHS`                                             | unset                                 | Unset keeps two-cell ASCII state marks but Unicode box-drawing chrome. Set to `ascii` for a 7-bit table (borders `+-|`, separators `-`, ellipsis `...`, bullets `*`, return `<-`; world panes become `[world: ascii mode]`). Set to `rich` for the fixed-width glyph language.                                                                                                                                                                                    |
| `ANGEL_OVERWATCH_CMD`                                      | `~/.local/bin/overwatch` | Override the command used for local CPU/GPU/memory telemetry.                                                                                                                                                                                                                                                                                 |
| `ANGEL_OVERWATCH_GPU`                                      | off                                   | Enable `nvidia-smi` GPU utilization and memory sampling in local overwatch telemetry.                                                                                                                                                                                                                                                         |
| `ANGEL_LIVE_ORCH` / `ANGEL_LIVE_REINFORCE`                 | off                                   | Live orchestration / reinforce-loop wiring (experimental).                                                                                                                                                                                                                                                                                    |
| `ANGEL_LIVE_CALIBRATE` | off | Opt-in gate for the live deli evidence-regime calibrator (`live_deli_evidence_calibration`, `#[ignore]`d). Runs the real `DeliClub` over the configured cheap links and reports admitted findings, open leads, untagged bullets, and fabricated citations per worker. It asserts zero fabricated citations — a tool-less worker citing `file:`/`benchmark:` invented the reference — so run it after touching `iterate.rs`, the deli contract, or the regime split, and whenever a worker model changes underneath us. Skips cleanly when no cheap link is configured. |
| `ANGEL_CALIBRATE_GLM_MODELS` | `glm-5.3,glm-5.3-flash,glm-5.2` | Comma-separated GLM model ids the calibrator samples, so a calibration is not one model's quirk. LongCat contributes its single `LongCat-2.0` surface alongside these. |
| `ANGEL_PHONE_` (prefix)                                    | —                                     | Phone-a-model escalation config.                                                                                                                                                                                                                                                                                                              |
| `ANGEL_CONTINUAL_HARNESS_MAX_PER_KIND` / `ANGEL_CONTINUAL_HARNESS_MAX_REFINEMENTS` | `6` / `5` | Maximum injected entries per kind and refinement records shown per entry in the supplemental context block. Store validation keeps its separate hard bounds. |
| `ANGEL_CONTINUAL_HARNESS_MAX_CONTENT` / `ANGEL_CONTINUAL_HARNESS_MAX_BYTES` | `180` chars / `3072` bytes | Per-entry display excerpt and total rendered continual-harness context caps. These bound model-facing context, not the validated on-disk record. |
| `ANGEL_HARNESS_ROLLOUT_REQUIRED`                           | off                              | Fail the turn before or during execution when requested rollout capture is disabled, cannot start, or cannot be sealed. `angel --task-json --require-rollout` sets this for the child run; it never silently downgrades to an uncaptured attempt.                                                                                                                                                                                                                                                                                                                        |
| `ANGEL_HELM_REVIEW_DIR` | unset | Test-only directory for exact consumed 192px helm crops from `helm::tests::every_consumed_sheet_has_distinct_framed_poses`. Ordinary execution never reads it. |
| `ANGEL_BACKED_MAP_DUMP` / `ANGEL_BACKED_VISTA_DUMP` / `ANGEL_BACKED_INTERIOR_DUMP_DIR` | unset | Test-only visual review outputs. When the corresponding ignored/review test runs, the first two name exact image files and the last names a directory for interior frames. Ordinary cockpit execution never reads them. |
| `ANGEL_TUI_MOUSE`                                          | on                               | App-owned mouse capture for pane controls, transcript selection, and scrolling. `0` leaves pointer selection and scrollback to the outer terminal; keyboard and bracketed paste remain active.                                                                                                                                                                                                                                                                                                                                                                          |
| `ANGEL_TUI_ATTENTION`                                      | on                               | Emit one coalesced terminal BEL for a completed turn/job, provider failure, vanished worker, or approval request only while the terminal is unfocused. `0` keeps attention silent.                                                                                                                                                                                                                                                                                                                                                                                       |
| `ANGEL_TURN_IDLE_TIMEOUT_SECS` | `0` (off) | Shared interactive/headless turn watchdog. No turn event or recent owned foreground child activity for this many seconds cancels owned work; stdout/stderr or sampled Linux process-group CPU consumption within the existing tool idle floor resets the clock. A live but silent sleeping child does not count. Tool hard timeouts and the turn deadline still apply; the result envelope names `idle_timeout`. `0` disables it. Raise it explicitly for legitimate long silent work. |
| `ANGEL_STEER_IDLE_INTERRUPT_SECS`                           | `60`                             | When operator steers are queued and the provider stream has made no progress for this long, interrupt the request (keeping the steers) so they are delivered as the next message instead of waiting for the idle timeout; `0` disables.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `ANGEL_PHONE_DEEPSEEK_URL` / `ANGEL_PHONE_DEEPSEEK_MODEL` | DeepSeek API / `deepseek-v4-pro` | Exact phone overrides for the default external DeepSeek second opinion. The URL/model fall back to `ANGEL_DEEPSEEK_*`, then the shown built-ins; the key follows the dynamic `ANGEL_PHONE_<KEY>_KEY`, provider key, then `ANGEL_BRAIN_KEY` policy. |
| `ANGEL_PHONE_SPARK_URL` / `ANGEL_PHONE_SPARK_MODEL` / `ANGEL_PHONE_SPARK_R1_URL` | unset | Exact examples for the local code and reasoning phone aliases. These fleet routes have no compiled endpoint; configure the phone URL (or matching `ANGEL_SPARK_*`) explicitly. With no model pin, the endpoint's live model discovery remains authoritative. |

> Note: `ANGEL_TAG_STYLE` (in `transcript.rs`) is an internal transcript-styling
> constant, not an environment variable — it's named with the `ANGEL_` prefix for
> readability and takes no configuration. Likewise `ANGEL_SWARM_RED` (in
> `swarm_compile/schema.rs`) is the swarm compiler's red-marker string prefix
> (`ANGEL_SWARM_RED:<id>`), not a knob.

> `ANGEL_INTERNAL_SANDBOX_POLICY`, `ANGEL_INTERNAL_SANDBOX_BACKEND`, and
> `ANGEL_INTERNAL_SANDBOX_LIFECYCLE` are private handoffs from the
> multithreaded cockpit to its fresh single-threaded `--sandbox-exec` helper:
> the serialized policy, the backend copied from `ANGEL_SANDBOX_BACKEND` at
> spawn time (so a tool payload cannot select a weaker outer domain after an
> `env_clear`), and `attached`/`detached` lifecycle metadata for trusted
> process tools that may outlive the cockpit. angel0 sets them on the helper
> child; they are not supported operator controls and setting them by hand
> has no effect on confinement.

> Test-only fixture names: the source's test modules reference `ANGEL_EMPTY`,
> `ANGEL_MISSING`, `ANGEL_FAKE_API_KEY`, `ANGEL_EXPERIENCE_DEFINITELY_UNSET_XYZ`,
> `ANGEL_MOA_OVERRIDE_PROBE_MAX_TOKENS`, `ANGEL_CAP_MARKER_PROBE_MAX_TOKENS`,
> `ANGEL_MOA_DEFAULTS_PROBE_MAX_TOKENS`, `ANGEL_MOA_PLAIN_PROBE_MAX_TOKENS`,
> `ANGEL_DEEPSEEK_V4_PRO_MAX_TOKENS`, `ANGEL_PRACTICE_MAX_TOKENS`,
> `ANGEL_TEST_MAX_TOKENS`, `ANGEL_CACHE_PIN_PROBE_PROMPT_CACHE`,
> `ANGEL_CACHE_STABLE_PROBE_CACHE_STABLE`,
> `ANGEL_STREAM_USAGE_PROBE_STREAM_USAGE`, and
> `ANGEL_STOP_PROBE_STOP_SEQS` as isolated probe keys or serialized-provenance
> fixtures. They are not additional named controls beyond the documented
> `ANGEL_<CLUB>_MAX_TOKENS` / `ANGEL_<CLUB>_STOP_SEQS` /
> `ANGEL_<CLUB>_CACHE_STABLE` / `ANGEL_<CLUB>_STREAM_USAGE` rules.

Secondary tool-strip notes roll only when they overflow, dwelling one second at
each end. Explicit transcript focus (F2 or a transcript click), scrollback,
selection (including draft text), or terminal blur pauses them. Clicking the
composer resumes motion; default Core module focus does not pause typing.
Reduced/off motion keeps a static ellipsis. `/trace` copies the current complete
note into the scrollable transcript before enabling future full trace output.
Tool-strip dotmax lanes distinguish pending thinking, running tools, verification,
agent dispatch/rendezvous, stream silence, and correlated terminal results.


## Test fixtures and receipt markers

These names are recognized by the source-literal documentation check. They are
not additional interactive model controls.

- `ANGEL_CODING_TRAINING_FIXTURE_EXPORT` and
  `ANGEL_NATIVE_TRAINING_FIXTURE_EXPORT` select explicit temporary export roots
  for ignored coding-training fixture tests. Unset by default.
- `ANGEL_EXPERIMENT_SNAPSHOT_SOURCE` selects the explicit active source path for
  the ignored experiment snapshot test. Unset by default.
- `ANGEL_NATIVE_USAGE_RECORDED_ENVELOPE` optionally supplies a recorded envelope
  to the native HTTP usage fixture. Unset by default.
- `ANGEL_NATIVE_USAGE_FIXTURE`, `ANGEL_NON_HTTP_USAGE_FIXTURE` and
  `ANGEL_SUPPLEMENTAL_CHAT_USAGE_FIXTURE` are printed receipt prefixes in usage
  contract tests, not environment settings.
- `ANGEL_OWNED_TEST_KEY`, `ANGEL_OWNED_TEST_SECRET` and
  `ANGEL_OWNED_TEST_TOKEN` hold synthetic credentials in redaction tests.
- `ANGEL_PRIVATE_CUSTOM_MODEL_MAX_TOKENS` is a negative-control override in the
  custom-model context-window test; arbitrary environment names cannot invent a
  supported model context limit. `ANGEL_META_MAX_TOKENS` appears only as an
  illustrative short numeric value in a redaction comment.
- `ANGEL_SOTA_SOLO`, `ANGEL_TASK_ID` and `ANGEL_RUN_ID` are deliberately injected
  parent-state values in delegated-lineage tests. They do not replace the
  delegate's own bounded identity and execution contract.
- `ANGEL_AUTO_COMPACT`, `ANGEL_ROLLOUT_CAPTURE` and `ANGEL_MOA_MODE` are
  compatibility settings injected by fixtures; these exact spellings have no
  active production reader. They do not control compaction, capture or routing.
- `ANGEL_J_AUTH_SECRET` and `ANGEL_J_WRITER_SECRET` hold synthetic credentials
  in auth/error-redaction and persisted-output redaction tests, respectively.
  They are not provider credentials or operator authentication settings.
- `ANGEL_J_WRITE_AUTH_MATRIX`, when present, lets the auth/TLS fixture write its
  Markdown matrix to its fixture evidence directory. Unset by default; not a production logging toggle.
- `ANGEL_NATIVE_VIDEO_FIXTURE` optionally supplies an existing MP4 to the
  feature-gated native-video stage test; the supplied file is not overwritten
  or removed. Unset generates a controlled temporary clip.
- `ANGEL_NATIVE_VIDEO_PROTOCOL` selects `halfblocks` (default) or `iterm2` in
  that test only. Use the documented image-protocol control for the product.
- `ANGEL_NATIVE_VIDEO_DUMP` optionally names that test's PNG output: decoded
  native-protocol pixels or the actual halfblock TestBackend raster. It does
  not capture or prove a physical terminal display. Unset writes no dump.

### Feature receipts, price estimates and sandbox diagnostics

| Variable | Default | Purpose |
| --- | --- | --- |
| `ANGEL_FEATURE_DEFAULTS` | built-in relevant mode; optional local receipt | Override the TOML feature receipt used by Caddy's default-mode resolver. The last `[[features]]` entry named `caddy` supplies `default`; `full`/`off` select those modes, otherwise relevant mode applies. Missing/unreadable/unparseable receipts retain the built-in default. This is not a general feature-toggle API. |
| `ANGEL_PRICES_FILE` | unset | Override the trace ledger's TOML `[[prices]]` table. A readable override wins; otherwise use `~/.angel0/prices.toml`, then the build-embedded table. Readable but invalid/unpriced rows remain unknown, not zero cost. Re-read per record, with the selected source recorded. These are list-price estimates or nominal subscription fees, not observed account charges or remaining quota. |
| `ANGEL_SANDBOX_TRACE` | off | `1`, `true` or `TRUE` emits helper phase/PID diagnostics on stderr and the full hardlink-scan digest receipt instead of its compact counts. Can enlarge captured tool output; it changes diagnostics, not confinement or sandbox authorization. |

### Run identity build metadata

`ANGEL_BUILD_RUSTC` is the complete `rustc -vV` output captured by `bin/angel0` at build time (including host triple). `ANGEL_BUILD_PROFILE` is the launcher-selected build profile. Both are compiled into build/run identity; absent values remain `unbound`. They do not select a runtime route or constrain a run.

- `ANGEL_GO_BIN`: Optional absolute native Go executable pin for typed `run_tests` (`runtime: go`). Task-writable paths and scripts are refused; the default probes system Go installations.

### Unproductive streak escalation

- `ANGEL_UNPRODUCTIVE_STREAK_ESCALATE`: completed-hop interval for ledger/notice/request-steer nudges. Metered SOTA turns default to `8`; other routes default to `0`/off. Explicit values, including `0`, override the default. The progress ledger measures verifiable progress rather than imposing a fixed read-count threshold, so long-form proof, profiling, and repository diagnosis can continue when they are producing new evidence.
- `ANGEL_UNPRODUCTIVE_STREAK_STOP`: optional stop with `escalated_unproductive` and a diagnosis after this streak length, only after an earlier escalation with no intervening progress. It defaults to `0`/off for every route; setting a positive value is an explicit operator choice. On non-metered routes the explicit stop applies only when `ANGEL_TASK_ACTIVE=1`. Competition mode never stops on this policy. The diagnosis includes `last_credited_progress` (kind and hop, or none); that field is also retained in the trajectory ledger and task envelope.

### Explicit headless practice

`ANGEL_PRACTICE=1` permits the offline practice driver in headless task/ask mode.
Without this exact opt-in, a practice route exits 2 with `error.kind=no_route`.
The opt-in is recorded in `identity.model.id` as `practice (ANGEL_PRACTICE=1)`.
Interactive mode retains its visible practice fallback. Configure the selected
provider's key/URL/model and `ANGEL_DRIVER`; `/connect` explains setup.
`ANGEL_API_CLUBS` is an optional user restriction.
### Measured model defaults

The cockpit embeds `docs/telemetry/model-calibration.toml`. Exact model IDs select
measured effort and optional stream-stall/turn-idle values. An unknown model keeps
provider behavior (45-second stream stall and 600-second turn idle). Explicit
per-driver effort overrides global effort; both override the table. Explicit
stream/idle environment values override the table, including zero to disable.
CLI/THINK effort overrides retain their existing priority.

`~/.angel0/model-calibration.toml` is loaded once per process. Its `[[models]]`
entries replace matching embedded entries; other embedded entries remain. Each
entry needs `model`, `receipt`, `reason`, and `date` (or a top-level date), with
optional `default_effort`, `stream_stall_secs`, and `turn_idle_secs`. Missing fields
in a replacement use provider defaults. Invalid override files fall back to the
embedded table with a diagnostic. Restart to load a promoted table.

`identity.budgets` records resolved values, sources, notes and receipt;
the headless driver line prints the resolved controls. Source `env` also denotes
an explicit CLI/THINK override stored by the route.

### Opt-in formation budgets

`ANGEL_FORMATION_TOKEN_BUDGET` and `ANGEL_FORMATION_WALL_SECS` are unsigned
integer allocations for turn-shared bookkeeping. Unset means no allocation;
zero still admits requests. Identity budgets add these keys when configured.
Each HTTP attempt reserves a conservative wire-byte prompt estimate plus output
allowance, then settles C06 normalized usage. Share sizing retains a useful output
floor even when allocation is exhausted. Unknown usage retains an estimated
charge and a null reported spend; it never stops admission.

The task envelope and graph episode retain the allocation ledger, signed
`remaining`, `over_allocation` and peak `over_allocation_tokens`; the ledger also
reports `current_over_allocation_tokens`. Peak observations survive settlement.
Compatibility fields `reservation_denied` and `budget_exhausted` mean charged
spend exceeded allocation, never that a reservation was denied or work stopped.
Wall overruns set `wall_allocation_exceeded` without setting a provider deadline.
Explicit graph/provider deadlines remain separate. Arithmetic saturation is
marked `accounting_overflowed`; such totals cannot qualify exact-spend evidence.
Providers without an established C06 counting contract cannot qualify equal-token
cohort evidence.

### Tool stdin and idle escalation

Tool sandbox commands default stdin to `/dev/null`; a tool that supplies input may
explicitly override that default. `ANGEL_TOOL_IDLE_SECS` is an opt-in positive
number of seconds without stdout or stderr before a recoverable `tool_idle`
escalation, unless the owned foreground process group has recent output or
sampled Linux CPU consumption. Unset, invalid, or `0` disables this heuristic;
the separate turn idle policy no longer silently creates a tool-kill policy.
The child tree is killed and the tool fails; the turn records `kind: tool_idle`
and the hop in `escalations[]` and lets the model recover. This applies in YOLO
as well. The turn watchdog allows three seconds for a due tool escalation to
settle before its fallback cancellation. Existing earlier timeout policies still
apply. A drain that expires after child exit reports `grandchild_holds_stdout`
or `grandchild_holds_stderr`, with capture explicitly incomplete.

### Verifier runtime pins

`ANGEL_NODE_BIN` and `ANGEL_PYTHON_BIN` select absolute runtime executable paths.
Typed verification additionally requires native images outside task-writable
roots. `ANGEL_CARGO_BIN` selects an absolute direct toolchain `cargo` binary
beside `rustc`, not a rustup proxy; the existing trust and Cargo configuration
checks still apply. Invalid explicit pins do not silently fall back. Restart
Angel after changing a pin. Missing runtimes report `runtime_missing` with the
runtime, lookup description, and installation/pin hint.

### Interactive frame timing

`ANGEL_FRAME_TIMING_LOG` is unset/off by default. Set it to a writable file to
append completed interactive terminal draws as TSV: elapsed microseconds since
logger creation, draw/flush microseconds, and microseconds since the previous
completed draw (`-` on the first row). A comment header supplies the approximate
Unix epoch origin in microseconds. Gaps include event polling, event processing,
and telemetry writes; draw cost excludes the telemetry write. These are product
CPU/terminal-write timestamps, not physical display presentation times. The
synchronous small write adds observer overhead. No terminal text is recorded;
open/write failures are ignored, and the operator owns file retention. The
resize-storm harness copies this scratch log into receipt chunks below 1 MB.

### Graph budget diagnostics

- `ANGEL_GRAPH_BUDGET_TRACE=1`: emit numeric reservation, denial, and settlement JSON to stderr, including node role, requested tokens, remaining tokens before/after, and released allowance. Off by default; contains no prompt or response bodies.

### Startup workspace indexing

`ANGEL_STARTUP_WALK_MS` sets the optional startup workspace-index wall-time allowance in milliseconds (default `500`; `0` returns unavailable/partial evidence immediately). Git enumeration honors `.gitignore` and `.git/info/exclude`, with a 16 MiB metadata capture allowance. Checks between file reads return an explicitly partial index with path counts, elapsed time, and a separate partial digest; a partial digest is never a complete tree identity or fresh-verification claim. The allowance applies to startup indexing, never to the model turn or exact verifier evidence. Filesystem calls and subprocess cleanup can exceed the cooperative deadline.
- `ANGEL_TURN_PHASE_TRACE=1`: emit payload-free `[turn-phase]` stderr breadcrumbs with PID, process-local turn id, monotonic elapsed milliseconds, and the phase being entered. Covers worker startup, context/preflight, provider dispatch/return, tools, hop end, and steer drain; disabled by default. `request_sent` marks entry to the provider adapter, not proof of an HTTP request; correlate with provider receipts. The last entered phase locates an outstanding step. Redirect stderr when running in a terminal to retain traces without disturbing the screen.
`ANGEL_INTERNAL_SANDBOX_STATUS_FD` is a private inherited anonymous-file descriptor set by the execution collector. The single-threaded helper sends sandbox attribution and confinement failures, then closes it before executing a tool. It is not an operator setting. `angel --doctor` probes local Bubblewrap user namespaces and prints host remedies; ordinary denied-userns execution uses Landlock-only confinement with bounded alias copying and explicit unprotected-alias receipts.

Landlock-only alias privatization attempts at most 64 files and 64 MiB per helper launch. Copy errors, exhausted copy allowance, and unscanned subtrees are retained in `aliases_unprotected`; the command proceeds with a transcript notice. Existing shared-temp scan exclusions remain visible in the scan receipt. A command’s own nested Bubblewrap failure is an ordinary tool failure with a doctor hint; only helper-channel Landlock setup failure pauses automatic loop continuation pending host repair.

## L01 unattended-run policy (2026-09-10)

Run caps are operator opt-ins. Headless startup now installs `ANGEL_MAX_HOPS=0`
and `ANGEL_TURN_DEADLINE_SECS=0` only when absent, preserving explicit CLI/env
values. Descendant turns also default to unlimited hops. The task prompt asks
for all requested deliverables after a green verifier rather than forcing an
immediate answer.

`ANGEL_SPIN_LIMIT`, `ANGEL_ERROR_LIMIT`, `ANGEL_POST_GREEN_TOOL_BATCHES`, and
`ANGEL_UNPRODUCTIVE_STREAK_STOP` default to 0 (off). Spin/error/streak
diagnostics continue. Competition turns never stop on the unproductive-streak
policy even when a stop value is explicitly configured.
`ANGEL_PROVIDER_RETRIES` is unbounded when absent; an explicitly supplied integer
retains the bounded retry policy. Default provider failures retry the same hop
with cancel-aware backoff and one notice per attempt; permanent account/config
failures (authentication, invalid request/model, billing, exhausted long-window
quota) still fail after one attempt with an actionable message instead of
looping, and a stream that dies without a terminal event is always recoverable
whether or not it had already streamed partial text. A permanent failure
outranks that classification even when the same error carries stall context. A
tool-bearing agent-graph node recovers inside its own turn, so the graph retries
only toolless nodes and an explicit retry budget is never spent twice. A replay
is a fresh provider request: output streamed on the failed attempt is not free,
and the ledger keeps every attempt.

`ANGEL_DESCENDANT_CALL_BUDGET` defaults to 0 (unbounded); explicit values 1–1024
retain cumulative admission. `ANGEL_SPAWN_TIMEOUT` and spawn `timeout_secs`
default to 0 (unbounded); positive values are honored without silent clamping.
`ANGEL_SWARM_WAVE_DEADLINE` defaults to 0; quorum still completes a wave.
`ANGEL_GRAPH_DEADLINE_SECS` already defaults to 0.
`ANGEL_TURN_IDLE_TIMEOUT_SECS` is off by default, including calibrated model
routes; explicit positive values still abandon an idle turn.
Ordinary handoff RL launches have no duration/token/roll caps. Explicit Podrace
selection retains its five-day profile. Campaign round defaults are unbounded
(`policy.max_rounds=0`); round history no longer creates a 128-round stop.

## angel0 on Omarchy

`ANGEL_VIDEO=0` builds the supported terminal configuration without FFmpeg decoding. The Omarchy launcher selects it while the FFmpeg 9 binding issue is open. `ANGEL_*` configuration keys and runner schemas remain compatible. Runtime user state now uses `~/.angel0`; the first-run archive under `~/.angel` is untouched.

### Still-image inspection (Scryglass)

Open an existing local image through the artifact list/Scryglass media reveal.
Click its image scene to focus and pin it. Wheel zooms about the pointer; left-drag
pans. Right-click or **Fit** resets. The compact footer has **−**, **+**, **Fit**,
**Back** and a zoom percentage relative to Fit (not a native-pixel 1:1 claim).
With Scryglass focused and the composer **empty**, `+`/`-` zoom, arrows pan, and
`0` fits. Drafts and slash commands retain their keys. **Esc** / **Back** closes
the overlay and returns to the previous world route; active-turn Esc still stops
work first. F4 uses the existing Scryglass focus layout.

Inspection pins once: automatic reveal timers/tools cannot replace the inspected
image. First-load hit targets are disabled. While **updating**, the last painted
same-source/request/geometry image remains visible and owns pointer coordinates;
wheel/key/drag input accumulates into the desired view, not dropped encoder ticks.
Release a drag anywhere to end it; resize, focus loss, modals and route exit also
cancel capture. Footer **Fit 100%** / **N% inspect (of Fit)** is relative to Fit,
not a native 1:1 claim. These are image controls, not world navigation.

Source detail is sampled off-thread from one bounded decoded image, not the
portrait thumbnail cache. Area-coverage downsampling antialiases thin strokes;
fractional bilinear magnification does not copy source crops. Sampling allocates
only the <=4MP RGBA output (<=16MiB) and constant scratch, in addition to the one
<=128MiB decoded source; encoding/retained protocol costs scale with output, not
source dimensions. The existing 64MiB encoded / 32MP source limits still apply.
Native still graphics use the safe terminal window pixel ioctl, refreshed every
draw with the **same local picker** for encoding and cache/input geometry. Zero
or unavailable physical geometry honestly falls back to 2×4-density halfblocks;
there are no new stdin escape probes or global picker/environment mutations. Video is **existing optional playback only**, not
part of these inspection controls.

For reproducible display-only verification, `ui_verify` accepts
`{"operation":{"op":"inspect_image","action":"zoom_in"},"scope":"scryglass"}`;
actions are `zoom_in`, `zoom_out`, `left`, `right`, `up`, `down`, `fit`. This never
opens a file. `ui_inspect` reports `scryglass.image_inspector`; wait for
`loading:false` and a non-null `viewport` for the latest desired view. During
`status:"updating"`, `painted` gives the old displayed zoom/pan, while top-level
zoom/pan describes the cumulative target. A non-null viewport still authorizes
pointer gestures on that same scene; old source/request/geometry is never reused.

### Sandbox test fixtures

`ANGEL_TEST_NETWORK_ALLOWED`, `ANGEL_TEST_ROUNDTRIP`, and
`ANGEL_TEST_PRELOAD_MARKER` are private inputs to the Bubblewrap integration
tests. They check network policy, exact child-environment round trips, and
loader-injection isolation respectively. They do not configure the cockpit.

### World raster pitch

- `ANGEL_WORLD_DOT_PX`: world sprite raster sample spacing, `3` or `4`
  (default `4`) **physical terminal screen pixels**, only when native graphics
  and terminal-reported cell pixel geometry are available. Not pixels per tile,
  font points, or logical desktop/DPI pixels. Camera zoom is independent.
  Transport is capped at 1536×960; oversized panes report `bounded`. Missing
  geometry reports `pitch unknown`; halfblocks report cell-based fallback, not
  physical dot control. Does not alter chat, agent, still inspector, or video.
  Ordered RGB Bayer uses a fixed pattern (no temporal jitter). Worker timings
  are emitted at debug target `world_raster` (compose and sample/encode µs).

## Worker and resource locations

| Variable | Default | Purpose |
|---|---|---|
| `ANGEL_RESOURCE_DIR` | detected source checkout or installed bundle | Root containing shipped `scripts/`, `cockpit/` and their dependencies. The source launcher sets this to its own checkout. |
| `ANGEL_BUILD_RESOURCE_SHA256` | unset compile-time value | Release resource identity; the archive build binds its entry manifest into the executable. |
| `ANGEL_HABITS_DIR` | `~/.angel0/habitsmith` | Shared native/worker Habitsmith status and verdict store. |
| `ANGEL_STILL_DIR` | `~/.angel0/still` | Shared native/worker Still status and gap history. |
| `ANGEL_CAUSAL_GRAPH` | `~/.angel0/dossier/graph.json` | Shared graph used by the memory and experiment workers. |
| `ANGEL_GRAPH_LEASE_TOKEN` | worker-injected | Lease handoff from a scheduler to its synchronous child; not an operator arming control. |
| `ANGEL_HABIT_MIN_BELIEF` | `0.75` | Minimum habit belief for a skill proposal. |
| `ANGEL_HABIT_MAX_PROPOSALS_PER_DAY` | `2` | Daily Habitsmith proposal cap. |
| `ANGEL_BARREL_TTL_DAYS` | `7` | Barrel shard retention window, applied by Still. |
| `ANGEL_STILL_MIN_VALUE` | `10` | Minimum scored value for a record to enter a Still dataset. |
| `ANGEL_STILL_STUDENT_URL` | local endpoint discovery | Explicit student endpoint override; when configured, failure does not fall through to another destination. |

The packaged workers, their arming controls, budgets and evaluator contract are
listed in [Local workers](../../docs/WORKERS.md).
