# scripts/

Host-side belt around the cockpit. Not the TUI.

| Directory | What |
| --- | --- |
| `release/` | Public archive + verify gates |
| `check/` | Quality / boundary / connection gates |
| `runtime/` | Workers the cockpit actually spawns |

Research cohorts and operator-private forge/fleet live outside this tree.
